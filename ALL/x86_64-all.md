# appbundle
◆ alacritty : A cross-platform, GPU-accelerated terminal emulator. To install it use the --appbundle flag or the .appbundle extension.
◆ android-tools : Android platform tools. To install it use the --appbundle flag or the .appbundle extension.
◆ audacity : A program that lets you manipulate digital audio waveforms. To install it use the --appbundle flag or the .appbundle extension.
◆ brave : Web browser that blocks ads and trackers by default. To install it use the --appbundle flag or the .appbundle extension.
◆ caja : File manager for the MATE desktop. To install it use the --appbundle flag or the .appbundle extension.
◆ chromium : A web browser built for speed, simplicity, and security. To install it use the --appbundle flag or the .appbundle extension.
◆ desmume : Nintendo DS emulator. To install it use the --appbundle flag or the .appbundle extension.
◆ dunst : Customizable and lightweight notification-daemon. To install it use the --appbundle flag or the .appbundle extension.
◆ eden : Nintendo Switch emulator forked from yuzu.. To install it use the --appbundle flag or the .appbundle extension.
◆ engrampa : A file archiver for MATE. To install it use the --appbundle flag or the .appbundle extension.
◆ falkon : Cross-platform QtWebEngine browser. To install it use the --appbundle flag or the .appbundle extension.
◆ firefox : Fast, Private and Safe Web Browser. To install it use the --appbundle flag or the .appbundle extension.
◆ gimp : GNU Image Manipulation Program. To install it use the --appbundle flag or the .appbundle extension.
◆ imagemagick : An image viewing/manipulation program. To install it use the --appbundle flag or the .appbundle extension.
◆ mousepad : Simple text editor for Xfce. To install it use the --appbundle flag or the .appbundle extension.
◆ mpv : A free, open source, and cross-platform media player.. To install it use the --appbundle flag or the .appbundle extension.
◆ obs-studio : Free, open source software for live streaming and recording. To install it use the --appbundle flag or the .appbundle extension.
◆ openarena-quake3e : A Community-Produced Deathmatch FPS Based on GPL idTech3 Technology (Quake3e Engine). To install it use the --appbundle flag or the .appbundle extension.
◆ pcmanfm : Extremely fast and lightweight file manager. To install it use the --appbundle flag or the .appbundle extension.
◆ pcsx2 : PlayStation 2 emulator. To install it use the --appbundle flag or the .appbundle extension.
◆ polybar : A fast and easy-to-use status bar. To install it use the --appbundle flag or the .appbundle extension.
◆ ppsspp : A PSP emulator written in C++. To install it use the --appbundle flag or the .appbundle extension.
◆ qbittorrent : An advanced BitTorrent client programmed in C++, based on Qt toolkit and libtorrent-rasterbar. To install it use the --appbundle flag or the .appbundle extension.
◆ quake3e : Improved Quake III Arena Engine. To install it use the --appbundle flag or the .appbundle extension.
◆ ristretto : A fast and lightweight picture viewer for Xfce. To install it use the --appbundle flag or the .appbundle extension.
◆ scrcpy : Display and control your Android device. To install it use the --appbundle flag or the .appbundle extension.
◆ steam : A launcher for the Steam client from Valve Corporation. To install it use the --appbundle flag or the .appbundle extension.
◆ thunar : Modern, fast and easy-to-use file manager for Xfce. To install it use the --appbundle flag or the .appbundle extension.
◆ transmission-qt : Fast, easy, and free BitTorrent client (Qt GUI). To install it use the --appbundle flag or the .appbundle extension.
◆ unleashedrecomp : No description available. To install it use the --appbundle flag or the .appbundle extension.
◆ xfce4-multicall : No description available. To install it use the --appbundle flag or the .appbundle extension.
◆ xfce4-power-manager : Power Manager for Xfce. To install it use the --appbundle flag or the .appbundle extension.
◆ xfce4-screenshooter : An application to take screenshots. To install it use the --appbundle flag or the .appbundle extension.
◆ xfce4-taskmanager : Easy to use task manager. To install it use the --appbundle flag or the .appbundle extension.
◆ xfce4-terminal : A modern terminal emulator primarily for the Xfce desktop environment. To install it use the --appbundle flag or the .appbundle extension.
◆ zenity : Display graphical dialog boxes from shell scripts. To install it use the --appbundle flag or the .appbundle extension.

# busybox

# python
◆ python2.7 : Interactive high-level object-oriented language [AppImage, build cp27-cp27m-manylinux1]. To install it use the --python flag or the .python extension.
◆ python2.7 : Interactive high-level object-oriented language [AppImage, build cp27-cp27mu-manylinux1]. To install it use the --python flag or the .python extension.
◆ python3.10 : Interactive high-level object-oriented language [AppImage, build cp310-cp310-manylinux2010]. To install it use the --python flag or the .python extension.
◆ python3.10 : Interactive high-level object-oriented language [AppImage, build cp310-cp310-manylinux2014]. To install it use the --python flag or the .python extension.
◆ python3.10 : Interactive high-level object-oriented language [AppImage, build cp310-cp310-manylinux_2_24]. To install it use the --python flag or the .python extension.
◆ python3.10 : Interactive high-level object-oriented language [AppImage, build cp310-cp310-manylinux_2_28]. To install it use the --python flag or the .python extension.
◆ python3.11 : Interactive high-level object-oriented language [AppImage, build cp311-cp311-manylinux2014]. To install it use the --python flag or the .python extension.
◆ python3.11 : Interactive high-level object-oriented language [AppImage, build cp311-cp311-manylinux_2_24]. To install it use the --python flag or the .python extension.
◆ python3.11 : Interactive high-level object-oriented language [AppImage, build cp311-cp311-manylinux_2_28]. To install it use the --python flag or the .python extension.
◆ python3.12 : Interactive high-level object-oriented language [AppImage, build cp312-cp312-manylinux2014]. To install it use the --python flag or the .python extension.
◆ python3.12 : Interactive high-level object-oriented language [AppImage, build cp312-cp312-manylinux_2_28]. To install it use the --python flag or the .python extension.
◆ python3.13 : Interactive high-level object-oriented language [AppImage, build cp313-cp313-manylinux2014]. To install it use the --python flag or the .python extension.
◆ python3.13 : Interactive high-level object-oriented language [AppImage, build cp313-cp313-manylinux_2_28]. To install it use the --python flag or the .python extension.
◆ python3.13 : Interactive high-level object-oriented language [AppImage, build cp313-cp313t-manylinux2014]. To install it use the --python flag or the .python extension.
◆ python3.13 : Interactive high-level object-oriented language [AppImage, build cp313-cp313t-manylinux_2_28]. To install it use the --python flag or the .python extension.
◆ python3.14 : Interactive high-level object-oriented language [AppImage, build cp314-cp314-manylinux2014]. To install it use the --python flag or the .python extension.
◆ python3.14 : Interactive high-level object-oriented language [AppImage, build cp314-cp314-manylinux_2_28]. To install it use the --python flag or the .python extension.
◆ python3.14 : Interactive high-level object-oriented language [AppImage, build cp314-cp314t-manylinux2014]. To install it use the --python flag or the .python extension.
◆ python3.14 : Interactive high-level object-oriented language [AppImage, build cp314-cp314t-manylinux_2_28]. To install it use the --python flag or the .python extension.
◆ python3.5 : Interactive high-level object-oriented language [AppImage, build cp35-cp35m-manylinux1]. To install it use the --python flag or the .python extension.
◆ python3.6 : Interactive high-level object-oriented language [AppImage, build cp36-cp36m-manylinux1]. To install it use the --python flag or the .python extension.
◆ python3.6 : Interactive high-level object-oriented language [AppImage, build cp36-cp36m-manylinux2010]. To install it use the --python flag or the .python extension.
◆ python3.6 : Interactive high-level object-oriented language [AppImage, build cp36-cp36m-manylinux2014]. To install it use the --python flag or the .python extension.
◆ python3.6 : Interactive high-level object-oriented language [AppImage, build cp36-cp36m-manylinux_2_24]. To install it use the --python flag or the .python extension.
◆ python3.6 : Interactive high-level object-oriented language [AppImage, build cp36-cp36m-manylinux_2_28]. To install it use the --python flag or the .python extension.
◆ python3.7 : Interactive high-level object-oriented language [AppImage, build cp37-cp37m-manylinux1]. To install it use the --python flag or the .python extension.
◆ python3.7 : Interactive high-level object-oriented language [AppImage, build cp37-cp37m-manylinux2010]. To install it use the --python flag or the .python extension.
◆ python3.7 : Interactive high-level object-oriented language [AppImage, build cp37-cp37m-manylinux2014]. To install it use the --python flag or the .python extension.
◆ python3.7 : Interactive high-level object-oriented language [AppImage, build cp37-cp37m-manylinux_2_24]. To install it use the --python flag or the .python extension.
◆ python3.7 : Interactive high-level object-oriented language [AppImage, build cp37-cp37m-manylinux_2_28]. To install it use the --python flag or the .python extension.
◆ python3.8 : Interactive high-level object-oriented language [AppImage, build cp38-cp38-manylinux1]. To install it use the --python flag or the .python extension.
◆ python3.8 : Interactive high-level object-oriented language [AppImage, build cp38-cp38-manylinux2010]. To install it use the --python flag or the .python extension.
◆ python3.8 : Interactive high-level object-oriented language [AppImage, build cp38-cp38-manylinux2014]. To install it use the --python flag or the .python extension.
◆ python3.8 : Interactive high-level object-oriented language [AppImage, build cp38-cp38-manylinux_2_24]. To install it use the --python flag or the .python extension.
◆ python3.8 : Interactive high-level object-oriented language [AppImage, build cp38-cp38-manylinux_2_28]. To install it use the --python flag or the .python extension.
◆ python3.9 : Interactive high-level object-oriented language [AppImage, build cp39-cp39-manylinux1]. To install it use the --python flag or the .python extension.
◆ python3.9 : Interactive high-level object-oriented language [AppImage, build cp39-cp39-manylinux2010]. To install it use the --python flag or the .python extension.
◆ python3.9 : Interactive high-level object-oriented language [AppImage, build cp39-cp39-manylinux2014]. To install it use the --python flag or the .python extension.
◆ python3.9 : Interactive high-level object-oriented language [AppImage, build cp39-cp39-manylinux_2_24]. To install it use the --python flag or the .python extension.
◆ python3.9 : Interactive high-level object-oriented language [AppImage, build cp39-cp39-manylinux_2_28]. To install it use the --python flag or the .python extension.

# soarpkg
◆ 2to3 : Python2 to Python3 converter [python3]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ 40four : A tech enumeration toolkit focused on 404 Not found pages. To install it use the --soarpkg flag or the .soarpkg extension.
◆ 7z : Command line archiver utility. To install it use the --soarpkg flag or the .soarpkg extension.
◆ acpid : Listen to ACPI events and spawn specific helpers on event arrival [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ acpid : Listen to ACPI events and spawn specific helpers on event arrival [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ act : Run your GitHub Actions locally. To install it use the --soarpkg flag or the .soarpkg extension.
◆ actionlint : Static checker for GitHub Actions workflow files. To install it use the --soarpkg flag or the .soarpkg extension.
◆ add-section : Add a dummy/minimal Section Header to binaries with no section headers [elftools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ add-shell : Add SHELLs to /etc/shells [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ add-shell : Add SHELLs to /etc/shells [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ addgroup : Add a group or add a user to a group [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ addgroup : Add a group or add a user to a group [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ addpart : Tell the kernel about the existence of a specified partition [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ addpart : Tell the kernel about the existence of a specified partition [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ addr2line : Converts addresses into filenames and line numbers [bintools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ addr2line : Converts addresses into filenames and line numbers [binutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ addr6 : Decode IPv6 addresses [ipv6toolkit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ adduser : Add a user to the System [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ adduser : Add a user to the System [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ adjtimex : Read and optionally set system timebase parameters [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ adjtimex : Read and optionally set system timebase parameters [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ afio : Archiver & backup program with fault tolerant compression. To install it use the --soarpkg flag or the .soarpkg extension.
◆ afrog : Security Tool & Vulnerability Scanner for Bug Bounty, Pentest and Red Teaming. To install it use the --soarpkg flag or the .soarpkg extension.
◆ age : A simple, modern and secure encryption tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ age-keygen : A simple, modern and secure encryption tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ agetty : Open a tty and prompt for login (acts as an alternative Linux getty) [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ agg : Asciicast (Asciinema) to GIF converter. To install it use the --soarpkg flag or the .soarpkg extension.
◆ agg-speed : Aggregates multiple links (TCP, Bluetooth, USB or similar) into one connection. To install it use the --soarpkg flag or the .soarpkg extension.
◆ agg-tunnel : Aggregates multiple links (TCP, Bluetooth, USB or similar) into one connection. To install it use the --soarpkg flag or the .soarpkg extension.
◆ agnos : Easily obtain certificates (including wildcards) from Let's Encrypt using DNS-01 challenges. To install it use the --soarpkg flag or the .soarpkg extension.
◆ agrind : Slice and dice logs on the command line. To install it use the --soarpkg flag or the .soarpkg extension.
◆ airixss : Finding XSS during recon. To install it use the --soarpkg flag or the .soarpkg extension.
◆ aix : AIx is a cli tool to interact with Large Language Models (LLM) APIs. To install it use the --soarpkg flag or the .soarpkg extension.
◆ alacritty : GPU-accelerated terminal emulator. To install it use the --soarpkg flag or the .soarpkg extension.
◆ albafetch : Faster neofetch alternative, written in C. To install it use the --soarpkg flag or the .soarpkg extension.
◆ algernon : Small self-contained pure-Go web server with Lua, Teal, Markdown, HTTP/2, QUIC, Redis and PostgreSQL support. To install it use the --soarpkg flag or the .soarpkg extension.
◆ alist : A file list/WebDAV program that supports multiple storages. To install it use the --soarpkg flag or the .soarpkg extension.
◆ aliyun-cli : Universal Command-Line Interface for Alibaba Cloud. To install it use the --soarpkg flag or the .soarpkg extension.
◆ allxfr : AXFR all the things. To install it use the --soarpkg flag or the .soarpkg extension.
◆ almalinux : Portable Ephemeral AlmaLinux Docker Image (DockerC). To install it use the --soarpkg flag or the .soarpkg extension.
◆ almalinux-minimal : Portable Ephemeral AlmaLinux Docker Image (DockerC). To install it use the --soarpkg flag or the .soarpkg extension.
◆ alpine : Portable Ephemeral Alpine Linux Docker Image (DockerC). To install it use the --soarpkg flag or the .soarpkg extension.
◆ alterx : Fast and customizable subdomain wordlist generator using DSL. To install it use the --soarpkg flag or the .soarpkg extension.
◆ am-i-isolated : Validate the isolation posture of Container Environments. To install it use the --soarpkg flag or the .soarpkg extension.
◆ amass : In-depth attack surface mapping and asset discovery. To install it use the --soarpkg flag or the .soarpkg extension.
◆ amazonlinux : Portable Ephemeral AmazonLinux Docker Image (DockerC). To install it use the --soarpkg flag or the .soarpkg extension.
◆ amp : A complete text editor for your terminal. To install it use the --soarpkg flag or the .soarpkg extension.
◆ anakin : Kill orphan processes. To install it use the --soarpkg flag or the .soarpkg extension.
◆ analyticsrelationships : Get related domains/subdomains by looking at Google Analytics IDs. To install it use the --soarpkg flag or the .soarpkg extension.
◆ anew : A tool for adding new lines to files, skipping duplicates. To install it use the --soarpkg flag or the .soarpkg extension.
◆ anew-rs : An efficient way to filter duplicate lines from input, à la uniq. To install it use the --soarpkg flag or the .soarpkg extension.
◆ anewer : Append lines from stdin to a file if they don't already exist in the file. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ani-cli : CLI to browse and watch anime. To install it use the --soarpkg flag or the .soarpkg extension.
◆ anipy-cli : Watch and Download Anime from the terminal. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ansi2html : ANSI to HTML converter [colorized-logs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ansi2txt : ANSI to plain text converter [colorized-logs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ anyproxy : TCP stream forwarder & Proxy. To install it use the --soarpkg flag or the .soarpkg extension.
◆ aperf : Gather performance data and visualize using HTML graphs. To install it use the --soarpkg flag or the .soarpkg extension.
◆ apk-static : Alpine Package Keeper (apk). To install it use the --soarpkg flag or the .soarpkg extension.
◆ apk.sh : Make reverse engineering Android apps easier. To install it use the --soarpkg flag or the .soarpkg extension.
◆ apkeep : Command-line tool for downloading APK files from various sources. To install it use the --soarpkg flag or the .soarpkg extension.
◆ apply : Apply a command to a set of arguments [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ apply : Apply a command to a set of arguments [outils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ apprise : Send notifications from the command-line to popular notification services. To install it use the --soarpkg flag or the .soarpkg extension.
◆ appstreamcli : Tool to work with AppStream metadata. To install it use the --soarpkg flag or the .soarpkg extension.
◆ apptainer : Standalone Linux Container Alternative to docker/podman that runs anywhere with 0 Dependency. To install it use the --soarpkg flag or the .soarpkg extension.
◆ apptainer : Standalone Linux Container Alternative to docker/podman that runs anywhere with 0 Dependency. To install it use the --soarpkg flag or the .soarpkg extension.
◆ aptly : Swiss army knife for Debian repository management. To install it use the --soarpkg flag or the .soarpkg extension.
◆ apx : Package manager with support for multiple sources. To install it use the --soarpkg flag or the .soarpkg extension.
◆ aqtinstall : Qt CLI Installer on multi-platforms. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ar : A utility for creating, modifying and extracting from archives [bintools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ar : A utility for creating, modifying and extracting from archives [binutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ arc : Tiny package manager for Linux. To install it use the --soarpkg flag or the .soarpkg extension.
◆ arch : Print machine hardware name (same as uname -m) [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ arch : Print machine hardware name (same as uname -m) [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ arch : Print system architecture [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ arch : Print system architecture [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ archey : A simple system information tool written in Python. To install it use the --soarpkg flag or the .soarpkg extension.
◆ archey : A simple system information tool written in Python. To install it use the --soarpkg flag or the .soarpkg extension.
◆ archey : A simple system information tool written in Python. To install it use the --soarpkg flag or the .soarpkg extension.
◆ archive-finder : Scan a list of hosts to locate potential archive files (.zip, .tar, .rar, etc.). To install it use the --soarpkg flag or the .soarpkg extension.
◆ archivemount : Mount cpio, .tar.gz, .tar.bz2 Archives. To install it use the --soarpkg flag or the .soarpkg extension.
◆ archlinux : Portable Ephemeral ArchLinux Docker Image (DockerC). To install it use the --soarpkg flag or the .soarpkg extension.
◆ arduino-cli : Arduino command line tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ares : Automated decoding of encrypted text without knowing the key or ciphers used. To install it use the --soarpkg flag or the .soarpkg extension.
◆ aretext : Minimalist text editor with vim-compatible key bindings. To install it use the --soarpkg flag or the .soarpkg extension.
◆ aria2 : A multi-protocol (HTTP/HTTPS, FTP, SFTP, BitTorrent & Metalink) & multi-source command-line download utility. To install it use the --soarpkg flag or the .soarpkg extension.
◆ aria2 : A multi-protocol (HTTP/HTTPS, FTP, SFTP, BitTorrent & Metalink) & multi-source command-line download utility. To install it use the --soarpkg flag or the .soarpkg extension.
◆ aria2c : A multi-protocol (HTTP/HTTPS, FTP, SFTP, BitTorrent & Metalink) & multi-source command-line download utility. To install it use the --soarpkg flag or the .soarpkg extension.
◆ arp : Manipulate ARP cache [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ arp : Manipulate ARP cache [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ arp : Manipulate the system ARP cache. To install it use the --soarpkg flag or the .soarpkg extension.
◆ arpd : Userspace arp daemon [iproute2]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ arping : Send ARP REQUEST to a neighbour host [iputils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ arping : Send ARP requests/replies [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ arping : Send ARP requests/replies [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ arti : Tor reimplementation in Rust. To install it use the --soarpkg flag or the .soarpkg extension.
◆ as : Standalone C Assembler [xcc]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ as : The GNU Assembler [bintools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ as : The GNU Assembler [binutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ascii : Prints a formatted ASCII table with decimal, hex, and symbols [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ascii : Prints a formatted ASCII table with decimal, hex, and symbols [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ash : Unix shell interpreter [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ash : Unix shell interpreter [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ askalono : Tool to detect open source licenses from texts. To install it use the --soarpkg flag or the .soarpkg extension.
◆ asn : OSINT command line tool for investigating network data. To install it use the --soarpkg flag or the .soarpkg extension.
◆ asnmap : Tool to gather network ranges using ASN information. To install it use the --soarpkg flag or the .soarpkg extension.
◆ assetfinder : Find domains and subdomains related to a given domain. To install it use the --soarpkg flag or the .soarpkg extension.
◆ assh : Smart SSH client wrapper. To install it use the --soarpkg flag or the .soarpkg extension.
◆ aterm : Record a terminal session in a separate pty & upload it to an ASHIRT server. To install it use the --soarpkg flag or the .soarpkg extension.
◆ atuin : Sync, search and backup tool for shell history. To install it use the --soarpkg flag or the .soarpkg extension.
◆ audiobook-dl : Audiobook CLI downloader. To install it use the --soarpkg flag or the .soarpkg extension.
◆ autopoint : Copies standard gettext infrastructure files into a source package [gettext]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ await : Run list of commands in parallel and waits for their termination. To install it use the --soarpkg flag or the .soarpkg extension.
◆ awk : One true awk, as described in The AWK Programming Language book with Support for UTF-8 and CSV Input. To install it use the --soarpkg flag or the .soarpkg extension.
◆ awk : Pattern scanning and text processing language for extracting and transforming data [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ awk : Pattern scanning and text processing language for extracting and transforming data [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ awk : Pattern scanning and text processing language for extracting and transforming data [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ aws : Universal Command Line Interface for Amazon Web Services. To install it use the --soarpkg flag or the .soarpkg extension.
◆ aws-nuke : Remove all the resources from an AWS account. To install it use the --soarpkg flag or the .soarpkg extension.
◆ aws-wire-lengths : Simple command line for various AWS management tasks. To install it use the --soarpkg flag or the .soarpkg extension.
◆ axel : Lightweight CLI download accelerator. To install it use the --soarpkg flag or the .soarpkg extension.
◆ axfr2hosts : Transfer DNS zones (AXFR) and convert A/AAAA/CNAME records into a hosts file for offline use. To install it use the --soarpkg flag or the .soarpkg extension.
◆ b2sum : Compute and check BLAKE2 message digest [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ b2sum : Compute and check BLAKE2 message digest [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ b2sum : Compute and check BLAKE2 message digest [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ b3sum : Command line utility for calculating BLAKE3 hashes. To install it use the --soarpkg flag or the .soarpkg extension.
◆ background : Launch a command in the background [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ background : Launch a command in the background [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ backtick : Run a program and stores its output in an environment variable [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ backtick : Run a program and stores its output in an environment variable [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ backtrace : Analyze return routes to Chinese ISPs and identify carrier-grade lines. To install it use the --soarpkg flag or the .soarpkg extension.
◆ badblocks : Search a device for bad blocks [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ badblocks : Search a device for bad blocks [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ baddns : Tool to check subdomains for subdomain takeovers and other DNS issues. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bandwhich : Terminal bandwidth utilization tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ base32 : Base32 encode/decode data and print to standard output [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ base32 : Base32 encode/decode data and print to standard output [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ base32 : Base32 encode/decode data and print to standard output [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ base32 : Base32 encode/decode data and print to standard output [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ base32 : Encode or decode data using Base32 encoding [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ base32 : Encode or decode data using Base32 encoding [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ base64 : Base64 encode/decode data and print to standard output [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ base64 : Base64 encode/decode data and print to standard output [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ base64 : Base64 encode/decode data and print to standard output [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ base64 : Base64 encode/decode data and print to standard output [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ base64 : Encode or decode data using Base64 encoding [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ base64 : Encode or decode data using Base64 encoding [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ basename : Strip directory and suffix from filenames [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ basename : Strip directory and suffix from filenames [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ basename : Strip directory and suffix from filenames [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ basename : Strip directory and suffix from filenames [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ basename : Strip directory path and .SUFFIX from FILE [bsdutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ basename : Strip directory path and .SUFFIX from FILE [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ basename : Strip directory path and .SUFFIX from FILE [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ basename : Strip directory path and .SUFFIX from FILE [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ basenc : Encode/decode data and print to standard output [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ basenc : Encode/decode data and print to standard output [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ basenc : Encode/decode data and print to standard output [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ basenc : Encode/decode data and print to standard output [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bash : GNU Bourne-Again Shell, the de facto standard shell on Linux. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bash : GNU Bourne-Again Shell, the de facto standard shell on Linux. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bat : Cat clone with colors, syntax highlighting and git integration. To install it use the --soarpkg flag or the .soarpkg extension.
◆ batcat : Cat clone with colors, syntax highlighting and git integration. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bbscope : Generate In-Scope Items based on URLs, Subdomains or BurpSuite Config File [lit-bb-hack-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bbtargets : Extract Bug Bounty targets from Project Discovery Chaos list [lit-bb-hack-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bc : An arbitrary precision calculator language [bsdutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bc : An arbitrary precision calculator language [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bc : An arbitrary precision calculator language [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bc : GNU software calculator. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bearer : Code security scanning tool (SAST). To install it use the --soarpkg flag or the .soarpkg extension.
◆ bed : Binary editor written in Go. To install it use the --soarpkg flag or the .soarpkg extension.
◆ beep : Generate a tone through the PC speaker [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ beep : Generate a tone through the PC speaker [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ beet : Flexible music library manager and tagger. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bench : A quick & easy Benchmarking Script to Auto test I/O & Bandwidth. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bench_bitcoin : Execute microbenchmarks [bitcoin]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ beszel : Lightweight server monitoring hub with historical data, docker stats, and alerts. To install it use the --soarpkg flag or the .soarpkg extension.
◆ beszel-agent : Lightweight server monitoring hub with historical data, docker stats, and alerts. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bin : Effortless binary manager. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bin-graph : Visualize binary files. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bin2video : Tool for encoding files as videos. To install it use the --soarpkg flag or the .soarpkg extension.
◆ binary-security-check : Analyzer of security features in executable binaries. To install it use the --soarpkg flag or the .soarpkg extension.
◆ binfmt : Tool to enable/setup binfmt. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bingrep : Binary Parser, Analyzer and Colorizer. To install it use the --soarpkg flag or the .soarpkg extension.
◆ binocle : A graphical tool to visualize binary data. To install it use the --soarpkg flag or the .soarpkg extension.
◆ binsider : A toolkit to perform analysis on binaries. To install it use the --soarpkg flag or the .soarpkg extension.
◆ binsider : A toolkit to perform analysis on binaries. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bintrans : A Dynamic Binary Translator [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bison : Yacc-compatible parser generator. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bita : Differential file synchronization over http. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bitcoin-cli : Bitcoin Core RPC client [bitcoin]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bitcoin-tx : Create, Parse, or Modify Bitcoin Core transactions [bitcoin]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bitcoin-wallet : Offline tool for creating and interacting with Bitcoin Core wallet files [bitcoin]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bitcoind : Headless bitcoin daemon that connects to the Bitcoin network [bitcoin]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ blackhole6 : Find IPv6 blackholes [ipv6toolkit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bleve : CLI for interacting with a Bleve index. To install it use the --soarpkg flag or the .soarpkg extension.
◆ blkdiscard : Discard blocks on a block device [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ blkdiscard : Discard blocks on a block device [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ blkdiscard : Discard the content of sectors on a device [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ blkid : Print UUIDs of all filesystems [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ blkid : Print UUIDs of all filesystems [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ blkid : Print block device information [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ blkid : User command-line interface for libblkid [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ blkpr : Run persistent reservations command on a device [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ blkzone : Run zone command on the given block device [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ blockdev : Call block device ioctls from the command line [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ blockdev : Perform block device operations [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ blockdev : Perform block device operations [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bluetuith : A TUI bluetooth manager for Linux. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bmake : Portable version of NetBSD make utility. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bombadillo : Browser for gopher/gemini/finger or local file system. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bomtool : Package compiler and linker metadata Toolkit. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bootchartd : Create performance charts of the boot process [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bootchartd : Create performance charts of the boot process [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bore : A command line tool for making tunnels to localhost. To install it use the --soarpkg flag or the .soarpkg extension.
◆ botsay : Like cowsay, but with randomly generated ASCII robots and color support. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bottom : A customizable Graphical Process/System Monitor for the Terminal. To install it use the --soarpkg flag or the .soarpkg extension.
◆ boxxy : Put bad Linux applications in a box with only their files. To install it use the --soarpkg flag or the .soarpkg extension.
◆ brctl : Manage ethernet bridges [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ brctl : Manage ethernet bridges [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ brename : Tool for batch renaming files/directories via regular expressions. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bridge : Show/Manipulate bridge addresses and devices [iproute2]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ brook : A Zero Config strong encryption and not detectable proxy. To install it use the --soarpkg flag or the .soarpkg extension.
◆ broot : Interactive directory tree view, fuzzy search, balanced BFS descent. To install it use the --soarpkg flag or the .soarpkg extension.
◆ brotli : Generic-purpose lossless compression tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ brutespray : Bruteforcing from various scanner output & Automatically attempts default creds on found services. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bsdcat : Expand files/std-in to standard output, decompressing data as needed [libarchive]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bsdcpio : Copy files to and from archives [libarchive]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bsdtar : Create/Manipulate Archive Files [libarchive]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bsdunzip : Extract files from a ZIP archive [libarchive]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ btm : A customizable Graphical Process/System Monitor for the Terminal. To install it use the --soarpkg flag or the .soarpkg extension.
◆ btop : Modern and colorful command line resource monitor that shows usage and stats. To install it use the --soarpkg flag or the .soarpkg extension.
◆ btrfs : Toolbox for managing btrfs filesystems [btrfs-progs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ btrfs-convert : Convert existing source filesystem image to a btrfs filesystem in-place [btrfs-progs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ btrfs-find-root : Find the satisfied root by root tree, objectid, generation, level [btrfs-progs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ btrfs-image : Create an image of a btrfs filesystem [btrfs-progs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ btrfs-map-logical : Tool to get physical offsets [btrfs-progs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ btrfs-select-super : Destructively overwrite all copies of the superblock with a specified copy [btrfs-progs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ btrfsck : btrfs filesystem checker to verify structural integrity & repair (alias of btrfs check) [btrfs-progs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ btrfstune : Enable, Disable, or set various filesystem parameters [btrfs-progs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bucketloot : Automated S3-compatible bucket inspector. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bulkdns : DNS scanner for large-scale measurement. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bunzip2 : Uncompress FILE (or standard input if FILE is '-' or omitted) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bunzip2 : Uncompress FILE (or standard input if FILE is '-' or omitted) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ busybox : A multi-purpose executable providing many standard UNIX utilities [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ busybox : Core u-root cmds into a Single Busybox like Multicall Binary. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bwrap : Namespaces-powered chroot-like solution for Unprivileged Sandboxing (Patched). To install it use the --soarpkg flag or the .soarpkg extension.
◆ bwrap : Namespaces-powered chroot-like solution for Unprivileged Sandboxing (Patched). To install it use the --soarpkg flag or the .soarpkg extension.
◆ bwrap : Namespaces-powered chroot-like solution for Unprivileged Sandboxing. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bwrap : Namespaces-powered chroot-like solution for Unprivileged Sandboxing. To install it use the --soarpkg flag or the .soarpkg extension.
◆ byacc : Berkeley YACC. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bzcat : Uncompress to stdout [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bzcat : Uncompress to stdout [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bzip2 : Compress FILE(s) with bzip2 algorithm [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bzip2 : Compress FILE(s) with bzip2 algorithm [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ bzip3 : Efficient statistical file compressor and spiritual successor to bzip2 [bzip3]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ c2w : Container to WASM converter. To install it use the --soarpkg flag or the .soarpkg extension.
◆ c2w-net : Container to WASM converter. To install it use the --soarpkg flag or the .soarpkg extension.
◆ c_rehash : Toolkit for the Transport Layer Security (TLS) and Secure Sockets Layer (SSL) protocols. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cache : Wrap any command line tool to provide caching for the output of that too. To install it use the --soarpkg flag or the .soarpkg extension.
◆ caddy : A powerful, enterprise-ready, and extensible HTTP/2 + HTTP/3 web server with automatic HTTPS (Beta). To install it use the --soarpkg flag or the .soarpkg extension.
◆ caddy : A powerful, enterprise-ready, and extensible HTTP/2 + HTTP/3 web server with automatic HTTPS. To install it use the --soarpkg flag or the .soarpkg extension.
◆ caido-cli : CLI for Caido: Lightweight web security auditing toolkit. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cal : Display a calendar [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cal : Display a calendar [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cal : Display a calendar, or some part of it [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cal : an implementation of the POSIX cal command, as described in the OpenBSD manpage, that strives to have the same alignment as Busybox's and has (or tries to have) the Toybox's cal option to highlight specified days. To install it use the --soarpkg flag or the .soarpkg extension.
◆ calendar : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ calendar : Reminder service [outils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cap2hccapx : Generate .hccapx files from .cap/.pcap to crack WPA/WPA2 Authentications [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ capsh : Capability Shell Wrapper [libcap]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ captest : Demonstrate capabilities [libcap-ng]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ captree : Display tree of process capabilities [libcap]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cargo-binstall : Binary installation for Rust projects. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cargo-quickinstall : cargo install with Pre-Compiled binary packages. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cargo-unlock : Remove Cargo.lock lockfile. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cariddi : Take a list of domains, crawl urls and scan for endpoints, secrets, api keys, file extensions, tokens and more. To install it use the --soarpkg flag or the .soarpkg extension.
◆ case : Compare a value against a series of regular expressions & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ case : Compare a value against a series of regular expressions & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cat : Concatenate FILE(s) and print them to stdout [bsdutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cat : Concatenate FILE(s) and print them to stdout [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cat : Concatenate FILE(s) and print them to stdout [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cat : Concatenate FILE(s) and print them to stdout [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cat : Concatenate files and print on the standard output [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cat : Concatenate files and print on the standard output [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cat : Concatenate files and print on the standard output [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cat : Concatenate files and print on the standard output [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ catcher : Fingerprint assets, Scan for Vulnerabilities & Verify them. To install it use the --soarpkg flag or the .soarpkg extension.
◆ catp : Print the output of a running process. To install it use the --soarpkg flag or the .soarpkg extension.
◆ catv : the harmful features of cat. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cc1 : Standalone C Compiler [xcc]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ccat : syntax highlighted cat. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cd : Change the current working directory to a given directory & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cd : Change the current working directory to a given directory & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cdncheck : A utility to detect various technology for a given IP address. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cdwe : cdwe (cd with env) is a configurable cd wrapper that lets you define your environment per Directory. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cent : Fetch & Organize all Nuclei Templates. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cert-cli : Obtain domains from SSL/TLS certificates using crt.sh. To install it use the --soarpkg flag or the .soarpkg extension.
◆ certbot : Tool to obtain certificates from Lets Encrypt and other ACME compliant CAs. To install it use the --soarpkg flag or the .soarpkg extension.
◆ certgraph : An open source intelligence tool to crawl the graph of certificate Alternate Names. To install it use the --soarpkg flag or the .soarpkg extension.
◆ certinfo : Scrape domain names from SSL certificates of arbitrary hosts. To install it use the --soarpkg flag or the .soarpkg extension.
◆ certspotter : Certificate Transparency Log Monitor. To install it use the --soarpkg flag or the .soarpkg extension.
◆ certstream-server : Certstream Server that aggregates, parses, and streams certificate data from multiple Certificate Transparency Logs. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cexec : Execute any command, cache its output and get cached output on subsequent execution. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cfdisk : Display or manipulate a disk partition table [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cfspeed : GO CLI-based implementation of speed.cloudflare.com. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cfspeedtest : Unofficial CLI for speed.cloudflare.com. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cfssl : CLI to issue/sign/bundle client certificates & a HTTP server to do them remotely [cfssl]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cfssl-bundle : CLI to bundle local/remote certificate files [cfssl]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cfssl-certinfo : CLI to output certinfo about the given cert [cfssl]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cfssl-newkey : CLI to generate a new key and CSR [cfssl]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cfssl-scan : CLI to scan a host for certificate issues [cfssl]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cfssljson : CLI to split out JSON with cert, csr, and key fields to separate files [cfssl]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cftun : Third-party Cloudflare Tunnel client. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cgrep : Code GREP searching for regexp, an RE2 (nearly PCRE) regular expression [codesearch]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chafa : Command-line utility for displaying images in a terminal. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chage : Change user password expiry information [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chainif : Conditionally run command chains based on another command's success or failure [emanutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chainredir : Display URL redirect chain [lit-bb-hack-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chaos-client : Client to interact with Project Discovery's Chaos DNS API. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chars : CLI tool to display information about unicode characters. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chat : Interact with a modem connected to stdin/stdou [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chat : Interact with a modem connected to stdin/stdou [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chattr : Change file attributes on a Linux file system [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chattr : Change file attributes on a Linux file system [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chattr : Change file attributes on an ext2 fs [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chattr : Change file attributes on an ext2 fs [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chcon : Change file security context [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chcon : Change file security context [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chcpu : Configure CPUs in a multi-processor system [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cheat : Create and view interactive cheatsheets on the command-line. To install it use the --soarpkg flag or the .soarpkg extension.
◆ checkbypass : Test WAF bypass attempts [lit-bb-hack-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ checkcdn : Accurately check whether the IP is a CDN node. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cherrybomb : Validating and Testing APIs using an OpenAPI file. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chfn : Change finger informaiton [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chfn : Change real user name and information [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chgpasswd : Update group passwords in batch mode [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chgrp : Change group ownership [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chgrp : Change group ownership [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chgrp : Change group ownership [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chgrp : Change the group membership of each FILE to GROUP [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chgrp : Change the group membership of each FILE to GROUP [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chisel : A fast TCP/UDP tunnel transported over HTTP, secured via SSH. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chmem : Set a particular size or range of memory online or offline [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chmod : Change file owner and group [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chmod : Change file owner and group [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chmod : Change file owner and group [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chmod : Change the permissions of a file or directory [bsdutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chmod : Change the permissions of a file or directory [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chmod : Change the permissions of a file or directory [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chmod : Change the permissions of a file or directory [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ choom : Display and adjust OOM-killer score [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chown : Change file owner and group [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chown : Change file owner and group [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chown : Change file owner and group [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chown : Change the owner and/or group of each FILE to OWNER and/or GROUP [bsdutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chown : Change the owner and/or group of each FILE to OWNER and/or GROUP [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chown : Change the owner and/or group of each FILE to OWNER and/or GROUP [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chown : Change the owner and/or group of each FILE to OWNER and/or GROUP [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chpasswd : Read user:password from stdin and update /etc/passwd [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chpasswd : Read user:password from stdin and update /etc/passwd [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chpasswd : Update passwords in batch mode [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chpst : Change the process state and run PROG [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chpst : Change the process state and run PROG [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chromedb : Read Chromium data (namely, cookies and local storage) straight from disk. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chronic : Run a command quietly unless it fails [moreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chroot : Run PROG with root directory set to NEWROOT [bsdutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chroot : Run PROG with root directory set to NEWROOT [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chroot : Run PROG with root directory set to NEWROOT [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chroot : Run PROG with root directory set to NEWROOT [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chroot : Run command or interactive shell with special root directory [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chroot : Run command or interactive shell with special root directory [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chroot : Run command or interactive shell with special root directory [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chrt : Manipulate real-time attributes of a process [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chrt : Manipulate real-time attributes of a process [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chrt : Show or change the real-time scheduling attributes of a process [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chsh : Change login shell [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chsh : Change login shell for a user [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chsrc : Change Source everywhere for every software. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chvt : Change the foreground virtual terminal to /dev/ttyN [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ chvt : Change the foreground virtual terminal to /dev/ttyN [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ci-info : CLI tool to get CI related information. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ciadpi : SOCKS proxy server implementing some DPI bypass methods. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cidr : CLI to perform various actions on CIDR ranges. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cindex : Prepare the trigram index for use by csearch [codesearch]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ciphey : Automatically decrypt encryptions without knowing the key or cipher, decode encodings, and crack hashes. To install it use the --soarpkg flag or the .soarpkg extension.
◆ circumflex : A command line tool for browsing Hacker News in the terminal. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cirrus-cli : CLI for executing Cirrus tasks locally and in any CI. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cksum : Calculate the CRC32 checksums of FILES [bsdutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cksum : Calculate the CRC32 checksums of FILES [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cksum : Calculate the CRC32 checksums of FILES [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cksum : Calculate the CRC32 checksums of FILES [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cksum : Compute and verify file checksums [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cksum : Compute and verify file checksums [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cksum : Compute and verify file checksums [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cksum : Compute and verify file checksums [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cksum : Display file checksums and block counts [outils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cleanpath : Extract unique paths from URL list [lit-bb-hack-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cleanup-rules : Strips rules from STDIN that are not compatible with a specified platform [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ clear : Clear screen [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ clear : Clear screen [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ clearlinux : Portable Ephemeral ClearLinux Docker Image (DockerC). To install it use the --soarpkg flag or the .soarpkg extension.
◆ cleed : Feed reader for the command line. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cli53 : CLI tool for the Amazon Route 53 DNS service. To install it use the --soarpkg flag or the .soarpkg extension.
◆ clipcat-menu : Clipcat client that utilizes a built-in or external finder to select clips. To install it use the --soarpkg flag or the .soarpkg extension.
◆ clipcat-notify : A tool for monitoring clipboard events & sending it to clipcatd. To install it use the --soarpkg flag or the .soarpkg extension.
◆ clipcatctl : CLI client for Clipcat. To install it use the --soarpkg flag or the .soarpkg extension.
◆ clipcatd : Server daemon for Clipcat. To install it use the --soarpkg flag or the .soarpkg extension.
◆ clive : Automate terminal operations. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cloak-client : A censorship circumvention tool to evade detection by authoritarian state adversaries. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cloak-server : A censorship circumvention tool to evade detection by authoritarian state adversaries. To install it use the --soarpkg flag or the .soarpkg extension.
◆ clockdiff : Measure clock difference between hosts [iputils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cloudcash : Check your cloud spending from the CLI. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cloudflare-warp-speedtest : Test the latency and speed of Cloudflare WARP IP addresses. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cloudflared : Cloudflare Tunnel daemon, Cloudflare Access toolkit, and DNS-over-HTTPS client. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cloudfox : Automating situational awareness for cloud penetration tests. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cloudlist : A tool for listing Assets from multiple Cloud Providers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cloudreve : Self-hosted file management and sharing system, supports multiple storage providers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cloudsword : Discover and test Cloud risks & Vulnerabilities. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cmd-wrapped : A CLI tool to view Unix shell history statistics, with support for zsh, bash, fish, and atuin. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cmdx : Manage tasks for your project such as test, build, format, lint, and release. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cmp : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cmp : Compares FILE1 vs stdin if FILE2 is not specified [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cmp : Compares FILE1 vs stdin if FILE2 is not specified [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cntb : CLI tool for managing your products from Contabo like VPS and VDS. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cntr : A container debugging tool based on FUSE. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cobalt : Command-line interface wrapper for https://cobalt.tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cobalt-cli : Command-line interface wrapper for https://cobalt.tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cocogitto : Set of CLI tools for the conventional commit and semver specifications. To install it use the --soarpkg flag or the .soarpkg extension.
◆ code-server : Run VS Code on any machine anywhere and access it in the browser. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cog : Set of CLI tools for the conventional commit and semver specifications. To install it use the --soarpkg flag or the .soarpkg extension.
◆ col : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ col : Filter out reverse line feeds from standard input [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ colcrt : Filter nroff output for CRT previewing [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ colrm : Filter out the specified columns [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ colrm : Remove columns from a file [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ column : Columnate lists [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ column : Format input into multiple columns [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ comb : Combine the lines from two files in every combination. To install it use the --soarpkg flag or the .soarpkg extension.
◆ combinator : A stand-alone implementation of the Combinator Attack [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ combinator3 : Combinator that accepts three input files & produces three combined  lists [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ combine : Combine sets of lines from two files using boolean operations [moreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ combipow : Produce all unique combinations from a short list of inputs [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ comm : Compare FILE1 to FILE2, or to stdin if - is specified [bsdutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ comm : Compare FILE1 to FILE2, or to stdin if - is specified [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ comm : Compare FILE1 to FILE2, or to stdin if - is specified [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ comm : Compare FILE1 to FILE2, or to stdin if - is specified [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ comm : Compare two sorted files line by line [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ comm : Compare two sorted files line by line [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ comm : Compare two sorted files line by line [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ commix : Automated All-in-One OS Command Injection and Exploitation Tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ compile_et : Error table compiler [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ compiledb : Generate Clang's JSON Compilation Database files for make-based build systems. To install it use the --soarpkg flag or the .soarpkg extension.
◆ compiledb-go : Generate Clang's JSON Compilation Database files for make-based build systems. To install it use the --soarpkg flag or the .soarpkg extension.
◆ compress : Reduces the size of files [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ conspy : View and interact with the system console from a remote terminal [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ conspy : View and interact with the system console from a remote terminal [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ containerd : An open and reliable container runtime. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cookiemonster : Decode and Modify vulnerable session cookies from several different frameworks. To install it use the --soarpkg flag or the .soarpkg extension.
◆ coreutils : Multicall Binary for Coreutils: GNU File, Shell, and Text utilities. To install it use the --soarpkg flag or the .soarpkg extension.
◆ coreutils : Multicall Binary for Coreutils: GNU File, Shell, and Text utilities. To install it use the --soarpkg flag or the .soarpkg extension.
◆ coreutils : Multicall Binary for Coreutils: GNU File, Shell, and Text utilities. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cotp : Encrypted CLI TOTP/HOTP Authenticator. To install it use the --soarpkg flag or the .soarpkg extension.
◆ countryfetch : Neofetch like tool for obtaining information about your Country. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cowitness : mimic an HTTP server and a DNS server, providing complete responses. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cowsay : Cowsay reborn, written in Go. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cowthink : Cowsay reborn, written in Go. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cp : Copy SOURCE to DEST, or multiple SOURCE(s) to DIRECTORY [bsdutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cp : Copy SOURCE to DEST, or multiple SOURCE(s) to DIRECTORY [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cp : Copy SOURCE to DEST, or multiple SOURCE(s) to DIRECTORY [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cp : Copy SOURCE to DEST, or multiple SOURCE(s) to DIRECTORY [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cp : Copy files and directories [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cp : Copy files and directories [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cp : Copy files and directories [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cpio : Extract or list files from a cpio archive, or create a cpio archive [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cpio : Extract or list files from a cpio archive, or create a cpio archive [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cpp : Standalone C Preprocessor [xcc]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cpufetch : Simple yet fancy CPU architecture fetching tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cpuid2cpuflags : Generate CPU_FLAGS_* for your CPU. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cpustat : Periodically dump out the current CPU utilisation statistics of running processes. To install it use the --soarpkg flag or the .soarpkg extension.
◆ crane : Tool for interacting with remote OCI/Images/Registries without daemon & root. To install it use the --soarpkg flag or the .soarpkg extension.
◆ crave : CLI Client for Crave.io. To install it use the --soarpkg flag or the .soarpkg extension.
◆ crawley : The unix-way web crawler. To install it use the --soarpkg flag or the .soarpkg extension.
◆ crc32 : Calculate CRC32 checksums of FILES [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ crc32 : Calculate CRC32 checksums of FILES [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ crcsum : Check CRC Checksum [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ creatememfd : Create a memory file descriptor at a given FD & execute a command with [emanutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ credsweeper : Tool to detect credentials in any directories or files. To install it use the --soarpkg flag or the .soarpkg extension.
◆ crictl : CLI for kubelet CRI [cri-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ crlfuzz : A fast tool to scan CRLF vulnerability written in Go. To install it use the --soarpkg flag or the .soarpkg extension.
◆ croc : Easily and securely send things from one computer to another. To install it use the --soarpkg flag or the .soarpkg extension.
◆ croc : Easily and securely send things from one computer to another. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cron : Daemon for running commands at specific times [crontab]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ crond : Daemon to execute scheduled commands [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ crond : Daemon to execute scheduled commands [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ crontab : Command to manage cron jobs for the current user [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ crontab : Command to manage cron jobs for the current user [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ crontab : Program used to install, remove or list the tables used to drive the cron daemon [crontab]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ crt : A CLI tool to check Certificate Transparency logs of a domain name. To install it use the --soarpkg flag or the .soarpkg extension.
◆ crt : CLI tool to check Certificate Transparency logs using crt.sh. To install it use the --soarpkg flag or the .soarpkg extension.
◆ crtsher : Obtain domains from certificate transparency logs. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cryptpw : Crypt the PASSWORD using crypt(3) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cryptpw : Crypt the PASSWORD using crypt(3) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cryptsetup : Tool to setup and manage LUKS encrypted disk partitions [cryptsetup]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ csearch : Code GREP over all indexed files, searching for regexp, an RE2 (nearly PCRE) regular expression [codesearch]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ csplit : Split a file into sections determined by context lines [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ csplit : Split a file into sections determined by context lines [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ csplit : Split a file into sections determined by context lines [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ csplit : Split file into pieces [bsdutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ csplit : Split file into pieces [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ csprecon : Discover new target domains using Content Security Policy. To install it use the --soarpkg flag or the .soarpkg extension.
◆ csview : Pretty and fast csv viewer for cli with cjk/emoji support. To install it use the --soarpkg flag or the .soarpkg extension.
◆ csvtk : A cross-platform, efficient and practical CSV/TSV toolkit. To install it use the --soarpkg flag or the .soarpkg extension.
◆ csweb : Web UI to query CodeSearch [codesearch]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ct3_to_ntlm : Convert ct3 to NTLM [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ct_monitor : Certificate Transparency log monitor. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ctrlaltdel : Set the function of the Ctrl-Alt-Del combination [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ctrlaltdel : Set the function of the Ctrl-Alt-Del combination [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ctrsploit : A Penetration Toolkit for Container Environments [ctrsploit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ctrsploit-checksec : Check basic security posture on a Container Environment [ctrsploit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ctrsploit-env : Gather basic information on a Container Environment [ctrsploit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ctstat : Unified linux network connection tracking statistics [iproute2]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cttyhack : Option to allow a program to access the controlling terminal [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cttyhack : Option to allow a program to access the controlling terminal [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cup : Easiest way to check for container image updates. To install it use the --soarpkg flag or the .soarpkg extension.
◆ curl : A command line tool for transferring files with URL syntax. To install it use the --soarpkg flag or the .soarpkg extension.
◆ curlie : Frontend to cURL that adds the ease of use of HTTPie. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cut : Print selected fields from each input FILE to standard output [bsdutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cut : Print selected fields from each input FILE to standard output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cut : Print selected fields from each input FILE to standard output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cut : Print selected fields from each input FILE to standard output [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cut : Remove sections from each line of files [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cut : Remove sections from each line of files [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cut : Remove sections from each line of files [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cut : Remove sections from each line of files [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cut-cdn : Removing CDN IPs from the list of IP addresses. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cutb : Cut specific prefixes or suffixes from wordlist entries for use in Combinator attacks [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cutlines : Cut & Paste Lines (from. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cvemap : A structured and easily navigable interface to various vulnerability databases. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cvtsudoers : Convert between sudoers file formats [sudo]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ cwe_checker : Finds vulnerable patterns in binary executables. To install it use the --soarpkg flag or the .soarpkg extension.
◆ czkawka-cli : Multi functional app to find duplicates, empty folders, similar images etc. To install it use the --soarpkg flag or the .soarpkg extension.
◆ czkawka_cli : Multi functional app to find duplicates, empty folders, similar images etc. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dadd : Add DURATION to DATE/TIME and print the result (alias=dateadd) [dateutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dae : eBPF-based Linux high-performance Transparent Proxy solution. To install it use the --soarpkg flag or the .soarpkg extension.
◆ daemonize : Run a program as a Unix daemon. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dagger-cli : A portable devkit for CI/CD pipelines. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dagu : Cron alternative with a Web UI. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dalfox : A powerful open-source XSS scanner and utility focused on automation. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dasel : Select, put and delete data from JSON, TOML, YAML, XML and CSV files with a single tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dash : POSIX compliant shell that aims to be as small as possible. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dash : POSIX compliant shell that aims to be as small as possible. To install it use the --soarpkg flag or the .soarpkg extension.
◆ datadash : Visualize and graph data in the terminal. To install it use the --soarpkg flag or the .soarpkg extension.
◆ date : Display time (using +FMT), or set time [bsdutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ date : Display time (using +FMT), or set time [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ date : Display time (using +FMT), or set time [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ date : Display time (using +FMT), or set time [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ date : Print or set the system date and time [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ date : Print or set the system date and time [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ date : Print or set the system date and time [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dateadd : Add DURATION to DATE/TIME and print the result [dateutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dateconv : Convert DATE/TIMEs between calendrical systems [dateutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ datediff : Compute duration from DATE/TIME (the reference date/time) to the other [dateutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dategrep : Grep standard input for lines that match EXPRESSION [dateutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dateround : Round DATE/TIME to the next occurrence of RNDSPEC [dateutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dateseq : Generate a sequence of date/times from FIRST to LAST or in INCREMENT [dateutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ datesort : Sort contents of FILE chronologically [dateutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ datetest : Like test(1) but for dates [dateutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ datezone : Convert DATE/TIMEs between timezones [dateutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ daytona : The Open Source Dev Environment Manager. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dbclient : Lightweight SSH client [dropbear]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dbclient : Lightweight SSH client [dropbear]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dbin : Poor man's package manager. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dblab : Interactive client for PostgreSQL, MySQL, SQLite3, Oracle and SQL Server. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dbus-cleanup-sockets : Clean up leftover sockets in a directory [dbus]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dbus-daemon : The D-Bus message bus daemon [dbus]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dbus-daemon-launch-helper : DBus SUID launch helper for autospawning [dbus]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dbus-launch : Utility to start a message bus from a shell script [dbus]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dbus-monitor : Monitor messages going through a D-Bus message bus [dbus]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dbus-run-session : Start a process as a new D-Bus session [dbus]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dbus-send : Send a message to a message bus [dbus]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dbus-test-tool : D-Bus traffic generator and test tool [dbus]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dbus-update-activation-environment : Update environment used for D-Bus session services [dbus]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dbus-uuidgen : Utility to generate UUIDs [dbus]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dc : Alternative to GNU coreutils using software from FreeBSD. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dc : Tiny RPN calculator [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dc : Tiny RPN calculator [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dcb : Show/Manipulate DCB (Data Center Bridging) settings [iproute2]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dconv : Convert DATE/TIMEs between calendrical systems (alias=dateconv) [dateutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dd : Convert and Copy a file [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dd : Convert and Copy a file [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dd : Convert and Copy a file [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dd : Copy a file with converting and formatting [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dd : Copy a file with converting and formatting [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dd : Copy a file with converting and formatting [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ddgr : DuckDuckGo from the terminal. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ddiff : Compute duration from DATE/TIME (the reference date/time) to the other (alias=datediff) [dateutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ddns-go : Simple and easy to use DDNS. To install it use the --soarpkg flag or the .soarpkg extension.
◆ deallocvt : Deallocate unused virtual terminal /dev/ttyN [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ deallocvt : Deallocate unused virtual terminal /dev/ttyN [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ deark : A utility for extracting data from various file formats. To install it use the --soarpkg flag or the .soarpkg extension.
◆ debugfs : ext2/ext3/ext4 file system debugger [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ debugfs : ext2/ext3/ext4 file system debugger [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ define : Replace a literal with a value & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ define : Replace a literal with a value & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ delgroup : Delete group GROUP from the system or user USER from group GROUP [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ delgroup : Delete group GROUP from the system or user USER from group GROUP [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ delpart : Tell the kernel to forget about a partition [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ delpart : Tell the kernel to forget about a specified partition [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ delta : A syntax-highlighting pager for git, diff, and grep output. To install it use the --soarpkg flag or the .soarpkg extension.
◆ deluser : Delete USER from the system [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ deluser : Delete USER from the system [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ denv : Run a command in a modified environment [dmon]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ deoptimizer : Machine code de-optimizer to bypass AV. To install it use the --soarpkg flag or the .soarpkg extension.
◆ depmod : Generate a list of module dependencies for the Linux kernel [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ depmod : Generate a list of module dependencies for the Linux kernel [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ depmod : Generate modules.dep and map files [kmod]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ depsdev : CLI client for deps.dev API. To install it use the --soarpkg flag or the .soarpkg extension.
◆ deskey_to_ntlm : Convert deskto to NTLM [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ desktop-file-edit : Modify an existing desktop file entry [desktop-file-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ desktop-file-edit : Modify an existing desktop file entry [desktop-file-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ desktop-file-install : Install a new desktop file entry or rebuild/modify the MIME-types DB [desktop-file-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ desktop-file-validate : Validate & Verify the integrity of a desktop file [desktop-file-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ desktop-file-validate : Validate & Verify the integrity of a desktop file [desktop-file-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ desync : Content-Addressable Data Synchronization Tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ devbox : CLI tool to easily create isolated shells and containers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ devlink : Manage/Monitor network device links [iproute2]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ devmem : Read/write from physical address [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ devmem : Read/write from physical address [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ devpod : Codespaces but open-source, client-only and unopinionated. To install it use the --soarpkg flag or the .soarpkg extension.
◆ df : Print filesystem usage statistics [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ df : Print filesystem usage statistics [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ df : Print filesystem usage statistics [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ df : Report file system space usage [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ df : Report file system space usage [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ df : Report file system space usage [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dgrep : Grep standard input for lines that match EXPRESSION (alias=dategrep) [dateutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dhcprelay : Relay DHCP requests between clients and server [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dhcprelay : Relay DHCP requests between clients and server [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dial : dials a network endpoint, ported from Torgo. To install it use the --soarpkg flag or the .soarpkg extension.
◆ diff : Compare files line by line and output the differences between them [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ diff : Compare files line by line and output the differences between them [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ diff : Compare files line by line and output the differences between them [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ diff3 : Compare files line by line and output the differences between them [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ diffoci : Diff for Docker and OCI container images. To install it use the --soarpkg flag or the .soarpkg extension.
◆ diffutils : Drop-in replacement of diffutils in Rust. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dir : List directory contents [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dir : List directory contents [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dir : List directory contents [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dircolors : Color setup for ls [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dircolors : Color setup for ls [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dircolors : Color setup for ls [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ direnv : Per-directory shell configuration tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dirname : Strip last component from file name [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dirname : Strip last component from file name [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dirname : Strip last component from file name [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dirname : Strip last component from file name [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dirname : Strip non-directory suffix from FILENAME [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dirname : Strip non-directory suffix from FILENAME [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dirname : Strip non-directory suffix from FILENAME [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dish : Monitor Services & Send Alerts to Webhooks/Telegram etc. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dive : A tool for exploring each layer in a Docker image. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dlevel : Filter list of subdomains by level. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dlog : Send lines from standard input to a log file [dmon]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dmesg : Print or control the kernel messaging buffer [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dmesg : Print or control the kernel ring buffer [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dmesg : Print or control the kernel ring buffer [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dmon : Daemonize and monitor Processes [dmon]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dns-bench : Find the fastest DNS in your location to improve internet browsing experience. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dns-doctor : Runs dig +trace and dig +norecurse , parses the output, and tries to diagnose some problems. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dnscrypt-proxy : Flexible DNS proxy, with support for encrypted DNS protocols. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dnsd : Small static DNS server daemon [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dnsd : Small static DNS server daemon [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dnsdomainname : Displays or sets the system's domain name for DNS lookups [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dnsdomainname : Displays or sets the system's domain name for DNS lookups [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dnsdomainname : Show/Set the system's DNS domain name. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dnslookup : Simple command line utility to make DNS lookups to the specified server. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dnspyre : command-line DNS benchmark tool built to stress test and measure the performance of DNS servers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dnstake : Tool to check missing hosted DNS zones. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dnsvalidator : Tool to validate, verify & maintain a list of IPv4 DNS servers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dnsx : Multi-purpose DNS toolkit allow to run multiple DNS queries. To install it use the --soarpkg flag or the .soarpkg extension.
◆ doas : A portable fork of the OpenBSD doas command. To install it use the --soarpkg flag or the .soarpkg extension.
◆ docker-bench : Benchmark Docker Deployment according to Security best practices. To install it use the --soarpkg flag or the .soarpkg extension.
◆ docker-repack : Repack docker images to optimize for pulling speed. To install it use the --soarpkg flag or the .soarpkg extension.
◆ docker-slim : Minify and Secure Docker containers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ docker-slim-sensor : Minify and Secure Docker containers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ doggo : Command-line DNS Client for Humans. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dollarat : Print the positional parameters of an execline script [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dollarat : Print the positional parameters of an execline script [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ domainname : Show/Set the system's NIS/YP domain name. To install it use the --soarpkg flag or the .soarpkg extension.
◆ doomxss : Detect DOM XSS sinks in HTML/JS files [lit-bb-hack-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dos2unix : Convert FILE in-place from DOS to Unix format [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dos2unix : Convert FILE in-place from DOS to Unix format [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dos2unix : DOS to Unix and vice versa text file format (line endings) converter [dos2unix]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dotbins : Manage CLI tool binaries in your dotfiles repository. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dotenv-linter : ⚡️Lightning-fast linter for .env files. Written in Rust 🦀. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dperf : Drive performance measurement tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg : Install, remove and manage Debian packages [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg : Install, remove and manage Debian packages [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg : Package Manager for Debian [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-architecture : Set and Determine the architecture for package building [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-buildapi : Returns the build API level to use during package build [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-buildflags : Return build flags to use during package build [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-buildpackage : Build binary or source packages from sources [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-buildtree : Perform common operations on the build tree for a source package [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-checkbuilddeps : Check build dependencies and conflicts [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-deb : Create, Manipulate, and Extract Debian package files (.deb) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-deb : Create, Manipulate, and Extract Debian package files (.deb) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-deb : Debian package archive (.deb) manipulation tool [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-distaddfile : Add entries to debian/files [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-divert : Override a package's version of a file [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-fsys-usrunmess : Undoes the merged-/usr-via-aliased-dirs mess [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-genbuildinfo : Generate Debian .buildinfo files [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-genchanges : Generate Debian .changes files [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-gencontrol : Generate Debian control files [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-gensymbols : Generate symbols files (shared library dependency information) [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-maintscript-helper : Works around known dpkg limitations in maintainer scripts [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-mergechangelogs : 3-way merge of debian/changelog files [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-name : Rename Debian packages to full package names [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-parsechangelog : Parse Debian changelog files [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-query : A tool to query the dpkg database [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-realpath : Print the resolved pathname with DPKG_ROOT support [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-scanpackages : Create Packages index files [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-scansources : Create Sources index files [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-shlibdeps : Generate shared library substvar dependencies [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-source : Debian source package (.dsc) manipulation tool [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-split : Debian package archive split/join tool [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-statoverride : Override ownership and mode of files [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-trigger : A package trigger utility [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dpkg-vendor : Queries information about distribution vendors [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ drlog : Read lines from stdin and append them to auto-rotated logs [dmon]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dropbear : Lightweight SSH server [dropbear]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dropbear : Lightweight SSH server [dropbear]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dropbearconvert : Convert between Dropbear and OpenSSH private key formats [dropbear]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dropbearconvert : Convert between Dropbear and OpenSSH private key formats [dropbear]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dropbearkey : Create private keys for the use with dropbear/dbclient [dropbear]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dropbearkey : Create private keys for the use with dropbear/dbclient [dropbear]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dropbearmulti : Lightweight Alternative to OpenSSH. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dropbearmulti : Lightweight Alternative to OpenSSH. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dround : Round DATE/TIME to the next occurrence of RNDSPEC (alias=dateround) [dateutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dselect : Debian package management frontend [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dseq : Generate a sequence of date/times from FIRST to LAST or in INCREMENT (alias=dateseq) [dateutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dsieve : Filter and enrich a list of subdomains by level. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dslog : Send lines from standard input to the system log [dmon]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dsort : Sort contents of FILE chronologically (alias=datesort) [dateutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dtest : Like test(1) but for dates (alias=datetest) [dateutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ du : Estimate file space usage [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ du : Estimate file space usage [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ du : Estimate file space usage [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ du : Summarize disk space used for each FILE and/or directory [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ du : Summarize disk space used for each FILE and/or directory [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ du : Summarize disk space used for each FILE and/or directory [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ duf : Disk Usage/Free Utility. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dufs : File server that supports static serving, uploading, searching, etc. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dumbpipe : Unix pipes between devices. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dummyhttp : Super simple HTTP server that replies a fixed body with a fixed response code. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dump.exfat : Userspace utilities for creating and fixing and debugging exFAT filesystem. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dumpe2fs : Dump ext2/ext3/ext4 file system information [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dumpe2fs : Dump ext2/ext3/ext4 file system information [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dumpelf : Dump internal ELF structure [pax-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dumpexfat : Show on-disk information of exfat filesystem. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dumpkmap : Print a binary keyboard translation table to standard output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dumpkmap : Print a binary keyboard translation table to standard output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dumpleases : Display DHCP leases granted by udhcpd [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dumpleases : Display DHCP leases granted by udhcpd [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ duplicut : Quickly dedupe massive wordlists, without changing the order. To install it use the --soarpkg flag or the .soarpkg extension.
◆ duplicut : Quickly dedupe massive wordlists, without changing the order. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dura : A background Daemon process that watches your Git repositories and commits your uncommitted changes. To install it use the --soarpkg flag or the .soarpkg extension.
◆ durl : Remove duplicate URLs by retaining only the unique combinations of hostname, path, and parameter names. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dust : Disk Usage Analyzer. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dvc : A version control system and experiment tracker designed for machine learning projects. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dvc : A version control system and experiment tracker designed for machine learning projects. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dwarfs : Mount highly compressed read-only file system [dwarfs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dwarfs-tools : Single Binary DwarFS Toolset including dwarfs, dwarfsck, dwarfsextract & mkdwarfs [dwarfs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dwarfs2 : Mount highly compressed read-only file system (fuse2) [dwarfs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dwarfsck : Check DwarFS image [dwarfs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dwarfsextract : Extract DwarFS image [dwarfs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dwp : The DWARF packaging utility [bintools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dwp : The DWARF packaging utility [binutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dyff : Diff tool for JSON/YAML files. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dynamic-file-searcher : Scan servers and hosts using dynamic paths. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dysk : Linux utility to get information on filesystems, like df but better. To install it use the --soarpkg flag or the .soarpkg extension.
◆ dzone : Convert DATE/TIMEs between timezones (alias=datezone) [dateutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ e2freefrag : Report free space fragmentation information [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ e2freefrag : Report free space fragmentation information [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ e2fsck : Check a Linux ext2/ext3/ext4 file system [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ e2fsck : Check a Linux ext2/ext3/ext4 file system [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ e2fuzz : Fuzz an ext4 image, for testing purposes [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ e2image : Save critical ext2/ext3/ext4 file system metadata to a file [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ e2image : Save critical ext2/ext3/ext4 file system metadata to a file [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ e2initrd_helper : e2initrd_helper [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ e2label : Change the label on an ext2/ext3/ext4 file system [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ e2mmpstatus : Check MMP status of an ext4 file system [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ e2scrub : Check the contents of a mounted ext[234] file system [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ e2scrub_all : Check all mounted ext[234] file systems for errors [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ e2undo : Replay an undo log for an ext2/ext3/ext4 file system [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ e2undo : Replay an undo log for an ext2/ext3/ext4 file system [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ e4crypt : ext4 file system encryption utility [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ e4crypt : ext4 file system encryption utility [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ e4defrag : Defragmenter for ext4 file system [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ e4defrag : Defragmenter for ext4 file system [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ eae : Extract and sort URL extensions [lit-bb-hack-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ eah : Extract and sort hostnames [lit-bb-hack-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ eap : Extract and sort protocols [lit-bb-hack-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ eaparam : Extract unique URL parameters [lit-bb-hack-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ eapath : Extract unique URLs without queries [lit-bb-hack-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ earh : Extract unique root hostnames [lit-bb-hack-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ecapture : Capture SSL/TLS text content without a CA certificate using eBPF. To install it use the --soarpkg flag or the .soarpkg extension.
◆ echo : Display a line of text [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ echo : Display a line of text [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ echo : Display a line of text [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ echo : Display a line of text [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ echo : Print the specified ARGs to stdout [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ echo : Print the specified ARGs to stdout [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ echo : Print the specified ARGs to stdout [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ecoji : Encodes (and decodes) data as emojis. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ed : A line-oriented text editor [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ed : A line-oriented text editor [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ed : A line-oriented text editor [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ed : Ed with syntax highlighting (ed from u-root but with syntax highlighting). To install it use the --soarpkg flag or the .soarpkg extension.
◆ edgevpn : Immutable, decentralized, statically built p2p VPN. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ee : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ eefjsf : Extract endpoints from JavaScript files [lit-bb-hack-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ eget : Easily install prebuilt binaries from GitHub. To install it use the --soarpkg flag or the .soarpkg extension.
◆ egrep : Search one or more input files for lines containing a match to a specified pattern. To install it use the --soarpkg flag or the .soarpkg extension.
◆ egrep : Search one or more input files for lines containing a match to a specified pattern. To install it use the --soarpkg flag or the .soarpkg extension.
◆ egrep : Searches text using extended regular expressions (EREs). [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ egrep : Searches text using extended regular expressions (EREs). [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ehole : Framework to Fingerprint Assets, Detect Vulnerabilities & Auto Exploitation. To install it use the --soarpkg flag or the .soarpkg extension.
◆ eject : Eject removable media via software control [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ eject : Eject specified DEVICE (or default /dev/cdrom) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ eject : Eject specified DEVICE (or default /dev/cdrom) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ elfcat : ELF visualizer. Generates HTML files from ELF binaries.. To install it use the --soarpkg flag or the .soarpkg extension.
◆ elfedit : Allows alteration of ELF format files [bintools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ elfedit : Allows alteration of ELF format files [binutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ elfexec : Execute ELF binary directly from stdin pipe. To install it use the --soarpkg flag or the .soarpkg extension.
◆ elgetopt : Perform getopt-style parsing on the arguments to an execline script [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ elgetopt : Perform getopt-style parsing on the arguments to an execline script [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ elgetpositionals : Substitute the positional parameters of an execline script [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ elgetpositionals : Substitute the positional parameters of an execline script [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ elglob : Perform globbing on a pattern & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ elglob : Perform globbing on a pattern & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ eltest : Evaluate an expression and indicates the result via its exit status [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ eltest : Evaluate an expression and indicates the result via its exit status [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ elvish : Expressive Programming Language & A Versatile Interactive Shell. To install it use the --soarpkg flag or the .soarpkg extension.
◆ emailfinder : Search emails from a domain through search engines. To install it use the --soarpkg flag or the .soarpkg extension.
◆ emptyenv : Empty/Cleanup the current environment & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ emptyenv : Empty/Cleanup the current environment & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ enc : A modern and friendly CLI alternative to GnuPG. To install it use the --soarpkg flag or the .soarpkg extension.
◆ encrypted-dns : An easy to install, high-performance, zero maintenance proxy to run an encrypted DNS server. To install it use the --soarpkg flag or the .soarpkg extension.
◆ encrypted-dns-server : An easy to install, high-performance, zero maintenance proxy to run an encrypted DNS server. To install it use the --soarpkg flag or the .soarpkg extension.
◆ enola : Hunt down social media accounts by username across social networks. To install it use the --soarpkg flag or the .soarpkg extension.
◆ enscan : OSINT Tool to Aggregate and Export Enterprise Information. To install it use the --soarpkg flag or the .soarpkg extension.
◆ entr : Run arbitrary commands when files change. To install it use the --soarpkg flag or the .soarpkg extension.
◆ enumeraga : Automatic multiprocess tool for a quick enumeration wrapping pentesting tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ enumerepo : List all public repositories for (valid) GitHub usernames. To install it use the --soarpkg flag or the .soarpkg extension.
◆ env : Print the current environment or run PROG after setting up the specified environment [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ env : Print the current environment or run PROG after setting up the specified environment [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ env : Print the current environment or run PROG after setting up the specified environment [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ env : Run a program in a modified environment [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ env : Run a program in a modified environment [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ env : Run a program in a modified environment [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ env : Run a program in a modified environment [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ envdir : Set various environment variables as specified by files in the directory dir and run PROG [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ envdir : Set various environment variables as specified by files in the directory dir and run PROG [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ envfile : Read a file containing variable assignments, add the variables to the environment & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ envfile : Read a file containing variable assignments, add the variables to the environment & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ envsubst : Substitutes Environment variables in shell format strings [gettext]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ envsubst : works like the built-in that many shells have. https://manned.org/man/alpine/envsubst. To install it use the --soarpkg flag or the .soarpkg extension.
◆ envuidgid : et $UID to account's uid and $GID to account's gid and run PROG [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ envuidgid : et $UID to account's uid and $GID to account's gid and run PROG [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ epoch : Easily convert epoch timestamps to human-readable formats and vice versa. To install it use the --soarpkg flag or the .soarpkg extension.
◆ epub2txt : Extract text from EPUB documents. To install it use the --soarpkg flag or the .soarpkg extension.
◆ errno : Look up errno names and descriptions [moreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ esbuild : An extremely fast JavaScript/CSS/TypeScript bundler and minifier. To install it use the --soarpkg flag or the .soarpkg extension.
◆ etcd : The official Go etcd client [etcd]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ etcdctl : Command Line Client for etcd [etcd]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ etcdutl : Command Line Administration utility for etcd [etcd]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ether-wake : Send a magic packet to wake up a computer [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ether-wake : Send a magic packet to wake up a computer [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ex : Vim in Ex mode [vim]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ exatorrent : Self-hostable, easy-to-use, lightweight, and feature-rich torrent client. To install it use the --soarpkg flag or the .soarpkg extension.
◆ exatorrent-noui : Self-hostable, easy-to-use, lightweight, and feature-rich torrent client. To install it use the --soarpkg flag or the .soarpkg extension.
◆ exec : Execute the command line as given [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ exec : Execute the command line as given [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ execline : Non-interactive scripting Tools (Multicall). To install it use the --soarpkg flag or the .soarpkg extension.
◆ execline-cd : Change the current working directory to a given directory & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ execline-cd : Change the current working directory to a given directory & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ execline-umask : Set the umask (file creation mask) & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ execline-umask : Set the umask (file creation mask) & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ execlineb : Read and Execute a script from a file as command line [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ execlineb : Read and Execute a script from a file as command line [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ exfat2img : Userspace utilities for creating and fixing and debugging exFAT filesystem. To install it use the --soarpkg flag or the .soarpkg extension.
◆ exfatattrib : Set or display exFAT file attributes. To install it use the --soarpkg flag or the .soarpkg extension.
◆ exfatfsck : Check an exFAT file system (alias=fsck.exfat). To install it use the --soarpkg flag or the .soarpkg extension.
◆ exfatlabel : Get or Set an exFAT file system label. To install it use the --soarpkg flag or the .soarpkg extension.
◆ exfatlabel : Userspace utilities for creating and fixing and debugging exFAT filesystem. To install it use the --soarpkg flag or the .soarpkg extension.
◆ exif : Read, write, modify, and display EXIF data on the command-line. To install it use the --soarpkg flag or the .soarpkg extension.
◆ exiflooter : Finds geolocation on all image urls and directories. To install it use the --soarpkg flag or the .soarpkg extension.
◆ exit : Exit with a given exit code [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ exit : Exit with a given exit code [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ expand : Convert tabs to spaces [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ expand : Convert tabs to spaces [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ expand : Convert tabs to spaces [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ expand : Convert tabs to spaces [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ expand : Convert tabs to spaces, writing to standard output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ expand : Convert tabs to spaces, writing to standard output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ expand : Convert tabs to spaces, writing to standard output [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ expander : Generate all patterns of a word by splitting, mutating, and reconstructing it with various lengths, offsets, and shifts [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ expiry : Check and enforce password expiration policy [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ export : Set an environment variable to a given value & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ export : Set an environment variable to a given value & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ expr : Evaluate expressions [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ expr : Evaluate expressions [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ expr : Evaluate expressions [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ expr : Evaluate expressions [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ expr : Print the value of EXPRESSION to standard output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ expr : Print the value of EXPRESSION to standard output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ expr : Print the value of EXPRESSION to standard output [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ eza : A modern replacement for ls. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ezuri : Simple Linux ELF Runtime Crypter. To install it use the --soarpkg flag or the .soarpkg extension.
◆ f2 : Command-line tool for batch renaming files and directories. To install it use the --soarpkg flag or the .soarpkg extension.
◆ faac : Open source MPEG-4 and MPEG-2 AAC encoder. To install it use the --soarpkg flag or the .soarpkg extension.
◆ faad : Open source MPEG-4 and MPEG-2 AAC decoder. To install it use the --soarpkg flag or the .soarpkg extension.
◆ factor : Factor a number into its prime factors [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ factor : Factor a number into its prime factors [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ factor : Factor a number into its prime factors [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ factor : Factor numbers [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ factor : Factor numbers [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ factor : Factor numbers [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ factor : Factor numbers [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fadvise : Utility to use the posix_fadvise system call [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ faillog : Display faillog records or set login failure limits [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ faked : A command-line tool for simulating superuser privileges. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fakeidentd : Provide fake ident (auth) service [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fakeidentd : Provide fake ident (auth) service [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fakeroot : A command-line tool for simulating superuser privileges. To install it use the --soarpkg flag or the .soarpkg extension.
◆ faketty : Wrapper to exec a command in a pty, even if redirecting the output. To install it use the --soarpkg flag or the .soarpkg extension.
◆ falconhound : FalconHound is a blue team multi-tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fallocate : Preallocate space to a file [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fallocate : Preallocate space to a file [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fallocate : Preallocate space to a file [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fallocate : Preallocate space to, or deallocate space from a file [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ false : Do nothing, unsuccessfully [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ false : Do nothing, unsuccessfully [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ false : Do nothing, unsuccessfully [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ false : Do nothing, unsuccessfully [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ false : Return an exit code of FALSE (1) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ false : Return an exit code of FALSE (1) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ false : Return an exit code of FALSE (1) [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fanficfare : Tool to make eBooks from stories on fanfiction and other websites. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fastfetch : Fast and feature-rich tool displaying system information. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fastfetch : Fast and feature-rich tool displaying system information. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fatattr : Change attributes on FAT filesystems [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fatattr : Change attributes on FAT filesystems [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ favirecon : Detect technologies, WAF, exposed panels, known services & more based on Favicon Hash. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fblog : Small command-line JSON Log viewer. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fbset : Show and modify frame buffer settings [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fbset : Show and modify frame buffer settings [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fbsplash : Display a splash screen on the framebuffer [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fbsplash : Display a splash screen on the framebuffer [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fc-cache : Build font information cache files [fontconfig]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fc-cat : Read font information cache files [fontconfig]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fc-conflist : List the configuration files processed by Fontconfig [fontconfig]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fc-list : List available fonts [fontconfig]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fc-match : Match available fonts [fontconfig]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fc-pattern : Parse and show pattern [fontconfig]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fc-query : Query font files [fontconfig]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fc-scan : Scan font files or directories [fontconfig]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fc-validate : Validate font files [fontconfig]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fclones : Finds duplicate, unique, under- or over-replicated files. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fd : A simple, fast and user-friendly alternative to find. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fd-find : A simple, fast and user-friendly alternative to find. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fdblock : Set/Unset the O_NONBLOCK flag on a given file descriptor & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fdblock : Set/Unset the O_NONBLOCK flag on a given file descriptor & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fdclose : Close a given file descriptor & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fdclose : Close a given file descriptor & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fdcmp : Compare two file descriptors and run a command based on the result [emanutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fdflush : Force floppy disk drive to detect disk change [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fdflush : Force floppy disk drive to detect disk change [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fdformat : Format floppy disk [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fdformat : Format floppy disk [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fdisk : Change partition table [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fdisk : Change partition table [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fdisk : Manipulate partition table [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fdm : A powerful modern download accelerator and organizer. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fdmove : Move/Copy a given file descriptor & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fdmove : Move/Copy a given file descriptor & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fdreserve : Update the environment with file descriptors that are guaranteed safe to use & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fdreserve : Update the environment with file descriptors that are guaranteed safe to use & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fdseal : Add/Check/Get memfd seals on file descriptors using F_ADD_SEALS and F_GET_SEALS [emanutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fdswap : Swap two file descriptors & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fdswap : Swap two file descriptors & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fdtruncate : Truncate an open file descriptor to a specified length & execute a command [emanutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fdupes : Tool to identify or delete duplicate files. To install it use the --soarpkg flag or the .soarpkg extension.
◆ feroxbuster : Fast, simple, recursive content discovery tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fetch : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ffmpeg : Universal media converter [ffmpeg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ffmpeg : Universal media converter [ffmpeg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ffprobe : Simple multimedia streams analyzer [ffmpeg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ffprobe : Simple multimedia streams analyzer [ffmpeg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ffuf : Fast web fuzzer written in Go. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ffuf-workflow : Automate the process of running FFUF and post-processing its results to extract valid URLs. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ffufpostprocessing : Filter ffuf results. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ffufw : A wrapper around ffuf to make directory bruteforcing easier and more intelligent. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fgconsole : Print the number of the active virtual console [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fgconsole : Print the number of the active virtual console [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fget : Multithread download for a list of files. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fgrep : Search one or more input files for lines containing a match to a specified pattern. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fgrep : Search one or more input files for lines containing a match to a specified pattern. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fgrep : Searches for fixed strings (literal text), ignoring regular expressions [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fgrep : Searches for fixed strings (literal text), ignoring regular expressions [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fico : Extract icons from app packages, executables, images, resource files & more. To install it use the --soarpkg flag or the .soarpkg extension.
◆ filan : Prints information about files/file descriptors meant for debugging socat [soact]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ filebrowser : Standalone Web Application for browsing/managing files and directories. To install it use the --soarpkg flag or the .soarpkg extension.
◆ filecap : See file-based capabilities [libcap-ng]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ filefrag : Report on file fragmentation [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ filefrag : Report on file fragmentation [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fim : File Integrity Monitoring tool to track any event over files. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fin : file info command, from Torgo. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fincore : Count pages of file contents in core [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ find : A toolset for searching and processing files [Rust (uutils)]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ find : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ find : Search for files [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ find : Search for files [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ find : Search for files in a directory hierarchy. To install it use the --soarpkg flag or the .soarpkg extension.
◆ find-suid : Find setuid binaries on a file system with speed. To install it use the --soarpkg flag or the .soarpkg extension.
◆ findfs : Find a filesystem by its label or UUID [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ findfs : Find a filesystem by its label or UUID [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ findfs : Find a filesystem by label or UUID [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ findmnt : Find a (mounted) filesystem: list or search mounted filesystems [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ findomain : The Fastest Subdomain Enumerator. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fingerprintx : Standalone utility for service discovery on open ports. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fish : Smart and User-friendly Command Line Shell. To install it use the --soarpkg flag or the .soarpkg extension.
◆ flac : Encode/Decode the FLAC lossless audio file format. To install it use the --soarpkg flag or the .soarpkg extension.
◆ flashfetch : Fastfetch with minimal features & configuration [fastfetch]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ flashfetch : Fastfetch with minimal features & configuration [fastfetch]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ flawz : A Terminal UI for browsing security vulnerabilities (CVEs). To install it use the --soarpkg flag or the .soarpkg extension.
◆ flock : Utilize serialization using file locks [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ flock : Utilize serialization using file locks [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ flock : Utilize serialization using file locks [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ flock : Utilize serialization using file locks [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ flow6 : Determine the Flow Label generation policy of a target node [ipv6toolkit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ flyscrape : Standalone and Scriptable web scraper. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fmt : Simple optimal text formatter [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fmt : Simple optimal text formatter [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fmt : Simple optimal text formatter [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fmt : Simple optimal text formatter [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fmt : Simple text formatter [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fold : Wrap each input line to fit in specified width [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fold : Wrap each input line to fit in specified width [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fold : Wrap each input line to fit in specified width [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fold : Wrap each input line to fit in specified width [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fold : Wrap input lines in each FILE (standard input by default), writing to standard output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fold : Wrap input lines in each FILE (standard input by default), writing to standard output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fold : Wrap input lines in each FILE (standard input by default), writing to standard output [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ forbacktickx : Run a program and uses its output as loop elements to run another program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ forbacktickx : Run a program and uses its output as loop elements to run another program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ foreground : Execute a sequence of commands [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ foreground : Execute a sequence of commands [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ forkstat : Log process fork(), exec() and exit() activity. To install it use the --soarpkg flag or the .soarpkg extension.
◆ forkstat : Tool to show process fork/exec/exit activity. To install it use the --soarpkg flag or the .soarpkg extension.
◆ forstdin : Use its input as loop elements to run another program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ forstdin : Use its input as loop elements to run another program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fortune : the cookie reader program, it gives you a random quote each time you execute it. To install it use the --soarpkg flag or the .soarpkg extension.
◆ forx : Run a loop [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ forx : Run a loop [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fpart : Sort and pack files into partitions. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fping : Quickly ping N number of hosts to determine their reachability. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fpsync : Sync directories in parallel using fpart. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fq : CLI tool and REPL for working with binary data inspired by jq. To install it use the --soarpkg flag or the .soarpkg extension.
◆ frag6 : Determine the Flow Label generation policy of a target node [ipv6toolkit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ free : Display amount of free and used memory in the system [procps]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ free : Display the amount of free and used system memory [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ free : Display the amount of free and used system memory [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ freeramdisk : Free all memory used by the specified ramdisk [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ freeramdisk : Free all memory used by the specified ramdisk [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ freeze : Tool to generate images of code and terminal output. To install it use the --soarpkg flag or the .soarpkg extension.
◆ freq : Like 'sort. To install it use the --soarpkg flag or the .soarpkg extension.
◆ frpc : Fast Reverse Proxy. To install it use the --soarpkg flag or the .soarpkg extension.
◆ frps : Fast Reverse Proxy. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fscan : A comprehensive intranet scanning tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fsck : Check a Linux ext2/ext3/ext4 file system [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fsck : Check and repair filesystems [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fsck : Check and repair filesystems [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fsck : Perform a file system check and repair [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fsck-minix : Check MINIX filesystem [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fsck-minix : Check MINIX filesystem [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fsck.btrfs : A stub command that does nothing (use btrfs check instead) [btrfs-progs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fsck.cramfs : Check and repair a compressed ROM filesystem [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fsck.exfat : Check an exFAT file system (alias=exfatfsck). To install it use the --soarpkg flag or the .soarpkg extension.
◆ fsck.exfat : Userspace utilities for creating and fixing and debugging exFAT filesystem. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fsck.ext2 : Check a Linux ext2 file system [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fsck.ext3 : Check a Linux ext3 file system [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fsck.ext4 : Check a Linux ext4 file system [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fsck.minix : Check the consistency of a Minix filesystem [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fsck.overlay : Maintenance tools for overlay-filesystem. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fsfreeze : Suspend access to a filesystem [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fsfreeze : Suspend access to a filesystem [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fsfreeze : Suspend access to a filesystem [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fsfreeze : Suspend access to a filesystem [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fsize : A more readable alternative to the stat command. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fsnotifywait : Wait for changes to files using fanotify [inotify-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fsnotifywatch : Gather filesystem access statistics using fanotify [inotify-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fstrim : Discard/trim unused blocks on a filesystem [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fstrim : Discard/trim unused blocks on a filesystem [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fstrim : Discard/trim unused blocks on a filesystem [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fsync : Synchronize a file's in-core state with storage device [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fsync : Synchronize a file's in-core state with storage device [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ftpd : Anonymous FTP server [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ftpd : Anonymous FTP server [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ftpget : Retrieve a remote file via FTP [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ftpget : Retrieve a remote file via FTP [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ftpgrab : Grab your files periodically from a remote FTP or SFTP server easily. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ftpput : Store a local file on a remote machine via FTP [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ftpput : Store a local file on a remote machine via FTP [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fuse-overlayfs : FUSE implementation for overlayfs. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fuse2fs : FUSE file system client for ext2/ext3/ext4 file systems [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fuser : Find processes which use FILEs or PORTs [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fuser : Find processes which use FILEs or PORTs [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fuser : Identify processes using files or sockets [psmisc]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fusermount : A program to mount and unmount FUSE filesystems [fuse]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fusermount3 : A program to mount and unmount FUSE filesystems [fuse3]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fuzzuli : URL fuzzing tool that aims to find critical backup files by creating a dynamic wordlist based on the domain. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fwanalyzer : Tool to Analyze Filesystem Images for Security. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fx : Terminal JSON viewer & processor. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fyne : Cross platform GUI toolkit in Go inspired by Material Design. To install it use the --soarpkg flag or the .soarpkg extension.
◆ fzf : General-purpose command-line fuzzy finder. To install it use the --soarpkg flag or the .soarpkg extension.
◆ galer : Fetch URLs from HTML attributes by crawl-in. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gapplication : D-Bus application launcher [glib]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ garble : Obfuscate Go code by wrapping the Go toolchain. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gate : Split Wordlists into equal sections for workload distribution [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gateau : A simple wrapper to import cookies from browsers for curl, wget and httpie. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gau : Get All URLs (GAU), A Tool to Fetch known URLs from Many Sources. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gawk : A powerful text-processing tool for pattern scanning and performing actions on matched lines [alias=awk]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gcli : Portable Github. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gcore : Generate core files for running processes [gdb]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gdb : The GNU Debugger for analyzing and debugging programs [gdb]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gdb-add-index : Add index files to speed up GDB [gdb]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gdbserver : Remote Server for the GNU Debugger [gdb]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gdu : Fast disk usage calculator with console interface. To install it use the --soarpkg flag or the .soarpkg extension.
◆ geckodriver : Proxy for using WebDriver clients with Gecko-based browsers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gen_crc32ctable : Generate CRC32 Table [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gencat : NLS catalog compiler [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gencmddeps : Generates a command dependency Go file. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gendesc : Generate a test case description file [lcov]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ generate-bidi : Generate Unicode Bidi [wezterm]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ generate-rules : Stand-alone utility to generate random rules [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ genhtml : Generate HTML view from LCOV coverage data files [lcov]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ geninfo : Generate tracefiles from GCOV coverage data files [lcov]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ genl : Generic netlink utility frontend [iproute2]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ genscope : Generate BurpSuite Config from domain list [lit-bb-hack-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gensquashfs : Generate squashfs images [squashfs-tools-ng]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ geodns : Network tools that run from multiple geographic locations using the GeoNet API. To install it use the --soarpkg flag or the .soarpkg extension.
◆ geoip.dat : A proxy server for bypassing network restrictions. To install it use the --soarpkg flag or the .soarpkg extension.
◆ geoip.dat : The best v2ray-core, with XTLS support. To install it use the --soarpkg flag or the .soarpkg extension.
◆ geoping : Network tools that run from multiple geographic locations using the GeoNet API. To install it use the --soarpkg flag or the .soarpkg extension.
◆ geosite.dat : A proxy server for bypassing network restrictions. To install it use the --soarpkg flag or the .soarpkg extension.
◆ geosite.dat : The best v2ray-core, with XTLS support. To install it use the --soarpkg flag or the .soarpkg extension.
◆ getcap : Display the capabilities on the queried file(s) [libcap]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ getcwd : Store its current working directory into a given environment variable & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ getcwd : Store its current working directory into a given environment variable & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ getfattr : Get extended attributes of a file or directory [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ getfattr : Get extended attributes of a file or directory [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ getghrel : CLI tool to locate and download the latest release assets from GitHub. To install it use the --soarpkg flag or the .soarpkg extension.
◆ getjs : Download & Extract JavaScript sources from URLs and web pages/HTTP responses. To install it use the --soarpkg flag or the .soarpkg extension.
◆ getopt : Parse command options [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ getopt : Parse command options [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ getopt : Parse command options [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ getopt : Parse command options [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ getpcaps : Display process capabilities [libcap]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ getpid : Store its process ID in a given environment variable & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ getpid : Store its process ID in a given environment variable & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ getsubids : Get the subordinate id ranges for a user [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gettext : Retrieve text string from message database [gettext]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gettext.sh : Gettext as a shell function library [gettext]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gettextize : Prepares a source package to use gettext [gettext]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ getty : Open a tty, prompt for a login name, then invoke /bin/login [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ getty : Open a tty, prompt for a login name, then invoke /bin/login [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gf : A wrapper around grep (Improved). To install it use the --soarpkg flag or the .soarpkg extension.
◆ gf : A wrapper around grep. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gh : GitHub CLI tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gh : GitHub CLI tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gh-dash : A beautiful CLI dashboard for GitHub. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ghalint : GitHub Actions linter. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ghauri : Tool for detecting and exploiting SQL injection security flaws. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ghcp : Tool to fork a repository, commit files, create a pull request and upload assets using GitHub API. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ghfetch : CLI tool to fetch GitHub user information and show like neofetch. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ghorg : Quickly clone or backup an entire org/users repositories into one directory. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ghostunnel : SSL/TLS proxy with mutual authentication for securing non-TLS services. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gi-compile-repository : Typelib compiler [glib]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gi-decompile-typelib : Typelib decompiler [glib]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gi-inspect-typelib : Typelib inspection tool [glib]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gickup : Tool to backup repositories. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ginstall : Copy files and set attributes [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gio : GIO (Gnome Input/Output) commandline tool [glib]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gio-launch-desktop : GDesktopAppInfo helper to set GIO_LAUNCHED_DESKTOP_FILE_PID [glib]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gio-querymodules : GIO module cache creation [glib]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gip : A command-line tool to get Global IP address. To install it use the --soarpkg flag or the .soarpkg extension.
◆ git-cliff : Changelog generator based on conventional commits. To install it use the --soarpkg flag or the .soarpkg extension.
◆ git-dumper : A tool to dump a git repository from a website. To install it use the --soarpkg flag or the .soarpkg extension.
◆ git-lfs : Git extension for versioning large files. To install it use the --soarpkg flag or the .soarpkg extension.
◆ git-sizer : Compute various size metrics for a Git repository. To install it use the --soarpkg flag or the .soarpkg extension.
◆ git-who : Git blame for file trees. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gitdorks_go : An automated collection tool for discovering sensitive information on GitHub. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gitea : A painless self-hosted Git service. To install it use the --soarpkg flag or the .soarpkg extension.
◆ github-comment : CLI to create and hide GitHub comments. To install it use the --soarpkg flag or the .soarpkg extension.
◆ github-endpoints : Find endpoints on GitHub of a given domain. To install it use the --soarpkg flag or the .soarpkg extension.
◆ github-regexp : Basically a regexp over a GitHub search. To install it use the --soarpkg flag or the .soarpkg extension.
◆ github-subdomains : Find subdomains on GitHub. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gitlab-cli : Official Gitlab CLI. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gitlab-subdomains : Find subdomains on Gitlab. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gitleaks : Scan git repos (or files) for secrets using regex and entropy. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gitql : A git query language. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gitty : Download GitHub File & Directory. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gitui : Blazing fast terminal-ui for git written in rust. To install it use the --soarpkg flag or the .soarpkg extension.
◆ glab : Official Gitlab CLI. To install it use the --soarpkg flag or the .soarpkg extension.
◆ glib-compile-resources : GLib resource compiler [glib]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ glib-compile-schemas : GSettings schema compiler [glib]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ glider : Forward proxy with multiple protocols support & dns/dhcp server with like dnsmasq. To install it use the --soarpkg flag or the .soarpkg extension.
◆ globalping-cli : CLI tool for benchmarking, monitoring, and optimizing internet infrastructure using a global network of probes. To install it use the --soarpkg flag or the .soarpkg extension.
◆ glow : Render markdown on the CLI. To install it use the --soarpkg flag or the .soarpkg extension.
◆ go-audit : An alternative to the auditd daemon that ships with many distros. To install it use the --soarpkg flag or the .soarpkg extension.
◆ go-fasttld : A high performance effective top level domains (eTLD) extraction module. To install it use the --soarpkg flag or the .soarpkg extension.
◆ goanywhere : Creates a Go workspace & executes command after '--'. To install it use the --soarpkg flag or the .soarpkg extension.
◆ goawk : A POSIX-compliant AWK interpreter written in Go, with CSV support. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gobject-query : Display a tree of types [glib]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gobuster : A Directory/File, DNS and VHost busting tool (Dev). To install it use the --soarpkg flag or the .soarpkg extension.
◆ gobuster : A Directory/File, DNS and VHost busting tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gobusybox : Core u-root cmds into a Single Busybox like Multicall Binary. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gocryptfs : Encrypted overlay filesystem written in Go. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gocryptfs-xray : Encrypted overlay filesystem written in Go. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gocurl : Simplified version of curl written in pure Go. To install it use the --soarpkg flag or the .soarpkg extension.
◆ godns : Dynamic DNS client tool supports AliDNS, Cloudflare, Google Domains, DNSPod, HE.net & DuckDNS & DreamHost, etc. To install it use the --soarpkg flag or the .soarpkg extension.
◆ godnsbench : Simple DNS bench util that supports encrypted protocols. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gofireprox : FireProx written in Go. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gogo : Highly controllable and Scalable Automation Engine for Red Teams. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gojq : Pure Go implementation of jq with yaml support. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gokr-rsync : Go Implementation of rsync Client & Server. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gokr-rsyncd : Go Implementation of rsync Client & Server. To install it use the --soarpkg flag or the .soarpkg extension.
◆ goldboot : Immutable infrastructure for the desktop. To install it use the --soarpkg flag or the .soarpkg extension.
◆ goldboot-registry : Immutable infrastructure for the desktop. To install it use the --soarpkg flag or the .soarpkg extension.
◆ golin : Automatically Detect weak passwords, Scan for vulnerabilities, and more.. To install it use the --soarpkg flag or the .soarpkg extension.
◆ goodls : This is a CLI tool to download shared files and folders from Google Drive. To install it use the --soarpkg flag or the .soarpkg extension.
◆ goop : Dump a git repository from a website, focused on as-complete-as-possible dumps and handling weird edge-cases. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gopass : The slightly more awesome standard unix password manager for teams. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gorilla : Tool for generating wordlists or extending an existing one using mutations. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gosearch : OSINT tool designed for uncovering digital footprints associated with a given username. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gosec : Go source code static analyzer, focusing on security. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gosmee : Command line server and client for webhooks deliveries. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gospider : Fast web spider written in Go. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gost : A simple tunnel written in golang. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gost : GO Simple Tunnel - a simple tunnel written in golang. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gotator : Generate DNS wordlists through permutations. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gotty : Share your terminal as a web application. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gowitness : A commandline web screenshot and information gathering tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gpasswd : Administer /etc/group and /etc/gshadow files [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gprof : Displays profiling information [bintools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gprof : Displays profiling information [binutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gptme : AI assistant in your terminal. To install it use the --soarpkg flag or the .soarpkg extension.
◆ graftcp : Redirect a given program's TCP traffic to SOCKS5 or HTTP proxy without using the LD_PRELOAD trick. To install it use the --soarpkg flag or the .soarpkg extension.
◆ graftcp-local : Redirect a given program's TCP traffic to SOCKS5 or HTTP proxy without using the LD_PRELOAD trick. To install it use the --soarpkg flag or the .soarpkg extension.
◆ grep : Search for PATTERN in each FILE or standard input [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ grep : Search for PATTERN in each FILE or standard input [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ grep : Search for PATTERN in each FILE or standard input [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ grep : Search one or more input files for lines containing a match to a specified pattern. To install it use the --soarpkg flag or the .soarpkg extension.
◆ grep : Search one or more input files for lines containing a match to a specified pattern. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gresource : GResource tool [glib]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ grex : A command-line tool for generating regular expressions from user-provided input strings. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gron : Transform JSON to make it easier to grep. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gronx : Cron Expression parser with task runner and daemon that supports crontab like task list file. To install it use the --soarpkg flag or the .soarpkg extension.
◆ groupadd : Create a new group [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ groupdel : Delete a group [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ groupmems : Administer members of a user's primary group [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ groupmod : Modify a group definition on the system [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ groups : Print the groups a user is in [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ groups : Print the groups a user is in [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ groups : Print the groups a user is in [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ groups : Print the groups a user is in [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ groups : Print the groups a user is in [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ groups : Print the groups a user is in [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ grpck : Verify integrity of group files [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ grpconv : Creates gshadow from group and gshadow [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ grpcurl : Command-line tool for interacting with gRPC servers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ grpunconv : Create group from group and gshadow and then removes gshadow [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gs-netcat : Netcat on steroids [gsocket]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gsan : Extract subdomains from SSL certificates in HTTPS sites. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gsettings : GSettings configuration tool [glib]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gsocket : Connect two users behind NAT/firewall via TCP/IP [gsocket]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gstring : Swiss army knife for manipulating strings. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gtester : Test running utility [glib]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gtts-cli : Create an mp3 file from spoken text via the Google TTS API. To install it use the --soarpkg flag or the .soarpkg extension.
◆ guarddog : CLI tool to Identify malicious PyPI and npm packages. To install it use the --soarpkg flag or the .soarpkg extension.
◆ guild : Runn scripts bundled with Guile [guile]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ guile : The GNU Project Extension Language [guile]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ guile-snarf : Extract subr info from distributed C files [guile]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gum : A tool for creating minimal interactive TUIs for shell scripts. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gungnir : Monitor certificate transparency (CT) logs for newly issued SSL/TLS certificates. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gunzip : Decompresses files, removing extensions like .gz, .z, .tgz, and .taz [gzip]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gunzip : Uncompress FILEs (or standard input) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gunzip : Uncompress FILEs (or standard input) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gup : Update binaries installed by go install with goroutines. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gxss : A tool to check a bunch of URLs that contain reflecting params. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gzexe : Compress executable files and makes them self-extracting [gzip]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gzip : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gzip : Compress FILEs (or standard input) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gzip : Compress FILEs (or standard input) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ gzip : Compress/Decompress files files to & from .gz format [gzip]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hack-browser-data : Decrypt and Export Browser Data (passwords, history, cookies, bookmarks etc). To install it use the --soarpkg flag or the .soarpkg extension.
◆ hacker-scoper : Automagically filter URLs with Bug Bounty program scope rules scraped from the internet. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hadolint : Smarter Dockerfile linter to validate best practices. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hakip2host : Convert IP Addresses to Associated Domain Names. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hakoniwa : Process isolation for Linux using namespaces, resource limits, landlock and seccomp. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hakoriginfinder : Tool for discovering the origin host behind a reverse proxy. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hakrawler : Simple, fast web crawler designed for easy, quick discovery of endpoints and assets within a web application. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hakrevdns : Small, fast tool for performing reverse DNS lookups en masse. To install it use the --soarpkg flag or the .soarpkg extension.
◆ haktrailsfree : Scrape securitytrails using cookie without apikey. To install it use the --soarpkg flag or the .soarpkg extension.
◆ halt : Shuts down or reboots the system immediately [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ halt : Shuts down or reboots the system immediately [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ handlr : Powerful alternative to xdg-utils, with regex support. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hardlink : Consolidate duplicate files using hardlinks [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hashsum : Compute and check message digests for many Algorithms. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hcloud : A command-line interface for Hetzner Cloud. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hcstat2gen : Generate custom Markov statistics, for use with hashcat's --markov-hcstat2 parameter [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hcstatgen : Generate .hcstat files for use with older hashcat's markov-hcstat parameter/statsprocessor [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hcxdumptool : Small tool to capture packets from wlan devices. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hcxeiutool : Prepare -E -I -U output of hcxpcapngtool for use by hashcat + rule or JtR + rule [hcxtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hcxhash2cap : Converts hash file (PMKID&EAPOL, PMKID, EAPOL-hccapx, EAPOL-hccap, WPAPSK-john) to cap [hcxtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hcxhashtool : Provide various filter operations on new PMKID/EAPOL hash line [hcxtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hcxpcapngtool : Convert raw capture files to Hashcat and JtR readable formats [hcxtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hcxpmktool : Calculate and verify a PSK and/or a PMK [hcxtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hcxpsktool : Calculate candidates for hashcat and john [hcxtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hcxwltool : Calculate candidates for hashcat and john based on mixed wordlists [hcxtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hd : An alias for hexdump -C [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hd : An alias for hexdump -C [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hdiffz : Fast Binary diff/patch tools with small deltas/differentials and low memory use. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hdiffz : Fast Binary diff/patch tools with small deltas/differentials and low memory use. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hdiffz : Fast Binary diff/patch tools with small deltas/differentials and low memory use. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hdparm : Utility to set and view hardware parameters of hard disk drives [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hdparm : Utility to set and view hardware parameters of hard disk drives [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ heacoll : Collect unique HTTP headers [lit-bb-hack-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ head : Output the first part of files [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ head : Output the first part of files [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ head : Output the first part of files [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ head : Output the first part of files [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ head : Print first N lines of each FILE to standard output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ head : Print first N lines of each FILE to standard output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ head : Print first N lines of each FILE to standard output [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ headless-terminal : Headless Terminal (ht) - wrap any binary with a terminal interface for easy programmatic access. To install it use the --soarpkg flag or the .soarpkg extension.
◆ headtail : head and tail simultaneously. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hearth : Linux server monitoring via SSH. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hednsextractor : Raw html extractor from Hurricane Electric portal. To install it use the --soarpkg flag or the .soarpkg extension.
◆ herd : Massively parallel ssh client. To install it use the --soarpkg flag or the .soarpkg extension.
◆ heredoc : Run a command with a certain string fed to a file descriptor [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ heredoc : Run a command with a certain string fed to a file descriptor [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hermit : Manage isolated, self-bootstrapping sets of tools in software projects. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hex : 🔮 Futuristic take on hexdump, made in Rust.. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hexdump : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hexdump : Display file contents in hexadecimal, decimal, octal, or ASCII [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hexdump : Display file(s) or standard input in a user specified format [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hexdump : Display file(s) or standard input in a user specified format [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hexedit : View and edit files in hexadecimal [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hexedit : View and edit files in hexadecimal [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hexlog : Hexdump stdin and/or stdout to stderr. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hexyl : A command-line hex viewer. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hide-pid : Hide any PID/Directory in the Linux kernel. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hl : Log viewer/processor that transforms logs in JSON/logfmt formats into a human-readable output. To install it use the --soarpkg flag or the .soarpkg extension.
◆ homeof : Print the home directory of a user [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ homeof : Print the home directory of a user [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ horust : Horust is a supervisor/init system written in rust and designed to run inside containers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hostctl : CLI tool to manage the /etc/hosts file. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hostid : Print out a unique 32-bit identifier for the machine [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hostid : Print out a unique 32-bit identifier for the machine [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hostid : Print out a unique 32-bit identifier for the machine [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hostid : Print the numeric identifier for the current host [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hostid : Print the numeric identifier for the current host [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hostid : Print the numeric identifier for the current host [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hostid : Print the numeric identifier for the current host [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hostname : Display or set the system's host name [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hostname : Display or set the system's host name [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hostname : Get or set hostname or DNS domain name [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hostname : Get or set hostname or DNS domain name [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hostname : Get or set hostname or DNS domain name [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hostname : Show/Set the system's host name. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hpatchz : Fast Binary diff/patch tools with small deltas/differentials and low memory use. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hpatchz : Fast Binary diff/patch tools with small deltas/differentials and low memory use. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hpatchz : Fast Binary diff/patch tools with small deltas/differentials and low memory use. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hpwd : an implementation of pwd that replaces your $HOME with a prefix such as ~ or something else. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hrekt : A really fast HTTP Prober. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hsync_demo : Multi-Threaded Delta update using HDiffPatch over HTTP(s) with support for large files, directories, zstd & More. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hsync_http : Multi-Threaded Delta update using HDiffPatch over HTTP(s) with support for large files, directories, zstd & More. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hsync_make : Multi-Threaded Delta update using HDiffPatch over HTTP(s) with support for large files, directories, zstd & More. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ht : Headless Terminal (ht) - wrap any binary with a terminal interface for easy programmatic access. To install it use the --soarpkg flag or the .soarpkg extension.
◆ htb-cli : Interact with Hackthebox using your terminal. To install it use the --soarpkg flag or the .soarpkg extension.
◆ htmlq : Parse and extract HTML from the command line. To install it use the --soarpkg flag or the .soarpkg extension.
◆ htop : Interactive text-mode process viewer. To install it use the --soarpkg flag or the .soarpkg extension.
◆ httpd : Listen for incoming HTTP requests [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ httpd : Listen for incoming HTTP requests [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ httpd-asm : Single-Page HTTP Server that doesn't depend on libc (Assembly Version). To install it use the --soarpkg flag or the .soarpkg extension.
◆ httpd-c : Single-Page HTTP Server that doesn't depend on libc (C Version). To install it use the --soarpkg flag or the .soarpkg extension.
◆ httpie : Modern, user-friendly command-line HTTP client for the API era. To install it use the --soarpkg flag or the .soarpkg extension.
◆ httprobe : Probing tool for working HTTP and HTTPS servers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ httpstat : Visualize cURL statistics. To install it use the --soarpkg flag or the .soarpkg extension.
◆ httptap : View HTTP/HTTPS requests made by any Linux program. To install it use the --soarpkg flag or the .soarpkg extension.
◆ httpx : Fast and multi-purpose HTTP toolkit. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hub : A command-line tool that makes git easier to use with GitHub. To install it use the --soarpkg flag or the .soarpkg extension.
◆ huggingface-cli : The official Python client for the Huggingface Hub. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hugo : Fast and Flexible Static Site Generator. To install it use the --soarpkg flag or the .soarpkg extension.
◆ huniq : Filter out duplicates on the command line. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hurl : CLI to run HTTP requests defined in simple plain text format. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hurlfmt : CLI to run HTTP requests defined in simple plain text format. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hush : A minimal, lightweight shell for Unix-like systems [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hush : A minimal, lightweight shell for Unix-like systems [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hwatch : A modern alternative to the watch command. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hwclock : Query and set hardware clock (RTC) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hwclock : Query and set hardware clock (RTC) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hwclock : Time clocks utility [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hwinfo : Hardware information tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hx : 🔮 Futuristic take on hexdump, made in Rust.. To install it use the --soarpkg flag or the .soarpkg extension.
◆ hyperfine : A command-line benchmarking tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ i2cdetect : Detect I2C devices on an I2C bus [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ i2cdetect : Detect I2C devices on an I2C bus [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ i2cdump : Dump I2C registers [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ i2cdump : Dump I2C registers [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ i2cget : Read from I2C device registers [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ i2cget : Read from I2C device registers [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ i2cset : Write to I2C device registers [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ i2cset : Write to I2C device registers [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ i2ctransfer : Perform I2C block transfers [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ i2ctransfer : Perform I2C block transfers [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ i2pd : Router for an end-to-end encrypted and anonymous internet. To install it use the --soarpkg flag or the .soarpkg extension.
◆ i386 : Change reported architecture to i386 [alias=setarch i386] [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ iamb : A Matrix client for Vim addicts. To install it use the --soarpkg flag or the .soarpkg extension.
◆ icmp6 : Exploit potential flaws in the processing of ICMPv6 Error messages [ipv6toolkit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ id : Print information about USER or the current user [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ id : Print information about USER or the current user [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ id : Print information about USER or the current user [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ id : Print real and effective user and group IDs [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ id : Print real and effective user and group IDs [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ id : Print real and effective user and group IDs [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ id : Print real and effective user and group IDs [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ if : Perform conditional execution [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ if : Perform conditional execution [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ifconfig : Configure a network interface [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ifconfig : Configure a network interface [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ifconfig : Configure a network interface. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ifdata : Get network interface info without parsing ifconfig output [moreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ifdown : Bring a network interface down [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ifdown : Bring a network interface down [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ifelse : Perform conditional execution, with two branches [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ifelse : Perform conditional execution, with two branches [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ifenslave : Configure network interfaces for parallel routing [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ifenslave : Configure network interfaces for parallel routing [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ifne : Run command if the standard input is not empty [moreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ifplugd : Network interface plug detection daemon [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ifplugd : Network interface plug detection daemon [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ifstat : Handy utility to read network interface statistics [iproute2]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ifte : Perform a conditional alternative [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ifte : Perform a conditional alternative [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ifthenelse : Perform a conditional alternative [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ifthenelse : Perform a conditional alternative [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ifup : Bring a network interface up [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ifup : Bring a network interface up [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ image-bootstrap : Generate Bootable Virtual Machine Images and write them to a Block Device. To install it use the --soarpkg flag or the .soarpkg extension.
◆ imgcat : Display images and gifs in your terminal. To install it use the --soarpkg flag or the .soarpkg extension.
◆ importas : Replace a literal with the value of an environment variable & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ importas : Replace a literal with the value of an environment variable & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ importenv : Launch an executable file with environment variables from a specific process id. To install it use the --soarpkg flag or the .soarpkg extension.
◆ importenv : lets you access variables defined in the namespace/scope of another process. To install it use the --soarpkg flag or the .soarpkg extension.
◆ indextree : Generates the tree of the directory listing page. To install it use the --soarpkg flag or the .soarpkg extension.
◆ inetd : Listen for network connections and launch programs [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ inetd : Listen for network connections and launch programs [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ini-file : CLI tool for modifying .ini files. To install it use the --soarpkg flag or the .soarpkg extension.
◆ init : Init is the parent of all processes [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ init : Init is the parent of all processes [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ inotifywait : Wait for changes to files using inotify [inotify-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ inotifywatch : Gather filesystem access statistics using inotify [inotify-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ inscope : Filter in scope and out of scope urls (subdomains) from stdin. To install it use the --soarpkg flag or the .soarpkg extension.
◆ insmod : Insert a module into the Linux Kernel [kmod]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ insmod : Load the specified kernel modules into the kernel [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ insmod : Load the specified kernel modules into the kernel [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ install : Copy files and set attributes [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ install : Copy files and set attributes [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ install : Copy files and set attributes [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ integritysetup : Tool to setup and manage integrity protected disk volumes [cryptsetup]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ interactsh-client : An Out Of Bounds (OOB) interaction gathering server and client tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ interactsh-server : An Out Of Bounds (OOB) interaction gathering server and client tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ intercept : Redirect device input events to stdout [interception-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ interlace : Easily turn single threaded CLIs into a fast, multi-threaded application with CIDR and glob support. To install it use the --soarpkg flag or the .soarpkg extension.
◆ intltool-merge : Merge translated strings into various types of file [intltool]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ intltool-prepare : Prepare software to make use of intltool [intltool]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ intltoolize : Copy intltool related files to software package [intltool]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ invidtui : A TUI based Invidious client. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ionice : Change I/O scheduling class and priority [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ionice : Change I/O scheduling class and priority [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ionice : Change I/O scheduling class and priority [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ionice : Show or change the I/O-scheduling class and priority of a process [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ioping : Simple disk I/O latency monitoring tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ iostat : Report CPU and I/O statistics [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ iostat : Report CPU and I/O statistics [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ iotop : A top-like utility for checking I/O load. To install it use the --soarpkg flag or the .soarpkg extension.
◆ iotop-c : A top-like utility for checking I/O load. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ip : Manage network interfaces, routing, and network devices [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ip : Manage network interfaces, routing, and network devices [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ip : Show/Manipulate routing, network devices, interfaces and tunnels [iproute2]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ip2net : Group ipv4/ipv6 list from stdout into subnets [zapret]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ip_compact : Compact a list of IP prefixes. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ip_diff : Compare two lists of IP prefixes. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ip_match : Match IP addresses against a list of prefixes. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ipa-server : Upload and install Apple ipa and Android apk in web. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ipaddr : Display or Manipulate IP address assignments on network interfaces [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ipaddr : Display or Manipulate IP address assignments on network interfaces [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ipasd : Upload and install Apple ipa and Android apk in web. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ipcalc : Calculate IP network settings from a IP address [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ipcalc : Calculate IP network settings from a IP address [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ipcmk : Create various IPC resources [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ipcrm : Remove certain IPC resources [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ipcrm : Upper-case options MQS remove an object by shmkey value [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ipcrm : Upper-case options MQS remove an object by shmkey value [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ipcs : Show information about IPC mechanisms [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ipcs : Show information about IPC mechanisms [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ipcs : Show information on IPC facilities [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ iperf : A TCP, UDP, and SCTP network bandwidth measurement tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ iperf : A tool for network performance measurement and tuning. To install it use the --soarpkg flag or the .soarpkg extension.
◆ iperf : Tool to measure maximum TCP and UDP bandwidth. To install it use the --soarpkg flag or the .soarpkg extension.
◆ iperf3 : A TCP, UDP, and SCTP network bandwidth measurement tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ iperf3 : A tool for network performance measurement and tuning. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ipfswatch : Tool to Monitor a directory and adds changes to IPFS. To install it use the --soarpkg flag or the .soarpkg extension.
◆ iphandle : Automatically remove HoneyPot IPs during Port Scan. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ipinfo : Official Command Line Interface for the IPinfo API. To install it use the --soarpkg flag or the .soarpkg extension.
◆ iplink : Manage network devices (e.g., enable/disable interfaces) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ iplink : Manage network devices (e.g., enable/disable interfaces) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ipneigh : Manage neighbor cache (ARP entries) in the IP network [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ipneigh : Manage neighbor cache (ARP entries) in the IP network [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ iproute : Manage routing tables and IP routes in the Linux kernel [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ iproute : Manage routing tables and IP routes in the Linux kernel [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ iprule : Manage routing policy rules in Linux [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ iprule : Manage routing policy rules in Linux [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ipspinner : Proxy to redirect all incoming requests through different chosen providers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ iptunnel : Manages network tunnels [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ iptunnel : Manages network tunnels [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ iroh-bulk : Multi CMD Toolkit for Iroh, peer-2-peer that just works. To install it use the --soarpkg flag or the .soarpkg extension.
◆ iroh-dns-server : Pkarr relay and DNS server for Iroh. To install it use the --soarpkg flag or the .soarpkg extension.
◆ irqtop : Interactive utility to display kernel interrupt information [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ isainfo : like the SunOS command, it will show info about your architecture, capabilities, bits, etc.. To install it use the --soarpkg flag or the .soarpkg extension.
◆ isd : TUI to interactively work with systemd units. To install it use the --soarpkg flag or the .soarpkg extension.
◆ isort : Sort Python imports automatically. To install it use the --soarpkg flag or the .soarpkg extension.
◆ isosize : Output the length of an iso9660 filesystem in a specified file [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ isosize : Show the length of an ISO-9660 filesystem [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ issue : reads an 'issue' file (/etc/issue or a user-suplied one). To install it use the --soarpkg flag or the .soarpkg extension.
◆ isutf8 : Check whether files are valid UTF-8 [moreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ iwasm : CLI for WebAssembly Micro Runtime (WAMR). To install it use the --soarpkg flag or the .soarpkg extension.
◆ jc : Convert the output of command-line tools to JSON. To install it use the --soarpkg flag or the .soarpkg extension.
◆ jd : JSON diff and patch. To install it use the --soarpkg flag or the .soarpkg extension.
◆ jen : A simple CLI generation tool for creating large datasets. To install it use the --soarpkg flag or the .soarpkg extension.
◆ jira-cli : Feature-rich interactive Jira command line. To install it use the --soarpkg flag or the .soarpkg extension.
◆ jj : Jujutsu is a Git-compatible DVCS that is both simple and powerful. To install it use the --soarpkg flag or the .soarpkg extension.
◆ jless : Command-line pager for JSON data. To install it use the --soarpkg flag or the .soarpkg extension.
◆ jnv : interactive JSON filter using jq. To install it use the --soarpkg flag or the .soarpkg extension.
◆ jobs : A tool to put running programs into background/foreground & provide Job Control to any Shell. To install it use the --soarpkg flag or the .soarpkg extension.
◆ join : Join lines of two files on a common field [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ join : Join lines of two files on a common field [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ join : Join lines of two files on a common field [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ join : Relational database operator [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ joker : Turn processes into daemon with Zero-Configuration. To install it use the --soarpkg flag or the .soarpkg extension.
◆ jot : Print sequential or random data [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ jot : Print sequential or random data [outils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ jpeg2yuv : Convert jpeg images to the yuv format [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ jq : Lightweight and flexible command-line JSON processor. To install it use the --soarpkg flag or the .soarpkg extension.
◆ jq : Lightweight and flexible command-line JSON processor. To install it use the --soarpkg flag or the .soarpkg extension.
◆ jqp : TUI playground to experiment with jq. To install it use the --soarpkg flag or the .soarpkg extension.
◆ jrnl : Command line journal application that stores your journal in a plain text file. To install it use the --soarpkg flag or the .soarpkg extension.
◆ jsdif : JavaScript monitoring tool for bug bounty hunters. To install it use the --soarpkg flag or the .soarpkg extension.
◆ jsluice : Extract URLs, paths, secrets, and other interesting bits from JavaScript. To install it use the --soarpkg flag or the .soarpkg extension.
◆ json-cleaner : Clean up JSON Output generated by popular bug-bounty tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ jujutsu : Jujutsu is a Git-compatible DVCS that is both simple and powerful. To install it use the --soarpkg flag or the .soarpkg extension.
◆ jumbo6 : Exploit potential flaws in the processing of IPv6 Jumbo payloads [ipv6toolkit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ jwt-hack : A tool for hacking/security testing to JWT. To install it use the --soarpkg flag or the .soarpkg extension.
◆ jyt : A tridirectional converter between Json, Yaml, and Toml. To install it use the --soarpkg flag or the .soarpkg extension.
◆ k9s : Terminal UI to interact with Kubernetes clusters. To install it use the --soarpkg flag or the .soarpkg extension.
◆ kalc : A Complex numbers, 2d/3d Graphing, Arbitrary Precision, Vector/Matrix, cli Calculator with real-time output and support for units. To install it use the --soarpkg flag or the .soarpkg extension.
◆ kalker : Calculator with Math syntax that supports user-defined variables and functions, Complex numbers, and estimation of Derivatives and Integrals. To install it use the --soarpkg flag or the .soarpkg extension.
◆ katana : Crawling and spidering framework. To install it use the --soarpkg flag or the .soarpkg extension.
◆ kbd-mode : Report or set the keyboard mode [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ kbd-mode : Report or set the keyboard mode [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ kcptun-client : Stable & Secure Tunnel based on KCP with N:M multiplexing and FEC. To install it use the --soarpkg flag or the .soarpkg extension.
◆ kcptun-server : Stable & Secure Tunnel based on KCP with N:M multiplexing and FEC. To install it use the --soarpkg flag or the .soarpkg extension.
◆ keep-sorted : Language-agnostic Formatter that sorts lines between two markers in a larger file. To install it use the --soarpkg flag or the .soarpkg extension.
◆ keyspace : Calculate keyspace in a hashcat-aware manner [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ kill : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ kill : Send a signal (default: TERM) to given PIDs [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ kill : Send a signal (default: TERM) to given PIDs [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ kill : Send a signal (default: TERM) to given PIDs [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ kill : Send a signal (default: TERM) to given PIDs [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ kill : Send a signal (default: TERM) to given PIDs [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ kill : Send a signal to a process [procps]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ kill : Send a signal to a process [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ killall : Kill processes by name, e.g. killall -HUP named [psmisc]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ killall : Send a signal (default: TERM) to given processes [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ killall : Send a signal (default: TERM) to given processes [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ killall5 : Send a signal (default: TERM) to all processes outside current session [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ killall5 : Send a signal (default: TERM) to all processes outside current session [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ killport : A command-line tool to easily kill processes running on a specified port. To install it use the --soarpkg flag or the .soarpkg extension.
◆ klogd : Kernel logger [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ klogd : Kernel logger [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ klp : CLI viewer for structured log files and streams (logfmt, JSONL and many others). To install it use the --soarpkg flag or the .soarpkg extension.
◆ kmod : Manage Linux Kernel modules [kmod]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ kmon : Linux Kernel Manager and Activity Monitor 🐧💻. To install it use the --soarpkg flag or the .soarpkg extension.
◆ knoxssme : Test URLs using Knoxss.me API [lit-bb-hack-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ kondo : Cleans dependencies and build artifacts from your projects. To install it use the --soarpkg flag or the .soarpkg extension.
◆ kopia : Fast and secure open-source backup/restore tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ksubdomain : Subdomain enumeration tool, asynchronous dns packets, use pcap to scan 1600,000 subdomains in 1 second. To install it use the --soarpkg flag or the .soarpkg extension.
◆ kubemetrics : Extract paths from Kubernetes metrics endpoints [lit-bb-hack-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ kubo : Official Inter Platnetary File System (IPFS) CLI. To install it use the --soarpkg flag or the .soarpkg extension.
◆ kunai : Threat-hunting tool for Linux like Sysmon. To install it use the --soarpkg flag or the .soarpkg extension.
◆ kungfu : Flexible DNS hijacking and proxy tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lam : Laminate files [outils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lame : High quality MPEG Audio Layer III (MP3) encoder. To install it use the --soarpkg flag or the .soarpkg extension.
◆ landrun : Run any Linux process in a secure, unprivileged sandbox using Landlock. To install it use the --soarpkg flag or the .soarpkg extension.
◆ last : Show a listing of last logged in users [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ last : Show listing of the last users that logged into the system [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ last : Show listing of the last users that logged into the system [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lastb : Show a listing of all bad login attempts (/var/log/btmp) [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lav2mpeg : Easy conversion of lav files to mpeg [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lav2wav : Extract the audio out of MJPEG container files to stdout [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lav2yuv : Convert a MJPEG file to raw yuv [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lavaddwav : Add WAV audio to MJPEG video files [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lavinfo : Display information about MJPEG video files [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lavpipe : Create raw YUV streams from pipe list scripts [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lavtrans : Convert MJPEG videos to other MJPEG video formats [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lazydocker : TUI for docker resource visualization and management. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lazygit : simple terminal UI for git commands. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lazyjournal : TUI for reading logs from journalctl, file system, Docker/Podman containers & More. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lckdo : Run a program with a lock held [moreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lcov : A graphical GCOV front-end [lcov]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ld : Standalone Linker Compiler [xcc]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ld : The GNU linker [bintools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ld : The GNU linker [binutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ld.bfd : The GNU linker with BFD libraries [bintools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ld.bfd : The GNU linker with BFD libraries [binutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ld.gold : A new, faster, ELF only linker [bintools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ld.gold : A new, faster, ELF only linker [binutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ldattach : Attach a line discipline to a serial line [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lddtree : Display ELF dependencies as a tree [pax-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lddtree : Read the ELF dependency tree. To install it use the --soarpkg flag or the .soarpkg extension.
◆ len : Filter words by length, outputting only those within a specified range [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ less : A fast file pager/viewer allowing both forward and backward navigation [less]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ less : View a file or list of files [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ less : View a file or list of files [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lessecho : Echoe arguments with metacharacters preceded by an escape character (backslash by default) [less]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lesskey : Customize key bindings for less [less]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lf : Terminal file manager. To install it use the --soarpkg flag or the .soarpkg extension.
◆ libdeflate-gzip : Heavily optimized CLI for DEFLATE/zlib/gzip compression and decompression. To install it use the --soarpkg flag or the .soarpkg extension.
◆ libtree : Tree ldd with an option to bundle dependencies into a single folder. To install it use the --soarpkg flag or the .soarpkg extension.
◆ libtree : Tree ldd with an option to bundle dependencies into a single folder. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ligolo-proxy : Tunneling/pivoting tool that uses a TUN interfaces. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lilipod : Simple Container Manager with no external dependencies. To install it use the --soarpkg flag or the .soarpkg extension.
◆ linetime : Add timestamps at the start of lines. To install it use the --soarpkg flag or the .soarpkg extension.
◆ link : Call the link function to create a link to a file [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ link : Call the link function to create a link to a file [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ link : Call the link function to create a link to a file [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ link : Call the link function to create a link to a file [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ link : Manage network interfaces [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ link : Manage network interfaces [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ linkook : OSINT tool to find Accounts/Emails across multiple platforms using a single Username. To install it use the --soarpkg flag or the .soarpkg extension.
◆ linutil : Distro-agnostic toolbox designed to simplify everyday Linux tasks. To install it use the --soarpkg flag or the .soarpkg extension.
◆ linux32 : Change reported architecture to 32bit [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ linux32 : Change reported architecture to 32bit [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ linux32 : Change reported architecture to 32bit [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ linux64 : Change reported architecture to 64bit [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ linux64 : Change reported architecture to 64bit [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ linux64 : Change reported architecture to 64bit [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ linuxrc : Tiny versions of many common UNIX utilities into a single small executable (Standalone). To install it use the --soarpkg flag or the .soarpkg extension.
◆ linuxrc : Tiny versions of many common UNIX utilities into a single small executable (Standalone). To install it use the --soarpkg flag or the .soarpkg extension.
◆ linuxwave : Generate music from the entropy of Linux. To install it use the --soarpkg flag or the .soarpkg extension.
◆ listen : listens and announces on a network port, ported from Torgo. To install it use the --soarpkg flag or the .soarpkg extension.
◆ listmonk : High performance, self-hosted, newsletter and mailing list manager with a modern dashboard. To install it use the --soarpkg flag or the .soarpkg extension.
◆ litefs : FUSE-based file system for replicating SQLite databases across a cluster of machines. To install it use the --soarpkg flag or the .soarpkg extension.
◆ litestream : Standalone Disaster Recovery Tool for SQLite. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ln : Create a link LINK or DIRECTORY/TARGET to the specified TARGET(s) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ln : Create a link LINK or DIRECTORY/TARGET to the specified TARGET(s) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ln : Create a link LINK or DIRECTORY/TARGET to the specified TARGET(s) [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ln : Make links between files [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ln : Make links between files [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ln : Make links between files [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ln : Make links between files [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lnav : Log file navigator. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lndir : Create a shadow directory of symbolic links to another directory [outils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lnstat : Unified linux network interface/route statistics [iproute2]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ loadfont : Load a console font from standard input [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ loadfont : Load a console font from standard input [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ loadkmap : Load a binary keyboard translation table from standard input [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ loadkmap : Load a binary keyboard translation table from standard input [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ locate : List files in databases that match a pattern. To install it use the --soarpkg flag or the .soarpkg extension.
◆ locate : Search a database for all pathnames which match the specified pattern [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ log_proxy : Utility for apps that write their logs to stdout/stderr. To install it use the --soarpkg flag or the .soarpkg extension.
◆ log_proxy_wrapper : Utility for apps that write their logs to stdout/stderr. To install it use the --soarpkg flag or the .soarpkg extension.
◆ logdy : Web based real-time log viewer. To install it use the --soarpkg flag or the .soarpkg extension.
◆ logger : Enter messages into the system log [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ logger : Write MESSAGE to the system log [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ logger : Write MESSAGE to the system log [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ logger : Write MESSAGE to the system log [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ login : Begin a new session on the system [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ login : Begin a new session on the system [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ login : Begin session on the system [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ login : Begin session on the system [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ logname : Print the name of the current user [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ logname : Print the name of the current user [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ logname : Print the name of the current user [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ logname : Print user's login name [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ logname : Print user's login name [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ logname : Print user's login name [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ logoutd : Enforce login time restrictions [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ logread : Show messages in syslogd's circular buffer [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ logread : Show messages in syslogd's circular buffer [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ logsave : Save the output of a command in a logfile [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ logsave : Save the output of a command in a logfile [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ logss : Split & Visualize an input stream of text/logs. To install it use the --soarpkg flag or the .soarpkg extension.
◆ logstation : Tail log files in web browser. To install it use the --soarpkg flag or the .soarpkg extension.
◆ logtimer : Add timestamp to output. To install it use the --soarpkg flag or the .soarpkg extension.
◆ loksh : Linux port of OpenBSDs Korn Shell (ksh). To install it use the --soarpkg flag or the .soarpkg extension.
◆ look : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ look : Display lines beginning with a specified string [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ loopwhilex : Perform a conditional loop [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ loopwhilex : Perform a conditional loop [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ losetup : Control loop devices [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ losetup : Control loop devices [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ losetup : Control loop devices [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lpd : Line printer daemon, handles print jobs [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lpd : Line printer daemon, handles print jobs [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lpq : List print jobs in the queue [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lpq : List print jobs in the queue [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lpr : Submit a print job to the printer [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lpr : Submit a print job to the printer [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ls : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ls : List directory contents [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ls : List directory contents [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ls : List directory contents [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ls : List directory contents [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ls : List directory contents [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ls : List directory contents [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lsattr : List ext2/ext3/ext4 file attributes [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lsattr : List ext2/ext3/ext4 file attributes [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lsattr : List file attributes on an ext2 fs [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lsattr : List file attributes on an ext2 fs [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lsblk : List information about block devices [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lscpu : Display information about the CPU architecture [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lsfd : List file descriptors [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lsipc : List System V inter-process communication (IPC) resources [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lsirq : Utility to display kernel interrupt information [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lsix : List files with thumbnails using Sixel graphics. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lslocks : List local system locks [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lslogins : Display information about known users in the system [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lsmem : List the ranges of available memory with their online status [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lsmod : List the currently loaded kernel modules [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lsmod : List the currently loaded kernel modules [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lsmod : Show the status of modules in the Linux Kernel [kmod]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lsns : List system namespaces [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lsof : List Information about Files Opened by Processes. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lsof : Lists open files and the processes using them [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lsof : Lists open files and the processes using them [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lspci : Lists all PCI devices on the system [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lspci : Lists all PCI devices on the system [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lsscsi : Displays SCSI devices on the system [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lsscsi : Displays SCSI devices on the system [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lsusb : List USB devices [usbutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lsusb : Lists USB devices connected to the system [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lsusb : Lists USB devices connected to the system [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lua : Lua Interpreter [lua]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ luac : Lua Compiler [lua]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ luet : 0-dependency Container-based Package Manager. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lurk : Simple and pretty alternative to strace. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lux : Fast, simple and clean video downloader. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lz : Gunzip and show a listing of a gzip'd tar'd archive [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lzcat : Decompress LZMA files to stdout [alias=xz --format=lzma --decompress --stdout] [xz]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lzcat : Uncompress to stdout [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lzcat : Uncompress to stdout [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lzcmp : Compare LZMA compressed files [xz]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lzdiff : Diff LZMA compressed files [xz]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lzegrep : Search LZMA compressed files using extended regular expressions [xz]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lzfgrep : Search LZMA compressed files using fixed strings [xz]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lzgrep : Search LZMA compressed files using regular expressions [xz]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lzless : View LZMA compressed files with less [xz]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lzma : Compress/Decompress files using LZMA format [alias=xz --format=lzma] [xz]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lzma : Compression tool using the LZMA algorithm [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lzma : Compression tool using the LZMA algorithm [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lzmadec : Small and fast decoder for LZMA compressed files [xz]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lzmainfo : Show information stored in the .lzma file header [xz]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lzmore : View LZMA compressed files with more [xz]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lzop : Fast compression tool based on LZO algorithm [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ lzop : Fast compression tool based on LZO algorithm [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ m4 : Macro language processor [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mabel : A fancy BitTorrent client for the terminal. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mac2unix : MAC to Unix and vice versa text file format (line endings) converter [dos2unix]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ macchina : System information fetcher, with an emphasis on performance and minimalism. To install it use the --soarpkg flag or the .soarpkg extension.
◆ maddy : Composable all-in-one mail server. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mailpit : Email and SMTP testing tool with API for developers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ make : GNU Make build tool to compile programs. To install it use the --soarpkg flag or the .soarpkg extension.
◆ make : GNU Make build tool to compile programs. To install it use the --soarpkg flag or the .soarpkg extension.
◆ make-prime-list : Generate Prime Numbers [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ make-prime-list : Generate Prime Numbers [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ makebb : Compiles many Go commands into one busybox-style binary. To install it use the --soarpkg flag or the .soarpkg extension.
◆ makebbmain : Adds u-root command package imports to an existing main() template file. To install it use the --soarpkg flag or the .soarpkg extension.
◆ makedevs : Utility to create device files in /dev [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ makedevs : Utility to create device files in /dev [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ makemime : Create multipart MIME-encoded message from FILEs [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ makemime : Create multipart MIME-encoded message from FILEs [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ maketab : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ malwoverview : Tool for threat hunting and gathering intel information from various sources. To install it use the --soarpkg flag or the .soarpkg extension.
◆ man : Format and display manual page [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ man : Format and display manual page [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mandown : man-page inspired Markdown viewer. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mani : CLI tool to manage multiple repositories. To install it use the --soarpkg flag or the .soarpkg extension.
◆ manifest-tool : Create and Query container image manifest list/indexes. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mantra : Tool used to hunt down API key leaks in JS files and pages. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mapcidr : Utility program to perform multiple operations for a given subnet/CIDR ranges. To install it use the --soarpkg flag or the .soarpkg extension.
◆ masscan : Asynchronous port scanner. To install it use the --soarpkg flag or the .soarpkg extension.
◆ massdns : A high-performance DNS stub resolver for bulk lookups and reconnaissance. To install it use the --soarpkg flag or the .soarpkg extension.
◆ massdns : A high-performance DNS stub resolver for bulk lookups and reconnaissance. To install it use the --soarpkg flag or the .soarpkg extension.
◆ matterbridge : Multi-protocols (IRC/XMPP/Mattermost/Slack/Matrix/etc) bridge. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mattrib : Change MSDOS file attribute flags [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mawk : Fast Interpreter for the AWK Programming Language. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mc : Unix like utilities for object store. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mcat : Dump raw disk image [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mcd : Change MSDOS directory [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mcheck : Verify all files on an MS-DOS formatted disk [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mcomp : Compares two files [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mcookie : Generate magic cookies for xauth [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mcookie : Print the name of the current user [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mcopy : Copy MSDOS files to/from Unix [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ md5 : Calculate a message digest (checksum) for a file [outils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ md5 : Print or check MD5 checksums [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ md5sum : Compute and check MD5 message digest [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ md5sum : Compute and check MD5 message digest [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ md5sum : Compute and check MD5 message digest [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ md5sum : Print or check MD5 checksums [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ md5sum : Print or check MD5 checksums [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mdel : Delete an MSDOS file [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mdeltree : Recursively delete an MSDOS directory and its contents [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mdev : A minimal device manager for Linux [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mdev : A minimal device manager for Linux [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mdig : Multi thread dns resolver [zapret]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mdir : Display an MSDOS directory [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mdoctorfat : Doctor the allocation table of a file [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mdq : Like jq but for Markdown. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mdsh : Markdown shell pre-processor. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mdu : Display the amount of space occupied by an MSDOS directory [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mediainfo : Command-line utility for reading information from media files. To install it use the --soarpkg flag or the .soarpkg extension.
◆ melange : Build APKs from source code. To install it use the --soarpkg flag or the .soarpkg extension.
◆ memhog : Allocate memory with policy for testing [numactl]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mergeeet : Read from multiple file descriptors (FDs) concurrently and merge their output to stdout [emanutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mergerfs : A featureful union filesystem. To install it use the --soarpkg flag or the .soarpkg extension.
◆ merklemap-cli : Search/Enumerate subdomains matching a given query using merklemap.com. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mesg : Control write access to your terminal [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mesg : Control write access to your terminal [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mesg : Control write access to your terminal [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mesg : Display (or do not display) messages from other users [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ messi : IPv6 Network Reconnaissance Toolkit [ipv6toolkit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ metabigor : Intelligence Tool to do OSINT but without API keys. To install it use the --soarpkg flag or the .soarpkg extension.
◆ metafinder : Search documents from a domain through search engines. To install it use the --soarpkg flag or the .soarpkg extension.
◆ metaflac : Encode/Decode the FLAC lossless audio file format. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mformat : Add an MSDOS filesystem to a low-level formatted floppy disk [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mgraftcp : Redirect a given program's TCP traffic to SOCKS5 or HTTP proxy without using the LD_PRELOAD trick. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mgwls : Combine words from two wordlist files and concatenate them with an optional delimiter. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mhz : CPU frequency measurement utility. To install it use the --soarpkg flag or the .soarpkg extension.
◆ micro : Modern and intuitive terminal-based text editor. To install it use the --soarpkg flag or the .soarpkg extension.
◆ microcom : Copy bytes for stdin to TTY and from TTY to stdout [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ microcom : Copy bytes for stdin to TTY and from TTY to stdout [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ micromamba : Tiny version of mamba, the fast conda package installer. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mieru : Socks5/HTTP/HTTPS proxy to bypass censorship. To install it use the --soarpkg flag or the .soarpkg extension.
◆ migratepages : Migrate the physical location a processes pages [numactl]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ migspeed : Test the speed of page migration [numactl]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mii-tool : View/Manipulate media-independent interface status. To install it use the --soarpkg flag or the .soarpkg extension.
◆ miller : Combination of sed,awk,cut,join,sort utils for CSV, Json, TSV & more. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mim : Perform MIME encoding/decoding [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mim : Perform MIME encoding/decoding [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ minfo : Print the parameters of a MSDOS filesystem [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ miniflux : Minimalist and opinionated feed reader. To install it use the --soarpkg flag or the .soarpkg extension.
◆ minify : Minify Web formats like HTML5, CSS3, JS, JSON, SVG and XML more. To install it use the --soarpkg flag or the .soarpkg extension.
◆ miniserve : CLI tool to serve files and dirs over HTTP. To install it use the --soarpkg flag or the .soarpkg extension.
◆ minisign : A dead simple tool to sign files and verify signatures. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mirrorbits : Geographical Download Redirector for Distributing Files Efficiently across a set of Mirrors. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mise : Polyglot runtime and dev tool version manager. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mispipe : Pipe two commands, returning the exit status of the first [moreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mitmproxy : SSL/TLS-capable man-in-the-middle proxy for HTTP and Websockets. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mk_cmds : Convert a table of command names and help messages into a C source file [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkbundle : CLI for building certificate pool bundles [cfssl]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkdir : Create DIRECTORY [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkdir : Create DIRECTORY [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkdir : Create DIRECTORY [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkdir : Make directories [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkdir : Make directories [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkdir : Make directories [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkdosfs : Make a FAT32 filesystem [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkdosfs : Make a FAT32 filesystem [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkdwarfs : Create highly compressed read-only file systems [dwarfs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mke2fs : Create an ext2/ext3/ext4 file system [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mke2fs : Create an ext2/ext3/ext4 file system [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mke2fs : Utility to create ext2/ext3/ext4 filesystems [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mke2fs : Utility to create ext2/ext3/ext4 filesystems [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkexfatfs : Create an exFAT file system (alias=mkfs.exfat). To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkfifo : Create named pipe (identical to 'mknod name p') [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkfifo : Create named pipe (identical to 'mknod name p') [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkfifo : Create named pipe (identical to 'mknod name p') [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkfifo : Make FIFOs (named pipes) [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkfifo : Make FIFOs (named pipes) [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkfifo : Make FIFOs (named pipes) [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkfs : Make a Linux filesystem [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkfs-ext2 : Make an EXT2 filesystem [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkfs-ext2 : Make an EXT2 filesystem [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkfs-minix : Make a MINIX filesystem [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkfs-minix : Make a MINIX filesystem [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkfs-vfat : Make a FAT32 filesystem [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkfs-vfat : Make a FAT32 filesystem [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkfs.bfs : Make an SCO bfs filesystem [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkfs.btrfs : Tool  to create btrfs filesystem on a single or multiple devices [btrfs-progs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkfs.cramfs : Make cramfs compressed ROM file system [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkfs.exfat : Create an exFAT file system (alias=mkexfatfs). To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkfs.exfat : Userspace utilities for creating and fixing and debugging exFAT filesystem. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkfs.ext2 : Create an ext2 file system [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkfs.ext3 : Create an ext3 file system [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkfs.ext4 : Create an ext4 file system [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkfs.minix : Make a Minix filesystem [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkmanifest : Make list of file names and their DOS 8+3 equivalent [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mknod : Create a special file (block, character, or pipe) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mknod : Create a special file (block, character, or pipe) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mknod : Create a special file (block, character, or pipe) [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mknod : Make block or character special files [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mknod : Make block or character special files [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mknodes : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkpasswd : Crypt the PASSWORD using crypt(3) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkpasswd : Crypt the PASSWORD using crypt(3) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkpasswd : Generate new password, optionally apply it to a user. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mksquashfs : Create and append to squashfs filesystems [squashfstools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mksub : Generate tens of thousands of subdomain combinations in a matter of seconds. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkswap : Create a swap area [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkswap : Prepare block device to be used as swap partition [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mkswap : Prepare block device to be used as swap partition [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mksyntax : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mktemp : Create a temporary file or directory [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mktemp : Create a temporary file or directory [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mktemp : Create a temporary file or directory [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mktemp : Create a temporary file or directory [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mktemp : Create a temporary file with name based on TEMPLATE and print its name [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mktemp : Create a temporary file with name based on TEMPLATE and print its name [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mktemp : Create a temporary file with name based on TEMPLATE and print its name [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mlabel : Make an MSDOS volume label [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mldq6 : Assess attack vectors based on ICMPv6 Multicast Listener Discovery Query messages [ipv6toolkit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mli2 : Merge two lists [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mmd : Make an MSDOS subdirectory [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mmlog : Log rotation tool for shell scripts, or apps which generate logging data to STDOUT/STDERR. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mmount : Mount an MSDOS disk [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mmove : Move/Rename an MSDOS file or subdirectory [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mmpfb : Creates single-master fonts from multiple master fonts [lcdf-typetools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mmv : Move/copy/append/link multiple files according to a set of wildcard patterns. To install it use the --soarpkg flag or the .soarpkg extension.
◆ moac : Generate passwords and analyze their strength given physical limits to computation. To install it use the --soarpkg flag or the .soarpkg extension.
◆ moac-pwgen : Generate passwords and analyze their strength given physical limits to computation. To install it use the --soarpkg flag or the .soarpkg extension.
◆ modinfo : Show information about a Linux Kernel module [kmod]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ modinfo : Show information about a Linux kernel module [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ modinfo : Show information about a Linux kernel module [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ modprobe : Add and remove modules from the Linux Kernel [kmod]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ modprobe : Add or remove kernel modules [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ modprobe : Add or remove kernel modules [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ monolith : ⬛️ CLI tool for saving complete web pages as a single HTML file. To install it use the --soarpkg flag or the .soarpkg extension.
◆ more : A file perusal filter for CRT viewing [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ more : Cross-platform Rust rewrite of the GNU coreutils. To install it use the --soarpkg flag or the .soarpkg extension.
◆ more : View FILE or standard input one screenful at a time [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ more : View FILE or standard input one screenful at a time [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ morph : Generate insertion rules for frequent character chains by position from a provided dictionary [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ morrigan : Busybox like Penetration Tool Set. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mosh-client : Mobile shell that supports roaming and intelligent local echo. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mosh-server : Mobile shell that supports roaming and intelligent local echo. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mount : Mount a filesystem [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mount : Mount a filesystem [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mount : Mount a filesystem [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mount.exfat : Mount an exFAT file system (alias=mount.exfat-fuse). To install it use the --soarpkg flag or the .soarpkg extension.
◆ mount.exfat-fuse : Mount an exFAT file system (alias=mount.exfat). To install it use the --soarpkg flag or the .soarpkg extension.
◆ mount.fuse : Configuration and mount options for FUSE file systems [fuse]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mount.fuse3 : Configuration and mount options for FUSE file systems [fuse3]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mount.unionfs : Mount a filesystem as an Union Filesystem [unionfs-fuse3]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mount.unionfs : Mount a filesystem as an Union Filesystem [unionfs-fuse]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mountpoint : Check if the directory is a mountpoint [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mountpoint : Check if the directory is a mountpoint [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mountpoint : Check whether a directory or file is a mountpoint [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mp2enc : Simple MPEG-1 layer-II audio encoder [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mpartition : Partition an MSDOS hard disk [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mpeg2enc : MPEG-1/2 encoder [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mpegtranscode : Suite of programs for processing MPEG or MJPEG video. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mplex : MPEG 1/2 program/system stream multiplexer [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mpstat : Report per-processor statistics [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mpstat : Report per-processor statistics [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mqttui : Simple lightweight terminal based MQTT monitor and publisher. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mrd : Remove an MSDOS subdirectory [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mren : Rename an existing MSDOS file [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ msgattrib : Filter messages of a translation catalog according to their attributes & manipulate them [gettext]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ msgcat : Concatenate and merge the specified PO files [gettext]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ msgcmp : Compare message catalog and template [gettext]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ msgcomm : Find messages which are common to two or more of the specified PO files [gettext]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ msgconv : Convert a translation catalog to a different character encoding [gettext]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ msgen : Create an English translation catalog [gettext]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ msgexec : Apply a command to all translations of a translation catalog [gettext]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ msgfilter : Apply a filter to all translations of a translation catalog [gettext]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ msgfmt : Generate binary message catalog from textual translation description [gettext]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ msggrep : Extracts all messages of a translation catalog based on a pattern or belonging to some source files [gettext]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ msginit : Initialize a message catalog [gettext]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ msgmerge : Merge message catalog and template [gettext]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ msgunfmt : Convert binary message catalog to Uniforum style .po file [gettext]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ msguniq : Unify duplicate translations in message catalog [gettext]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mshortname : Show short name of a file [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mshowfat : Show FAT clusters allocated to file [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mt : Control magnetic tape drive operation [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mt : Control magnetic tape drive operation [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mtools : Utilities to access DOS disks in Unix [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mtoolstest : Tests and Display the configuration [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mtr : A network diagnostic tool that combines the functionality of traceroute and ping [mtr]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mtr-packet : Send & Receive network probes [mtr]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mtype : Display contents of an MSDOS file [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mubeng : An incredibly fast proxy checker & IP rotator with ease. To install it use the --soarpkg flag or the .soarpkg extension.
◆ muffet : Website link checker which scrapes and inspects all pages in a website recursively. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mult : Run a command multiple times and glance at the outputs. To install it use the --soarpkg flag or the .soarpkg extension.
◆ multidefine : Split a value and define several variables at once & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ multidefine : Split a value and define several variables at once & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ multirootca : CLI certificate authority server that can use multiple signing keys [cfssl]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ multisubstitute : Perform several substitutions at once in its argv & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ multisubstitute : Perform several substitutions at once in its argv & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mup : Micro μ file Uploader. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mux : Mux streams of input events [interception-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mv : Move (rename) files [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mv : Move (rename) files [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mv : Move (rename) files [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mv : Rename SOURCE to DEST, or move SOURCE(s) to DIRECTORY [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mv : Rename SOURCE to DEST, or move SOURCE(s) to DIRECTORY [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mv : Rename SOURCE to DEST, or move SOURCE(s) to DIRECTORY [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mxtar : Wrapper for using GNU tar directly from a floppy disk [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ mzip : Change protection mode and eject disk on Zip/Jaz drive [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ na6 : Assess attack vectors based on ICMPv6 Neighbor Advertisement messages [ipv6toolkit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ naabu : A fast port scanner written in go with a focus on reliability and simplicity. To install it use the --soarpkg flag or the .soarpkg extension.
◆ namegen : A unique(ish) random name and id generator. To install it use the --soarpkg flag or the .soarpkg extension.
◆ namei : Follow a pathname until a terminal point is found [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nameif : Name network interfaces based on MAC addresses. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nameif : Rename network interface while it in the down state [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nameif : Rename network interface while it in the down state [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nanddump : Reads NAND flash memory and dumps its contents [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nanddump : Reads NAND flash memory and dumps its contents [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nandwrite : Writes data to a NAND flash memory device [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nandwrite : Writes data to a NAND flash memory device [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ navi : Interactive cheatsheet tool for the command-line. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nbdclient : Connects to a Network Block Device server [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nbdclient : Connects to a Network Block Device server [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nc : Netcat, a tool for network connections [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nc : Netcat, a tool for network connections [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nc : TCP/IP swiss army knife (GNU-variant). To install it use the --soarpkg flag or the .soarpkg extension.
◆ nc : TCP/IP swiss army knife (libressl-variant). To install it use the --soarpkg flag or the .soarpkg extension.
◆ ncal : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ncat : Concatenate and redirect sockets [nmap]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ncdu : Disk usage analyzer with an ncurses interface. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ncdu : Disk usage analyzer with an ncurses interface. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ncgopher : Fast and modern Gopher, Gemini and finger terminal client. To install it use the --soarpkg flag or the .soarpkg extension.
◆ neofetch : A CLI system information tool (Updated Fork). To install it use the --soarpkg flag or the .soarpkg extension.
◆ nerdctl : Docker-compatible CLI for containerd, with support for Compose, Rootless, eStargz, OCIcrypt, IPFS. To install it use the --soarpkg flag or the .soarpkg extension.
◆ netbird : Client for Netbird; A WireGuard-based mesh network. To install it use the --soarpkg flag or the .soarpkg extension.
◆ netbird-mgmt : Management service for Netbird. To install it use the --soarpkg flag or the .soarpkg extension.
◆ netbird-relay : Relay service for Netbird. To install it use the --soarpkg flag or the .soarpkg extension.
◆ netbird-signal : Signal service for Netbird. To install it use the --soarpkg flag or the .soarpkg extension.
◆ netcap : See network capabilities [libcap-ng]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ netcat : TCP/IP swiss army knife (GNU-variant). To install it use the --soarpkg flag or the .soarpkg extension.
◆ netcat : TCP/IP swiss army knife (libressl-variant). To install it use the --soarpkg flag or the .soarpkg extension.
◆ netexec : The Network Execution (CrackMapExec) Tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ netmaker : WireGuard automation from homelab to enterprise. To install it use the --soarpkg flag or the .soarpkg extension.
◆ netproc : Tool to monitor network traffic based on processes. To install it use the --soarpkg flag or the .soarpkg extension.
◆ netscanner : Network scanner with features like WiFi scanning, packetdump and more. To install it use the --soarpkg flag or the .soarpkg extension.
◆ netstat : Display networking information [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ netstat : Display networking information [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ netstat : Print network connections, routing tables, interface statistics, masquerade connections, and multicast memberships. To install it use the --soarpkg flag or the .soarpkg extension.
◆ newgidmap : Set the gid mapping of a user namespace [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ newgrp : Log in to a new group [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ newuidmap : Set the uid mapping of a user namespace [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ newusers : Update and Create new users in batch [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nfpm : A simple deb, rpm, apk, ipk, and arch linux packager. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nfqws : Utility for modifying TCP connection at the package level [zapret]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ngettext : Translate message and choose plural form [gettext]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nginx : HTTP and reverse proxy server. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ngocok : ngrok Collaborator Link — yet another Burp Collaborator alternative for free with ngrok. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ngrok : External Tunnel to Internal Assets. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ngspice : Mixed-level/Mixed-signal circuit simulator based on Spice3f5, Cider1b1, and Xspice. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ni6 : Exploit potential flaws in the processing of ICMPv6 Node Information messages [ipv6toolkit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nice : Run PROG with modified scheduling priority [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nice : Run PROG with modified scheduling priority [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nice : Run PROG with modified scheduling priority [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nice : Run a program with modified scheduling priority [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nice : Run a program with modified scheduling priority [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nice : Run a program with modified scheduling priority [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ninja : Small build system with a focus on speed. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nisdomainname : Show/Set the system's NIS domain name. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nitro : Tiny and Flexible Init system and process Supervisior. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nitroctl : Tiny and Flexible Init system and process Supervisior. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nl : Number lines of files [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nl : Number lines of files [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nl : Number lines of files [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nl : Number lines of input [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nl : Number lines of input [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nl : Number lines of input [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nllint : Linter to check newlines at the end of files. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nm : Lists symbols from object files [bintools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nm : Lists symbols from object files [binutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nmap : Network exploration tool and security/port scanner [nmap]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nmap-formatter : A tool to convert NMAP results to html, csv, json, markdown, graphviz (dot) or sqlite. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nmapurls : parses Nmap xml reports and outputs a list of http(s) URLs. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nmctl : WireGuard automation from homelab to enterprise. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nmeter : Monitor system in real time [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nmeter : Monitor system in real time [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nodepass : Elegant, efficient TCP tunneling solution that creates secure communication bridges between network endpoints. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nohup : Run PROG immune to hangups, with output to a non-tty [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nohup : Run PROG immune to hangups, with output to a non-tty [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nohup : Run PROG immune to hangups, with output to a non-tty [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nohup : Run a command immune to hangups, with output to a non-tty [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nohup : Run a command immune to hangups, with output to a non-tty [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nohup : Run a command immune to hangups, with output to a non-tty [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nohup : Run a command immune to hangups, with output to a non-tty [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nologin : Politely refuse a login [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nologin : Politely refuse a login [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nologin : Refuse login with an error message [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nologin : Refuse login with an error message [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nomore403 : Tool to bypass 403/40X response codes. To install it use the --soarpkg flag or the .soarpkg extension.
◆ noseyparker : Finds secrets and sensitive information in textual data and Git history. To install it use the --soarpkg flag or the .soarpkg extension.
◆ noseyparker-cli : Finds secrets and sensitive information in textual data and Git history. To install it use the --soarpkg flag or the .soarpkg extension.
◆ notify : Send the output from any tool to Slack, Discord, Telegram & More. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nping : Network packet generation tool/ping utility [nmap]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nping : Ping with Real-Time Data and Visualizations. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nproc : Print the number of processing units available [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nproc : Print the number of processing units available [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nproc : Print the number of processing units available [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nproc : Print the number of processing units available [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nproc : Print the number of processing units available [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nproc : Print the number of processing units available [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nproc : Print the number of processing units available [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nq : Job Queue Utility [nq]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nq.sh : Wrapper for nq to run CMD in background and in order, saving output to * files [nq]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nqtail : Job Queue Log Viewer [nq]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nqtail.sh : Wrapper for nqtail to tail -F the queue outputs, quitting when the job finishes [nq]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nqterm : Job Queue Wrapper for tmux/screen [nq]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nrich : Quickly analyze all IPs in a file and see which ones have open ports/vulnerabilities using Shodan. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nrp : List domains without redirects [lit-bb-hack-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ns6 : Assess attack vectors based on ICMPv6 Neighbor Solicitation messages [ipv6toolkit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nsenter : Run a program with namespaces of other processes [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nsenter : Run command in a different namespace [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nsenter : Run command in a different namespace [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nsjail : Process Isolation Tool that utilizes Linux namespace subsystem, resource limits, and the seccomp-bpf syscall filters. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nslookup : Query the nameserver for the IP address of the given HOST [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nslookup : Query the nameserver for the IP address of the given HOST [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nstat : Display network statistics for network interfaces, protocols, and sockets [iproute2]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ntask : Distributed task management program that allows you to distribute tasks, commands, or programs across multiple computers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nth : CLI for the identification of hashes. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ntpd : Network Time Protocol daemon [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ntpd : Network Time Protocol daemon [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nu : Shell with a focus on structured data. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nuclei : Fast and customizable vulnerability scanner based on simple YAML based DSL. To install it use the --soarpkg flag or the .soarpkg extension.
◆ numactl : Control NUMA policy for processes or shared memory [numactl]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ numademo : A libnuma test/demo program that benchmarks NUMA memory configurations [numactl]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ numastat : Show per-NUMA-node memory statistics for processes and the operating system [numactl]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ numfmt : Convert numbers from/to human-readable strings [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ numfmt : Convert numbers from/to human-readable strings [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ numfmt : Convert numbers from/to human-readable strings [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ numfmt : Convert numbers from/to human-readable strings [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nushell : Shell with a focus on structured data. To install it use the --soarpkg flag or the .soarpkg extension.
◆ nushell : Shell with a focus on structured data. To install it use the --soarpkg flag or the .soarpkg extension.
◆ objcopy : Copies and translates object files [bintools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ objcopy : Copies and translates object files [binutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ objdump : Displays information from object files [bintools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ objdump : Displays information from object files [binutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ oci : Command Line Interface for Oracle Cloud Infrastructure. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ocrs : Extract text from images using OCR. To install it use the --soarpkg flag or the .soarpkg extension.
◆ od : Dump files in octal and other formats [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ od : Dump files in octal and other formats [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ od : Dump files in octal and other formats [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ od : Write an unambiguous representation, octal bytes by default, of FILE to standard output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ od : Write an unambiguous representation, octal bytes by default, of FILE to standard output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ogg123 : Play Ogg, and FLAC files [vorbis-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ oggdec : Simple decoder to PCM audio file (Wave or RAW) [vorbis-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ oggenc : Encode audio into the Ogg Vorbis format [vorbis-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ogginfo : Print information about Ogg files [vorbis-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ oils-for-unix : Unix shell with JSON-compatible structured data. To install it use the --soarpkg flag or the .soarpkg extension.
◆ oksh : Portable OpenBSD ksh, based on the Public Domain Korn Shell (pdksh). To install it use the --soarpkg flag or the .soarpkg extension.
◆ onctl : Manage Virtual Machines in Multi Cloud. To install it use the --soarpkg flag or the .soarpkg extension.
◆ onionpipe : Forward ports on the local host to remote Onion addresses as Tor hidden services and vice-versa. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ooniprobe : OONI Probe Engine and CLI. To install it use the --soarpkg flag or the .soarpkg extension.
◆ oosexclude : Remove outofscope subdomains from a updated or a local file. To install it use the --soarpkg flag or the .soarpkg extension.
◆ openapi-tui : Terminal UI to list, browse and run APIs defined with OpenAPI spec. To install it use the --soarpkg flag or the .soarpkg extension.
◆ opengfw : Flexible, easy-to-use, open source implementation of GFW on Linux. To install it use the --soarpkg flag or the .soarpkg extension.
◆ opengist : Self-hosted pastebin powered by Git. To install it use the --soarpkg flag or the .soarpkg extension.
◆ openpathfd : Replace the specified file descriptor with an O_PATH-opened path & execute a command [emanutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ openpidfd : Open a pidfd for a given PID, assign it to a specified FD & execute a command [emanutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ openrisk : Generates a risk score based on the results of a Nuclei scan. To install it use the --soarpkg flag or the .soarpkg extension.
◆ openssl : Toolkit for the Transport Layer Security (TLS) and Secure Sockets Layer (SSL) protocols. To install it use the --soarpkg flag or the .soarpkg extension.
◆ openvpn : A full-featured open source SSL VPN solution. To install it use the --soarpkg flag or the .soarpkg extension.
◆ openvscode-server : Run VS Code on any machine anywhere and access it in the browser (GitPod). To install it use the --soarpkg flag or the .soarpkg extension.
◆ openvt : Start PROG on a new virtual terminal [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ openvt : Start PROG on a new virtual terminal [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ops : Tool for creating and running a Nanos unikernel. To install it use the --soarpkg flag or the .soarpkg extension.
◆ opusdec : Encode/Decode audio in Opus format to Wave/RAW PCM [opus-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ opusenc : Encode audio into Opus format [opus-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ opusinfo : Display information and perform validity checks on Opus files [opus-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ oras : OCI registry client - manage content like artifacts, images, packages. To install it use the --soarpkg flag or the .soarpkg extension.
◆ oras : OCI registry client - manage content like artifacts, images, packages. To install it use the --soarpkg flag or the .soarpkg extension.
◆ orbiton : Fast and config-free text editor and IDE limited to VT100. To install it use the --soarpkg flag or the .soarpkg extension.
◆ org2asn : Extract ASN and IPs from bgp.he.net. To install it use the --soarpkg flag or the .soarpkg extension.
◆ oryx : TUI for sniffing network traffic using eBPF on Linux. To install it use the --soarpkg flag or the .soarpkg extension.
◆ osh : Unix shell with JSON-compatible structured data. To install it use the --soarpkg flag or the .soarpkg extension.
◆ osh : Unix shell with JSON-compatible structured data. To install it use the --soarpkg flag or the .soarpkg extension.
◆ osmedeus : A Workflow Engine for Offensive Security. To install it use the --soarpkg flag or the .soarpkg extension.
◆ otftotfm : Create TeX font metrics from OpenType fonts [lcdf-typetools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ otree : A command line tool to view objects (JSON/YAML/TOML) in TUI tree widget. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ouch : Command-line utility for easily compressing and decompressing files and directories. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ov : Feature-rich terminal-based text viewer & pager. To install it use the --soarpkg flag or the .soarpkg extension.
◆ overlay : Maintenance tools for overlay-filesystem. To install it use the --soarpkg flag or the .soarpkg extension.
◆ overmind : Process manager for Procfile-based applications and tmux. To install it use the --soarpkg flag or the .soarpkg extension.
◆ p1radup : Process URLs and remove duplicate query parameters. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pacaptr : Pacman-like syntax wrapper for many package managers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pagemon : Interactive memory/page monitoring tool to browse the memory map of a process. To install it use the --soarpkg flag or the .soarpkg extension.
◆ parallel : Run programs in parallel [moreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ partprobe : Inform the OS of partition table changes [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ partprobe : Inform the OS of partition table changes [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ partx : Tell the kernel about the presence and numbering of partitions [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ passdetective : Scans command history to detect mistakenly written passwords, API keys, and secrets. To install it use the --soarpkg flag or the .soarpkg extension.
◆ passwd : Change USER's password [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ passwd : Change USER's password [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ passwd : Change user password [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ paste : Concatenate and paste lines from files [bsdutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ paste : Concatenate and paste lines from files [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ paste : Concatenate and paste lines from files [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ paste : Concatenate and paste lines from files [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ paste : Merge lines of files [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ paste : Merge lines of files [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ paste : Merge lines of files [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ patch : Apply patches to files [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ patch : Apply patches to files [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ patch : Apply patches to files [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ patchelf : A small utility to modify the dynamic linker and RPATH of ELF executables. To install it use the --soarpkg flag or the .soarpkg extension.
◆ path6 : Versatile IPv6 traceroute [ipv6toolkit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pathchk : Check whether file names are valid or portable [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pathchk : Check whether file names are valid or portable [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pathchk : Check whether file names are valid or portable [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pathchk : check pathnames [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pdfcpu : A PDF processor written in Go. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pdfly : CLI tool to extract (meta)data from PDF and manipulate PDF files. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pdtm : ProjectDiscovery's Open Source Tool Manager. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pee : Tee standard input to pipes [moreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pencode : Complex payload encoder. To install it use the --soarpkg flag or the .soarpkg extension.
◆ permute : A stand-alone implementation of the Permutation Attack [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ permute_exist : Check if a permute Exists [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pet : Simple command-line snippet manager. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pg_activity : Top-like application for PostgreSQL server activity monitoring. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pgcli : Zero Config port forwarding tool works like ssh tunneling. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pgmtoy4m : Convert mpeg2dec pgm and pgmpipe output to YUV4MPEG2 [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pgrep : Display process(es) selected by regex PATTERN [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pgrep : Display process(es) selected by regex PATTERN [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pgrep : Search for processes by name and other attributes, returning their process IDs [procps]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pgrok : Selfhosted TCP/HTTP tunnel, alternative to ngrok. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pgweb : Cross-platform client for PostgreSQL databases. To install it use the --soarpkg flag or the .soarpkg extension.
◆ php-bulk-cli : Static PHP CLI with most extensions. To install it use the --soarpkg flag or the .soarpkg extension.
◆ php-bulk-fpm : Static PHP FastCGI Process Manager with most extensions. To install it use the --soarpkg flag or the .soarpkg extension.
◆ php-bulk-micro : Static PHP SAPI with most extensions. To install it use the --soarpkg flag or the .soarpkg extension.
◆ php-common-cli : Static PHP CLI with only common extensions. To install it use the --soarpkg flag or the .soarpkg extension.
◆ php-common-fpm : Static PHP FastCGI Process Manager with only common extensions. To install it use the --soarpkg flag or the .soarpkg extension.
◆ php-common-micro : Static PHP SAPI with only common extensions. To install it use the --soarpkg flag or the .soarpkg extension.
◆ php-minimal-cli : Static PHP CLI with only minimal extensions. To install it use the --soarpkg flag or the .soarpkg extension.
◆ php-minimal-fpm : Static PHP FastCGI Process Manager with only minimal extensions. To install it use the --soarpkg flag or the .soarpkg extension.
◆ php-minimal-micro : Static PHP SAPI with only minimal extensions. To install it use the --soarpkg flag or the .soarpkg extension.
◆ picocrypt : Small, simple, & Secure Encryption Tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ picoshare : A minimalist, easy-to-host service for sharing images and other files. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pidfdgetfd : Retrieve a file descriptor from a pidfd, assign it to a target file descriptor & execute a command [emanutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pidof : Determine the Process identifier of a running program [procps]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pidof : List PIDs of all processes with names that match NAMEs [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pidof : List PIDs of all processes with names that match NAMEs [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pier : A Linux script management CLI written in Rust. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pigz : A parallel implementation of gzip for multi-core machines. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pik : Find and Kill process Interactively. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ping : Send ICMP ECHO_REQUEST packets to IPv4 network hosts [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ping : Send ICMP ECHO_REQUEST packets to IPv4 network hosts [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ping : Send ICMP ECHO_REQUEST to network hosts [iputils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ping6 : Send ICMP ECHO_REQUEST packets to IPv6 network hosts [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ping6 : Send ICMP ECHO_REQUEST packets to IPv6 network hosts [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pingmole : CLI that helps to filter pingmole servers and pick the closest one. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pingtunnel : Reliably tunnel TCP connections over ICMP packets. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pinky : Lightweight finger [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pinky : Lightweight finger [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pinky : Lightweight finger [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pipe-progress : Show progress of piped data transfer [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pipe-progress : Show progress of piped data transfer [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pipeline : Run two commands with a pipe between them [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pipeline : Run two commands with a pipe between them [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ piperw : Create a pipe (an anonymous one) & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ piperw : Create a pipe (an anonymous one) & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pipesz : Set/Examine pipe and FIFO buffer sizes [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pipetty : Tool to pipe from a pseudoterminal [colorized-logs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pivot-root : Move the current root file system to PUT_OLD and make NEW_ROOT the new root file system [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pivot-root : Move the current root file system to PUT_OLD and make NEW_ROOT the new root file system [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pivot_root : Change the root filesystem [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pivot_root : Move the current root file system to PUT_OLD and make NEW_ROOT the new root file system [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pixi : Conda Package management made easy. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pixterm : Draw images in your ANSI terminal with true color. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pkgconf : Package compiler and linker metadata Toolkit. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pkill : Send a signal to process(es) selected by regex PATTERN [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pkill : Send a signal to process(es) selected by regex PATTERN [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pkill : Send a signal to processes based on name and other attributes [procps]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pktstat : A network monitoring tool with bandwidth tracking. To install it use the --soarpkg flag or the .soarpkg extension.
◆ planor : TUI client for cloud services (AWS, Vultr, Heroku, Render.com, Fleek & More). To install it use the --soarpkg flag or the .soarpkg extension.
◆ plipconfig : Fine tune PLIP device parameters. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pls : A Prettier and Powerful ls(1) for the Pros. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pmap : Report memory map of a process [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pmap : Report memory map of a process [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pmap : Report memory map of a process [procps]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ png2yuv : Convert PNG images to the YUV4MPEG stream format [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pnmtoy4m : Convert PNM images to YUV4MPEG2 stream [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ podsync : Turn YouTube or Vimeo channels, users, or playlists into podcast feeds. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pollinfd : Monitor a file descriptor for input events & execute a command [emanutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ popmaildir : Retrieve mail from a Maildir [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ popmaildir : Retrieve mail from a Maildir [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ porch-pirate : Recon/OSINT Tool for finding API Endpoints etc from Postman. To install it use the --soarpkg flag or the .soarpkg extension.
◆ port-for : CLI for local TCP ports management. To install it use the --soarpkg flag or the .soarpkg extension.
◆ portguard : Zero Config port forwarding tool works like ssh tunneling. To install it use the --soarpkg flag or the .soarpkg extension.
◆ portmap : Fast Passive Portscan Tool using Shodan/InternetDB Public Data. To install it use the --soarpkg flag or the .soarpkg extension.
◆ posix-cd : Change the current working directory to a given directory & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ posix-cd : Change the current working directory to a given directory & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ posix-umask : Change its file mode creation mask & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ posix-umask : Change its file mode creation mask & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ poweroff : Shut down the system [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ poweroff : Shut down the system [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ powerstat : Measure the power consumption of a machine using the battery stats or the Intel RAPL interface. To install it use the --soarpkg flag or the .soarpkg extension.
◆ powertop : Monitor and optimize power usage [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ powertop : Monitor and optimize power usage [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ powertop : Power consumption measurement and estimation tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ppath : Pretty Print your systems PATH environment variable. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ppfuzz : A fast tool to scan client-side prototype pollution vulnerability. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pphack : The Most Advanced Client-Side Prototype Pollution Scanner. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pping : Multi Protocol (tcp, tls, http, icmp, dns, quic & more) Ping. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ppmap : Scan & Exploit XSS via Prototype Pollution. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ppmtoy4m : Convert PPM images to YUV4MPEG2 stream [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pprof : A tool for visualization and analysis of profiling data [gperftools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pr : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pr : Convert text files for printing [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pr : Convert text files for printing [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pr : Convert text files for printing [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ prepare : Dictionary optimizer for the Permutation Attack [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pretender : MitM sidekick for relaying attacks featuring DHCPv6 DNS takeover, mDNS, LLMNR and NetBIOS-NS spoofing. To install it use the --soarpkg flag or the .soarpkg extension.
◆ prettyping : Ping wrapper that produces coloured, easily readable output. To install it use the --soarpkg flag or the .soarpkg extension.
◆ printenv : Print all or part of environment [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ printenv : Print all or part of environment [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ printenv : Print all or part of environment [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ printenv : Print all or part of environment [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ printenv : Print all or part of environment [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ printenv : Print all or part of environment [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ printenv : Print all or part of environment [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ printf : Format and print ARGUMENT(s) according to FORMAT [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ printf : Format and print ARGUMENT(s) according to FORMAT [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ printf : Format and print ARGUMENT(s) according to FORMAT [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ printf : Format and print data [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ printf : Format and print data [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ printf : Format and print data [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ printf : Format and print data [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ printf : the POSIX printf command. To install it use the --soarpkg flag or the .soarpkg extension.
◆ prlimit : Show or change the resource limits of a process [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ procan : Prints information about process parameters to stdout meant for debugging socat [soact]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ procs : A modern replacement for ps written in Rust. To install it use the --soarpkg flag or the .soarpkg extension.
◆ proot : User-space implementation of chroot, bind mount, and binfmt_misc. To install it use the --soarpkg flag or the .soarpkg extension.
◆ proot : User-space implementation of chroot, mount --bind and binfmt_misc. To install it use the --soarpkg flag or the .soarpkg extension.
◆ protocurl : cURL for Protobuf. To install it use the --soarpkg flag or the .soarpkg extension.
◆ proxify : Swiss Army knife Proxy tool for HTTP/HTTPS traffic capture, manipulation and replay. To install it use the --soarpkg flag or the .soarpkg extension.
◆ proxy-ns : Run programs in a network namespace where all traffic go through proxy. To install it use the --soarpkg flag or the .soarpkg extension.
◆ proxychains : Redirect any TCP connection through a proxy or proxy chain. To install it use the --soarpkg flag or the .soarpkg extension.
◆ proxychains-daemon : Redirect any TCP connection through a proxy or proxy chain. To install it use the --soarpkg flag or the .soarpkg extension.
◆ prtstat : Print statistics of a process [psmisc]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ps : Report a snapshot of the current processes [procps]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ps : Report process status [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ps : Report process status [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ps_mem : Utility to accurately report the in core memory usage for a program. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pscan : Scan a host, print all open ports [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pscan : Scan a host, print all open ports [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pscap : See process capabilities [libcap-ng]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ psendfd : Use ptrace to manipulate and redirect file descriptors of a running process [emanutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pseudocron : sleep(1) using a cron expression. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pslog : Prints log path of a process [psmisc]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pspax : List ELF/PaX information about running processes [pax-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pspax : List ELF/PaX information about running processes [pax-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pspy : Monitor linux processes without root permissions. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pstree : Display a tree of processes [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pstree : Display a tree of processes [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pstree : Show the currently running processes as a tree [psmisc]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pstree.x11 : Show the currently running processes as a tree (X11) [psmisc]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ptcpdump : Process-aware, eBPF-based tcpdump. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ptx : Produce a permuted index of file contents [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ptx : Produce a permuted index of file contents [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ptx : Produce a permuted index of file contents [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ptytty : Execute a command with I/O redirection to a specified pseudo-terminal device [emanutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ puncia : CLI utility for Subdomain Center & Exploit Observer. To install it use the --soarpkg flag or the .soarpkg extension.
◆ puredns : Fast domain resolver and subdomain bruteforcing with accurate wildcard filtering. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pv : Monitor the progress of data through a pipeline. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pwait : Small utility to wait for a process to finish. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pwck : Verify the integrity of password files [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pwconv : Convert to shadow passwords and groups [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pwd : Print name of current/working directory [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pwd : Print name of current/working directory [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pwd : Print name of current/working directory [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pwd : Print name of current/working directory [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pwd : Print the full filename of the current working directory [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pwd : Print the full filename of the current working directory [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pwd : Print the full filename of the current working directory [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pwdsafety : Check how safe a password is. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pwdx : Report current working directory of a process [procps]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pwdx : Show the current working directory of a process [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pwdx : Show the current working directory of a process [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pwninit : Automate starting binary exploit challenges. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pwunconv : Convert from shadow passwords and groups [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pxattr : Manipulate xattr on Dwarfs File Systems [dwarfs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pyarmor : Obfuscate, Bind & expire Python Scripts. To install it use the --soarpkg flag or the .soarpkg extension.
◆ pyp : Easily run Python at the shell. To install it use the --soarpkg flag or the .soarpkg extension.
◆ python : Interpreter/Runner for Python [python3]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ python-config : The Python Config Parser [python3]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ python3 : A high-level dynamically-typed programming language (LIMITED FEATURES). To install it use the --soarpkg flag or the .soarpkg extension.
◆ qbittorrent-nox : Static Single binary for Qbittorent. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-aarch64 : Simulate a 64-bit Arm machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-aarch64 : Simulate a 64-bit Arm machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-aarch64_be : Simulate a 64-bit big-endian Arm machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-aarch64_be : Simulate a 64-bit big-endian Arm machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-alpha : Simulate a DEC Alpha machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-alpha : Simulate a DEC Alpha machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-arm : Simulate a 32-bit Arm machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-armeb : Simulate a 32-bit big-endian Arm machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-armeb : Simulate a 32-bit big-endian Arm machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-bridge-helper : QEMU Network Utility [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-edid : QEMU EDID test tool [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-hexagon : Simulate a Qualcomm Hexagon DSP [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-hexagon : Simulate a Qualcomm Hexagon DSP [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-hppa : Simulate a HP PA-RISC machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-hppa : Simulate a HP PA-RISC machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-i386 : Simulate a 32-bit x86 machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-i386 : Simulate a 32-bit x86 machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-img : QEMU Disk Image utility [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-io : QEMU Disk exerciser [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-loongarch64 : Simulate a 64-bit LoongArch machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-loongarch64 : Simulate a 64-bit LoongArch machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-m68k : Simulate a Motorola 68000 series machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-m68k : Simulate a Motorola 68000 series machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-microblaze : Simulate a Xilinx MicroBlaze big-endian machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-microblaze : Simulate a Xilinx MicroBlaze big-endian machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-microblazeel : Simulate a Xilinx MicroBlaze little-endian machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-microblazeel : Simulate a Xilinx MicroBlaze little-endian machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-mips : Simulate a 32-bit big-endian MIPS machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-mips : Simulate a 32-bit big-endian MIPS machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-mips64 : Simulate a 64-bit big-endian MIPS machin [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-mips64 : Simulate a 64-bit big-endian MIPS machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-mips64el : Simulate a 64-bit little-endian MIPS machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-mips64el : Simulate a 64-bit little-endian MIPS machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-mipsel : Simulate a 32-bit little-endian MIPS machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-mipsel : Simulate a 32-bit little-endian MIPS machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-mipsn32 : Simulate a 32-bit big-endian MIPS machine with N32 ABI [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-mipsn32 : Simulate a 32-bit big-endian MIPS machine with N32 ABI [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-mipsn32el : Simulate a 32-bit little-endian MIPS machine with N32 ABI [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-mipsn32el : Simulate a 32-bit little-endian MIPS machine with N32 ABI [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-nbd : QEMU Disk Network Block Device Server [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-or1k : Simulate an OpenRISC 1000 machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-or1k : Simulate an OpenRISC 1000 machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-ppc : Simulate a 32-bit PowerPC machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-ppc : Simulate a 32-bit PowerPC machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-ppc64 : Simulate a 64-bit big-endian PowerPC machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-ppc64 : Simulate a 64-bit big-endian PowerPC machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-ppc64le : Simulate a 64-bit little-endian PowerPC machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-ppc64le : Simulate a 64-bit little-endian PowerPC machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-pr-helper : QEMU persistent reservation helper [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-riscv32 : Simulate a 32-bit RISC-V machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-riscv32 : Simulate a 32-bit RISC-V machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-riscv64 : Simulate a 64-bit RISC-V machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-riscv64 : Simulate a 64-bit RISC-V machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-s390x : Simulate an IBM System z machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-s390x : Simulate an IBM System z machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-sh4 : Simulate a SuperH SH-4 little-endian machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-sh4 : Simulate a SuperH SH-4 little-endian machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-sh4eb : Simulate a SuperH SH-4 big-endian machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-sh4eb : Simulate a SuperH SH-4 big-endian machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-sparc : Simulate a 32-bit SPARC machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-sparc : Simulate a 32-bit SPARC machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-sparc32plus : Simulate a 32-bit SPARC V8+ machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-sparc32plus : Simulate a 32-bit SPARC V8+ machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-sparc64 : Simulate a 64-bit SPARC machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-sparc64 : Simulate a 64-bit SPARC machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-storage-daemon : QEMU storage daemon [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-aarch64 : Simulate a Full System 64-bit Arm machine. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-alpha : Simulate a Full System DEC Alpha machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-arm : Simulate a Full System 32-bit Arm machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-avr : Simulate a Full System AVR 8 bit based machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-hppa : Simulate a Full System HP PA-RISC machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-i386 : Simulate a Full System 32-bit x86 machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-loongarch64 : Simulate a Full System 64-bit LoongArch machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-m68k : Simulate a Full System Motorola 68000 series machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-microblaze : Simulate a Full System Xilinx MicroBlaze big-endian machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-microblazeel : Simulate a Full System Xilinx MicroBlaze little-endian machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-mips : Simulate a Full System 32-bit big-endian MIPS machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-mips64 : Simulate a Full System 64-bit big-endian MIPS machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-mips64el : Simulate a Full System 64-bit little-endian MIPS machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-mipsel : Simulate a Full System 32-bit little-endian MIPS machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-or1k : Simulate a Full System OpenRISC 1000 machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-ppc : Simulate a Full System 32-bit PowerPC machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-ppc64 : Simulate a Full System 64-bit big-endian PowerPC machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-riscv32 : QEMU System/User mode Emulation & Virtualization binaries. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-riscv64 : Simulate a Full System 64-bit RISC-V machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-rx : Simulate a Full System RX target (GDB simulator) [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-s390x : Simulate a Full System IBM System z machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-sh4 : Simulate a Full System SuperH SH-4 little-endian machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-sh4eb : Simulate a Full System SuperH SH-4 big-endian machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-sparc : Simulate a Full System 32-bit SPARC machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-sparc64 : Simulate a Full System 64-bit SPARC machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-tricore : Simulate a Full System Tricore micro-controller [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-x86_64 : Simulate a Full System 64-bit x86 machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-xtensa : Simulate a Full System Tensilica Xtensa little-endian machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-system-xtensaeb : Simulate a Full System Tensilica Xtensa big-endian machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-vmsr-helper : QEMU persistent reservation utility [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-x86_64 : Simulate a 64-bit x86 machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-x86_64 : Simulate a Full System 64-bit x86 machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-xtensa : Simulate a Full System Tensilica Xtensa little-endian machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-xtensa : Simulate a Tensilica Xtensa little-endian machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-xtensaeb : Simulate a Full System Tensilica Xtensa big-endian machine [qemu]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qemu-xtensaeb : Simulate a Tensilica Xtensa big-endian machine [qemu-user]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qman : Modern manual page viewer for Terminals. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qp : Query installed packages from any ecosystem. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qq : Interoperable configuration format Transcoder with jq query syntax. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qsreplace : Accept URLs on stdin, replace all query string values with a user-supplied value. To install it use the --soarpkg flag or the .soarpkg extension.
◆ qsv : Blazing-fast Data-Wrangling toolkit. To install it use the --soarpkg flag or the .soarpkg extension.
◆ quickcert : A better way of querying certificate transparency logs. To install it use the --soarpkg flag or the .soarpkg extension.
◆ quotes-escaper : Escape Quotes (Single. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ra6 : Assess attack vectors based on ICMPv6 Router Advertisement messages [ipv6toolkit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rage : Simple, modern, and secure file encryption tool, using the age format. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rage-keygen : Simple, modern, and secure file encryption tool, using the age format. To install it use the --soarpkg flag or the .soarpkg extension.
◆ raidautorun : Tell the kernel to automatically search and start RAID arrays [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ raidautorun : Tell the kernel to automatically search and start RAID arrays [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ranlib : Generates an index to the contents of an archive [bintools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ranlib : Generates an index to the contents of an archive [binutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rapwp : Generate payload-embedded URLs [lit-bb-hack-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rarp : Manipulate the system RARP table. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rate-limit-checker : Check whether a domain has a rate limit enabled. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rattler-build : Universal package builder for Windows, macOS and Linux based on Conda. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rcat : Rustcat(rcat), A modern port listener and reverse shell. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rclone : A program to sync files to and from various cloud storage providers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rd6 : Assess attack vectors based on ICMPv6 Router Advertisement messages [ipv6toolkit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rdap : ICANN implementation of the Registry Data Access Protocol (RDAP). To install it use the --soarpkg flag or the .soarpkg extension.
◆ rdap-client : RDAP command line in GO. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rdate : Get and possibly set the system date and time from a remote HOST [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rdate : Get and possibly set the system date and time from a remote HOST [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rdate : Set the system's date from a remote host [outils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rdev : Print the device node associated with the filesystem mounted at '/' [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rdev : Print the device node associated with the filesystem mounted at '/' [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rdma : Manage Remote Direct Memory Access (RDMA) devices and connections for high-performance networking [iproute2]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rdsquashfs : Examine/Uncompress SquashFS filesystems [squashfs-tools-ng]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ readahead : Pre-read files into page cache [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ readahead : Pre-read files into page cache [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ readelf : Displays information from any ELF format object file [bintools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ readelf : Displays information from any ELF format object file [binutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ reader : A minimal command line reader offering better readability of web pages on the CLI. To install it use the --soarpkg flag or the .soarpkg extension.
◆ readlink : Display the value of a symlink [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ readlink : Display the value of a symlink [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ readlink : Print resolved symbolic links or canonical file names [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ readlink : Print resolved symbolic links or canonical file names [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ readlink : Print resolved symbolic links or canonical file names [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ readlink : Print resolved symbolic links or canonical file names [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ readprofile : Display kernel profiling information [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ readprofile : Read kernel profiling information [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ readprofile : Read kernel profiling information [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ realpath : Print the resolved path [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ realpath : Print the resolved path [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ realpath : Print the resolved path [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ realpath : Return the absolute pathnames of given argument [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ realpath : Return the absolute pathnames of given argument [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ realpath : Return the absolute pathnames of given argument [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ reboot : Reboots the system [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ reboot : Reboots the system [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rebos : Tool to mimic NixOS's repeatability, managing packages and services on any Linux distro. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rebuildctl : Independent verification system of binary packages. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rebuilderd : Independent verification system of binary packages. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rebuilderd-worker : Independent verification system of binary packages. To install it use the --soarpkg flag or the .soarpkg extension.
◆ recode-sr-latin : Convert Serbian text from Cyrillic to Latin script [gettext]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ redguard : RedGuard is a C2 front flow control tool To Avoid Blue Teams,AVs,EDRs check. To install it use the --soarpkg flag or the .soarpkg extension.
◆ redirfd : Redirect a given file descriptor to a file & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ redirfd : Redirect a given file descriptor to a file & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ redive : Trace URL redirections in the terminal.. To install it use the --soarpkg flag or the .soarpkg extension.
◆ reformime : Parse MIME-encoded message [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ reformime : Parse MIME-encoded message [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ regbot : A scripting tool on top of the regclient [regclient]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ regctl : Client interface to interacting with registries [regclient]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ region-restriction-check : A bash script to check the status of your IP on various geo-restricted Services. To install it use the --soarpkg flag or the .soarpkg extension.
◆ regsync : OCI Image mirroring tool to copy images between two locations [regclient]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rekor-cli : Software Supply Chain Transparency Log. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rekor-server : Software Supply Chain Transparency Log. To install it use the --soarpkg flag or the .soarpkg extension.
◆ relf : a program that shows you the sections of an ELF file and can also guess what the unknown sections contain. To install it use the --soarpkg flag or the .soarpkg extension.
◆ relic : A tool for adding Digital Signatures to operating system packages for Linux and Windows. To install it use the --soarpkg flag or the .soarpkg extension.
◆ remaining : Subtract words from a search wordlist using a base wordlist, ideal for combinator attacks [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ remove-shell : Remove SHELLs from /etc/shells [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ remove-shell : Remove SHELLs from /etc/shells [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ removehost : Extract queries without protocol and host [lit-bb-hack-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ removepro : Extract URLs without protocols [lit-bb-hack-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rename : Rename files [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ renice : Alter the priority of running processes [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ renice : Change priority of running processes [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ renice : Change priority of running processes [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ renice : Change priority of running processes [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ reptyr : Reparent a running program to a new terminal. To install it use the --soarpkg flag or the .soarpkg extension.
◆ req-exclude : Excludes passwords that match specific policy criteria [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ req-include : Filter passwords based on specified policy criteria, including lowercase, uppercase, and digit requirements [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rescope : A scope generation tool for Burp Suite & ZAP. To install it use the --soarpkg flag or the .soarpkg extension.
◆ reset : Reset the screen [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ reset : Reset the screen [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ resize : Resize the screen [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ resize : Resize the screen [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ resize2fs : ext2/ext3/ext4 file system resizer [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ resize2fs : ext2/ext3/ext4 file system resizer [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ resizepart : Resize a Partition [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ resizepart : Tell the kernel about the new size of a partition [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ resolve-march-native : Tool to determine what GCC flags -march=native would resolve into. To install it use the --soarpkg flag or the .soarpkg extension.
◆ restic : Fast, secure, efficient backup program. To install it use the --soarpkg flag or the .soarpkg extension.
◆ resto : Send pretty HTTP & API requests with TUI. To install it use the --soarpkg flag or the .soarpkg extension.
◆ resume : Resume a suspended job [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ resume : Resume a suspended job [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rev : Reverse lines characterwise [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rev : Reverse lines of input [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rev : Reverse lines of input [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rev : Reverse lines of input [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ reverse-ssh-client : SSH based reverse shell. To install it use the --soarpkg flag or the .soarpkg extension.
◆ reverse-ssh-server : SSH based reverse shell. To install it use the --soarpkg flag or the .soarpkg extension.
◆ reviewdog : Automated code review tool integrated with any code analysis tools regardless of programming language. To install it use the --soarpkg flag or the .soarpkg extension.
◆ revive : Faster, stricter, configurable, extensible, and beautiful drop-in replacement for golint. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rewritepkg : Takes a Go command's source and rewrites it to be a u-root busybox compatible library package. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rfkill : Tool for enabling and disabling wireless devices [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rg : Recursive & Line-Oriented Search Tool, combining The Silver Searcher (ag) with the raw speed of grep. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rga : Ripgrep, but also search in PDFs, E-Books, Office documents, zip, tar.gz, and more. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rga-fzf : Ripgrep, but also search in PDFs, E-Books, Office documents, zip, tar.gz, and more. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rga-fzf-open : Ripgrep, but also search in PDFs, E-Books, Office documents, zip, tar.gz, and more. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rga-preproc : Ripgrep, but also search in PDFs, E-Books, Office documents, zip, tar.gz, and more. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rhash : Utility for calculation and verification of hash sums and magnet links. To install it use the --soarpkg flag or the .soarpkg extension.
◆ riff : A wrapper around diff that highlights which parts of lines have changed. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ripgrep : Recursive & Line-Oriented Search Tool, combining The Silver Searcher (ag) with the raw speed of grep. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ripgrep : Recursive & Line-Oriented Search Tool, combining The Silver Searcher (ag) with the raw speed of grep. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rldd : Print Shared Object Dependencies. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rli : Compares a single file against another file(s) and removes all duplicates [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rli2 : Compares a single file against another file(s) and removes all duplicates, Enhanced version [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rlwrap : Wrapper to allow the editing of keyboard commands. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rm : Remove (unlink) the FILE(s) [bsdutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rm : Remove (unlink) the FILE(s) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rm : Remove (unlink) the FILE(s) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rm : Remove (unlink) the FILE(s) [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rm : Remove files or directories [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rm : Remove files or directories [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rm : Remove files or directories [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rm : Remove files or directories [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rmdir : Remove empty directories [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rmdir : Remove empty directories [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rmdir : Remove empty directories [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rmdir : Remove empty directories [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rmdir : Remove the DIRECTORY [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rmdir : Remove the DIRECTORY [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rmdir : Remove the DIRECTORY [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rmmod : Remove a module from the Linux Kernel [kmod]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rmmod : Removes a module from the Linux kernel [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rmmod : Removes a module from the Linux kernel [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rmt : Remote magnetic tape server [tar]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rngseed : Manipulates the Linux kernel's entropy pool [s6-linux-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rnr : A CLI tool to rename files and directories that supports regex. To install it use the --soarpkg flag or the .soarpkg extension.
◆ robotstxt : Extract paths from robots.txt [lit-bb-hack-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ roboxtractor : Extract endpoints marked as disallow in robots files to generate wordlists. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rootlessctl : A utility for managing rootless container environments [rootlesskit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rootlesskit : A user-space implementation of a minimal namespace and network configuration tool [rootlesskit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rootlesskit-docker-proxy : A proxy for Docker daemons to work seamlessly in rootless environments [rootlesskit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ropr : Blazing fast multithreaded ROP Gadget finder. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rospo : Simple, reliable, persistent ssh tunnels with embedded ssh server. To install it use the --soarpkg flag or the .soarpkg extension.
◆ route : Edit kernel routing tables [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ route : Edit kernel routing tables [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ route : Show/Manipulate the IP routing table. To install it use the --soarpkg flag or the .soarpkg extension.
◆ routedns : DNS stub resolver, proxy and router. To install it use the --soarpkg flag or the .soarpkg extension.
◆ routel : List routes with pretty output format [iproute2]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rpfu : Remove standard ports from URLs [lit-bb-hack-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rpm : Manipulate RPM packages [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rpm : Manipulate RPM packages [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rpm2cpio : Output a cpio archive of the rpm file [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rpm2cpio : Output a cpio archive of the rpm file [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rqbit : A bittorrent command line client and server. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rs : Reshape a data array [outils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rs6 : Assess attack vectors based on ICMPv6 Router Solicitation messages [ipv6toolkit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rsacracker : RSA cracker with support for RSA, X509, OPENSSH in PEM and DER formats. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rsbkb : Busybox like Encoders/decoders, CTF and reverse engineering helpers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rshijack : TCP connection hijacker. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rsync : An open source utility that provides fast incremental File Transfer. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rtacct : Display and monitors the accounting information of routing tables [iproute2]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rtcwake : Enter a system sleep state until a specified wakeup time [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rtcwake : Enter a system sleep state until specified wakeup time [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rtcwake : Enter a system sleep state until specified wakeup time [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rtmon : Listen to and monitor RTnetlink [iproute2]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rtstat : Display routing table statistics, including the number of routes and related metrics [iproute2]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ruff : Extremely fast Python linter and code formatter. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rules_optimize : Optimize Hashcat Rules [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ run-init : Executes the specified program as the initial process in a new system environment [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ run-init : Executes the specified program as the initial process in a new system environment [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ run-parts : Run a bunch of scripts in a directory [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ run-parts : Run a bunch of scripts in a directory [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ runblock : Write execline commands in the execline language [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ runblock : Write execline commands in the execline language [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ runcon : Run command with specified security context [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ runcon : Run command with specified security context [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ runcron : simple, safe, container-friendly cron alternative. To install it use the --soarpkg flag or the .soarpkg extension.
◆ runiq : An efficient way to filter duplicate lines from input (Rust uniq). To install it use the --soarpkg flag or the .soarpkg extension.
◆ runlevel : Find the current and previous system runlevel [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ runlevel : Find the current and previous system runlevel [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ runme : Execute commands inside/from runbooks, docs, and READMEs. To install it use the --soarpkg flag or the .soarpkg extension.
◆ runprog : Safely run program by unshare/ptrace/pre-forked containers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ runsv : Start and monitor a service and optionally an appendant log service [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ runsv : Start and monitor a service and optionally an appendant log service [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ runsvdir : Start a runsv process for each subdirectory [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ runsvdir : Start a runsv process for each subdirectory [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ runuser : Run <command> with the effective user ID and group ID of <user> [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ruplacer : Find and replace text in source files. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rush : A cross-platform command-line tool for executing jobs in parallel. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rust-parallel : Run commands in parallel (parallel. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rustcat : Rustcat(rcat), A modern port listener and reverse shell. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rustscan : The modern,fast,smart and effective port scanner. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rustypaste : Minimal file upload/pastebin service. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rview : Vim in View Mode with restrictions [vim]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rvim : Vim in Read-Only with restrictions [vim]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rx : Receive a file using the xmodem protocol [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rx : Receive a file using the xmodem protocol [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ rye : A Hassle-Free Python Experience. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ryelang : Interpreter for RyeLang. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s3scanner : A tool to find open S3 buckets in AWS or other cloud providers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s3sync : Really fast sync tool for S3. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s5cmd : Parallel S3 and local filesystem execution tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-accessrules-cdb-from-fs : Compile a directory containing a ruleset into a CDB file [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-accessrules-fs-from-cdb : Decompile a CDB file containing a ruleset into a directory [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-applyuidgid : Execute a program with reduced privileges with specified UID/GID for the process [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-basename : Prints the basename of a pathname [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-cat : Transfers data from stdin to stdout, until it receives EOF or gets killed [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-chmod : Changes the permissions of a file [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-chown : Changes the owner and/or group of a file [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-chroot : Executes a program in a chroot jail [s6-linux-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-cleanfifodir : Clean up an s6-fifodir, removing any unused or unnecessary FIFO files [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-clock : Gets/Sets the system clock [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-clockadd : Adjusts the system clock depending on the input provided by a time client [s6-networking]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-clockview : Prints the time discrepancy between the local system clock and the time provided by a time server [s6-networking]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-connlimit : Control the number of client connections to TCP and Unix domain sockets [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-cut : Prints selected portions of the lines of its input [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-dirname : Prints the dirname of a path [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-dns-hosts-compile : Converts /etc/hosts into a CDB file for efficient use by s6-dns programs [s6-dns]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-dnsip : Finds both the IPv6 and IPv4 addresses associated to a domain name [s6-dns]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-dnsip4 : Finds the IPv4 addresses associated to a domain name [s6-dns]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-dnsip4-filter : Reads domain names from stdin and outputs their IPv4 addresses [s6-dns]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-dnsip6 : Finds the IPv6 addresses associated to a domain name [s6-dns]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-dnsip6-filter : Reads domain names from stdin and outputs their IPv6 addresses [s6-dns]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-dnsmx : Finds the MX information associated to a domain name [s6-dns]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-dnsname : Finds the name associated to an IPv4 or IPv6 address [s6-dns]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-dnsname-filter : Reads IPs from stdin and outputs their domain names [s6-dns]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-dnsns : Finds the relevant nameservers providing data for a domain [s6-dns]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-dnsq : Performs non-recursive DNS queries and prints the answer packet and optional debug info [s6-dns]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-dnsqr : Resolves DNS queries, printing the answer packet and optional debug information [s6-dns]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-dnsqualify : Qualifies a domain using the domain and search rules in /etc/resolv.conf, outputting one FQDN per line [s6-dns]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-dnssoa : Finds the SOA information associated to a domain [s6-dns]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-dnssrv : Finds the SRV information associated to a service name, protocol name and domain name [s6-dns]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-dnstxt : Finds the TXT information associated to a domain name [s6-dns]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-dumpenv : Stores environment variables into a directory [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-echo : Writes its arguments to stdout [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-env : Prints the current environment or modifies the environment before running a program [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-envdir : Change the environment and then executes another program [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-envuidgid : Set the UID, GID, and GIDLIST environment variables before executing a program [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-expr : Evaluates an expression and writes the result to stdout [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-false : Returns 1 [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-fdholder-daemon : Store/Retrieve file descriptors for clients via a Unix domain socket [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-fdholder-delete : Delete a file descriptor stored with a given ID using an s6-fdholderd daemon [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-fdholder-getdump : Retrieve the entire state, including stored file descriptors, ID, and expiration dates of an s6-fdholderd daemon [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-fdholder-list : List the ID of currently held file descriptors of an s6-fdholderd daemon [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-fdholder-retrieve : Retrieve a FD & execute a program with the FD as the program's standard input [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-fdholder-setdump : Dump a set of file descriptors with IDs and expiration dates into an s6-fdholderd daemon [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-fdholder-store : Connect to an s6-fdholderd daemon and store an open file descriptor with the daemon [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-fdholder-transferdump : Connect to one s6-fdholderd daemon to another and transfer the contents [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-fdholderd : A server that holds file descriptors as part of the s6-fdholder-daemon system [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-fghack : Detect bugs in programs by monitoring unexpected file descriptor writes and supervise daemons with improper descriptor handling [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-format-filter : Processes lines according to its arguments, and prints the result [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-freeramdisk : Frees the memory occupied by a RAM disk [s6-linux-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-ftrig-listen : Subscribe to multiple s6-fifodir instances, spawn a program, and wait for specific events to occur on the fifodirs [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-ftrig-listen1 : Subscribe to a single s6-fifodir, wait for events, and ensure the listener is active before the process sends notifications [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-ftrig-notify : Send a series of events as a message to all listeners in a specified s6-fifodir [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-ftrig-wait : Subscribe to an s6-fifodir and wait for a specified pattern of events to occur on it [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-ftrigrd : Manage a set of subscriptions to s6-fifodir [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-getservbyname : CLI to the getservbyname() function, converting a service name and protocol into a port number [s6-networking]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-grep : Matches its input against a pattern [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-head : Prints the first lines of its input files [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-hiercopy : Copies a directory structure recursively [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-hostname : Gets/Sets the machine hostname [s6-linux-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-ident-client : Client application that performs IDENT (RFC 1413) queries [s6-networking]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-instance-control : Send commands to a running instance of an instanced service [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-instance-create : Creates a new instance of a currently supervised instanced service [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-instance-delete : Deletes an existing instance of a currently supervised instanced service [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-instance-list : Print a list of all currently created instances of an instanced service [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-instance-maker : Creates a service directory implementing an instanced service [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-instance-status : Print the status of instanced services in human-readable/programmatically parsable output [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-ioconnect : Perform full-duplex data transmission between two sets of open file descriptors [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-ipcclient : UCSPI client tool for Unix domain sockets [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-ipcserver : UCSPI server tool for Unix domain sockets [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-ipcserver-access : Access Control Tool for Unix domain sockets [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-ipcserver-socketbinder : Binds a Unix domain socket, then executes a program [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-ipcserverd : The Serving part of the s6-ipcserver [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-linkname : Gives the content of a symbolic link or resolves a path [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-linux-init : A program that is meant to run as pid 1 as a stage 1 init [s6-linux-init]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-linux-init-echo : Writes its arguments to stdout [s6-linux-init]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-linux-init-hpr : Triggers a software or (with -f) hardware shutdown, typically via halt, poweroff, or reboot [s6-linux-init]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-linux-init-logouthookd : Cleans up its client's utmp record when it dies [s6-linux-init]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-linux-init-maker : Generates a directory with an /sbin/init script and necessary files to boot and set up an s6 infrastructure [s6-linux-init]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-linux-init-nuke : Kills all processes immediately [s6-linux-init]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-linux-init-shutdown : Triggers the system shutdown procedure [s6-linux-init]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-linux-init-shutdownd : The daemon that manages the shutdown procedure for an s6-linux-init installation [s6-linux-init]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-linux-init-telinit : Changes runlevels, i.e. changes the system state [s6-linux-init]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-linux-init-umountall : Unmounts all filesystems [s6-linux-init]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-ln : Creates a link to a file [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-log : Reliable logging with automated log rotation with full POSIX Regex [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-logwatch : Watches the current file of a logdir, printing it in real time [s6-linux-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-ls : Lists the contents of a directory [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-maximumtime : Executes a program with a time limit [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-mkdir : Creates directories [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-mkfifo : Creates FIFOs, a.k.a. named pipes [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-mkfifodir : Creates fifodir, belonging to the current user [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-mount : Mounts filesystems [s6-linux-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-nice : Executes into a program with an altered nice value [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-notifyoncheck : Polls a service's readiness and reports it via the s6 notification mechanism [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-nuke : Sends signals to every process it is allowed to send [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-pause : Blocks until it is killed [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-permafailon : Checks a service's death tally and, if it matches a pattern, causes permanent failure, stopping the supervisor from restarting it [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-pivotchroot : Performs a pivot_root system call, then changes root to the new root filesystem and executes a program [s6-linux-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-printenv : Prints its environment variables [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-ps : Shows a list of processes on the system [s6-linux-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-quote : Quotes a string and writes it to stdout [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-quote-filter : Acts as a filter, quoting lines it reads on stdin and writing the quoted lines to stdout [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-randomip : Generates random IP addresses and prints them to its standard output, one per line [s6-dns]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-rc : Machine state manager, A program to manage the live state of a machine [s6-rc]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-rc-bundle : Add/Delete bundles from a compiled service database without having to recompile it from source [s6-rc]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-rc-compile : A service database compiler [s6-rc]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-rc-db : Analyze a compiled service database & print the extracted information [s6-rc]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-rc-dryrun : A command wrapper and nullifier, roughly equivalent to the Unix echo command [s6-rc]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-rc-format-upgrade : Updates a live directory's s6-rc database to the new format after an upgrade [s6-rc]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-rc-init : Initialization tool for the s6-rc system run at boot time as root [s6-rc]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-rc-update : Switches service databases and updates the live state without rebooting [s6-rc]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-rename : Atomically renames a file [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-rmrf : Removes a file or directory tree entirely [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-seq : Prints a sequence of numbers [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-setlock : Takes a lock on a file, then executes into another program [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-setsid : Runs a program as a new session leader or new foreground/background process group [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-setuidgid : Executes a program as another user specified by the account argument [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-sleep : Sleeps for a given time, then optionally executes a program [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-sntpclock : A small SNTP client [s6-networking]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-socklog : A minimal syslog daemon that logs messages via Unix or TCP sockets [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-softlimit : Modifies process limits (e.g., memory, file size, stack) before executing another program [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-sort : Sorts its input [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-sudo : Connects to a Unix socket, forwarding FDs, arguments, and environment with different privileges [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-sudoc : Interacts with s6-sudod via a Unix socket, sending arguments and descriptors [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-sudod : Forks a program after receiving arguments and descriptors from s6-sudoc over a Unix socket [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-supervise : Monitors a long-lived process or service, ensuring it stays alive [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-svc : Sends commands to a running s6-supervise process [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-svdt : Prints the death tally of a service, showing when the process died and the cause [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-svdt-clear : Clears the recorded death tally of a service being supervised at the specified servicedir [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-svlink : Integrate service directories into an existing service manager sequence and eliminate race conditions [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-svlisten : Runs a program, blocking until a supervised service goes up or down [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-svlisten1 : Runs a program, blocking until a supervised service goes up or down [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-svok : Checks if a given s6-service-directory is being monitored by an s6-supervise process [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-svperms : Manages service permissions for reading, controlling, and subscribing [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-svscan : Starts and monitors a collection of s6-supervise processes [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-svscanctl : Sends commands to a running s6-svscan process [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-svstat : Prints a summary of the state of a process monitored by s6-supervise [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-svunlink : Integrates service directories and eliminates race conditions [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-svwait : Monitors one or more s6-service-directory directories, waiting for a specified state [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-swapoff : Deactivates swap partitions [s6-linux-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-swapon : Activates swap partitions [s6-linux-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-sync : Flushes all the dirty system buffers, and blocks until they're clean [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-tai64n : Prepend each stdin line with a TAI64N timestamp [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-tai64ndiff : Acts as a filter, reading from stdin and writing to stdout [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-tai64nlocal : Convert lines starting with a TAI64N timestamp into a human-readable local date and time [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-taiclock : A client for the TAICLOCK protocol [s6-networking]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-taiclockd : A server for the TAICLOCK protocol [s6-networking]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-tail : Prints a file or its stdin after a certain number of bytes or lines [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-tcpclient : UCSPI client tool for INET domain sockets [s6-networking]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-tcpserver : UCSPI tool for TCP connections, i.e. a super-server [s6-networking]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-tcpserver-access : TCP access control tool [s6-networking]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-tcpserver-socketbinder : Binds an INET domain socket to an IPv4/IPv6 address and port, then executes a program [s6-networking]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-tcpserverd : The serving part of the s6-tcpserver [s6-networking]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-tlsc : Establishes a TLS/SSL client connection over an existing TCP connection, then execs into an application [s6-networking]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-tlsc-io : Client-side program in s6-networking that performs cryptography for s6-tlsc [s6-networking]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-tlsclient : UCSPI client tool for TLS/SSL connections over INET domain sockets [s6-networking]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-tlsd : Performs the server side of TLS/SSL connection over an existing TCP connection, then execs into an application [s6-networking]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-tlsd-io : Client-side program in s6-networking that performs cryptography for s6-tlsd [s6-networking]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-tlsserver : UCSPI server tool for TLS/SSL connections over INET domain sockets [s6-networking]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-touch : Changes the modification and access times of a file, creating it if it does not exist [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-true : Returns 0 [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-ucspitlsc : Establishes communication channels according to the UCSPI-TLS protocol, then execs into an application [s6-networking]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-ucspitlsd : Server Side program for s6-ucspitlsc [s6-networking]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-umount : Unmounts filesystems [s6-linux-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-uniquename : Creates a guaranteed unique file name and prints it to stdout [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-unquote : Unquotes a quoted string and writes it to stdout [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-unquote-filter : Acts as a filter, reading quoted strings on stdin, unquoting them and writing the results to stdout [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-update-symlinks : Links the content of several similar directory trees under a single tree [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ s6-usertree-maker : Creates an s6-service-directory to run an s6-svscan process owned by a specified user, in a user-specific s6-scan-directory [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sake : Task runner for local and remote hosts via ssh & more. To install it use the --soarpkg flag or the .soarpkg extension.
◆ samurai : Ninja-compatible build tool written in C. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sandwine : Run Windows apps with Wine and bwrap/bubblewrap isolation. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sanicdns : Recursive DNS Resolver. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sanitize-filename : A basic filename sanitizer, based on Node's sanitize-filename. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sastsweep : Identify Vulnerabilities in Open Source Codebases at Scale. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sbctl : Secure Boot key manager. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sbuild-linter : SBUILD linter that lints SBUILD Recipes [sbuilder]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ scan6 : IPv6 Scanning Tool [ipv6toolkit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ scanelf : User-space utility to scan ELF files [pax-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ scanelf : User-space utility to scan ELF files [pax-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ scanmacho : User-space utility to scan Mach-O files [pax-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ scanmacho : User-space utility to scan Mach-O files [pax-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sccache : Like ccache with cloud storage support. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sccache : Like ccache with cloud storage support. To install it use the --soarpkg flag or the .soarpkg extension.
◆ scilla : DNS, subdomain, port, directory enumeration tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ scons : A Python replacement for Makefile-based build systems. To install it use the --soarpkg flag or the .soarpkg extension.
◆ scopegen : Generate .scope compatible format for Inscope/ScopeView. To install it use the --soarpkg flag or the .soarpkg extension.
◆ scp : OpenSSH secure file copy [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ scp : OpenSSH secure file copy [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ scp : Secure remote copy [dropbear]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ scp : Secure remote copy [dropbear]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ scrape-ct-log : Quickly and reliably Download entries from a Certificate Transparency log. To install it use the --soarpkg flag or the .soarpkg extension.
◆ screener : Take screenshots of webpages. To install it use the --soarpkg flag or the .soarpkg extension.
◆ screenfetch : Generate ASCII art with terminal, shell, and OS info. To install it use the --soarpkg flag or the .soarpkg extension.
◆ script : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ script : Make a typescript of a terminal session [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ script : Record a terminal session [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ script : Record a terminal session [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ script6 : Make complex IPv6 tasks easy [ipv6toolkit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ scriptlive : Execute terminal typescript [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ scriptreplay : Play back terminal typescripts, using timing information [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ scriptreplay : Play back typescripts, using timing information [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ scriptreplay : Play back typescripts, using timing information [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sdiff : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sdns : A high-performance, recursive DNS resolver server with DNSSEC support. To install it use the --soarpkg flag or the .soarpkg extension.
◆ secretmemfd : Create a secret memory file descriptor, duplicate it to a given file descriptor & execute a command [emanutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sed : SEd (stream editor) is a Unix utility that parses and transforms text. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sed : Stream editor for filtering and transforming text [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sed : Stream editor for filtering and transforming text [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sed : Stream editor for filtering and transforming text [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ seedrng : Seed the random number generator [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ seedrng : Seed the random number generator [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ seekablepipe : Turns the reading end of a pipe into a seekable file descriptor, using a temporary file [s6-portable-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sendmail : Read email from stdin and send it [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sendmail : Read email from stdin and send it [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sendme : Tool to send files and directories. To install it use the --soarpkg flag or the .soarpkg extension.
◆ seprule : Generate unique combinations of characters with a rule and separator [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ seq : Print a sequence of numbers [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ seq : Print a sequence of numbers [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ seq : Print a sequence of numbers [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ seq : Print a sequence of numbers [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ seq : Print numbers from FIRST to LAST, in steps of IN [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ seq : Print numbers from FIRST to LAST, in steps of IN [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ seq : Print numbers from FIRST to LAST, in steps of IN [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sequin : Human-readable ANSI sequences. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sessionprobe : A Tool to Evaluate User Privileges of a session token by checking Access across a list of URLs. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setarch : Change reported architecture for program [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setarch : Change reported architecture for program [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setarch : Change reported architecture for program [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setarch : Change the reported architecture and set personality flags [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setcap : Set file capabilities [libcap]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setconsole : Redirect system console output to DEVICE [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setconsole : Redirect system console output to DEVICE [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setfattr : Set file attributes [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setfattr : Set file attributes [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setfont : Load a console font [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setfont : Load a console font [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setkeycodes : Set entries into the kernel's scancode-to-keycode map [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setkeycodes : Set entries into the kernel's scancode-to-keycode map [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setlogcons : Redirect the kernel output to console N [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setlogcons : Redirect the kernel output to console N [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setpriv : Run a program with different Linux privilege settings [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setpriv : Run a program with different privilege settings [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setpriv : Run a program with different privilege settings [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setserial : Set or view serial port configuration [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setserial : Set or view serial port configuration [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setsid : Run PROG in a new session [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setsid : Run PROG in a new session [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setsid : Run PROG in a new session [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setsid : Run a program in a new session [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setterm : Set the attributes of a terminal [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setuidgid : Set uid and gid to account's uid and gid [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ setuidgid : Set uid and gid to account's uid and gid [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sf : Signature-based file format identification. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sfdisk : Display or manipulate a disk partition table [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sftp : OpenSSH secure file transfer [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sftp : OpenSSH secure file transfer [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sftp-server : OpenSSH SFTP server subsystem [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sg : Execute command as different group ID [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sgpt : Postgres CLI with autocompletion and syntax highlighting. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sh : GNU Bourne-Again Shell, the de facto standard shell on Linux. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sh : The ASH Shell [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sh-is-ash : The ASH Shell [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sh-is-ash : The ASH Shell [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha1 : Calculate shasum for a file [outils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha1sum : Compute and check SHA1 message digest [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha1sum : Compute and check SHA1 message digest [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha1sum : Compute and check SHA1 message digest [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha1sum : Print or check SHA1 checksums [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha1sum : Print or check SHA1 checksums [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha224sum : Compute and check SHA224 message digest [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha224sum : Compute and check SHA224 message digest [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha224sum : Compute and check SHA224 message digest [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha256 : Calculate sha256sum for a file [outils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha256sum : Compute and check SHA256 message digest [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha256sum : Compute and check SHA256 message digest [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha256sum : Compute and check SHA256 message digest [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha256sum : Print or check SHA256 checksums [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha256sum : Print or check SHA256 checksums [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha384sum : Compute and check SHA384 message digest [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha384sum : Compute and check SHA384 message digest [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha384sum : Compute and check SHA384 message digest [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha3sum : Compute SHA-3 checksums for files [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha3sum : Compute SHA-3 checksums for files [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha512 : Calculate sha512sum for a file [outils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha512sum : Compute and check SHA512 message digest [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha512sum : Compute and check SHA512 message digest [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha512sum : Compute and check SHA512 message digest [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha512sum : Print or check SHA512 checksums [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sha512sum : Print or check SHA512 checksums [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shar : Create a shell archive [sharutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shcopy : Copy text to clipboard from anywhere using ANSI OSC 52 sequence. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shell2http : HTTP-server to execute shell commands. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shellcheck : Static analysis and lint tool for shell scripts. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shellharden : The corrective bash syntax highlighter. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shellz : Utility for tracking/controlling shells and tunnels. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sherlock : Hunt down social media accounts by username. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shfmt : A shell parser, formatter, and interpreter. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shift : Shift the positional parameters of an execline script [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shift : Shift the positional parameters of an execline script [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shortscan : An IIS short filename enumeration tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shortutil : An IIS short filename enumeration tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shot-scraper : Command-line utility for taking automated screenshots of websites. To install it use the --soarpkg flag or the .soarpkg extension.
◆ showcert : Inspect TLS certificates presented by remote servers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ showkey : Show keys pressed [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ showkey : Show keys pressed [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shpool : Lightweight Persistent session management like tmux. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shred : Overwrite a file to hide its contents [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shred : Overwrite a file to hide its contents [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shred : Overwrite a file to hide its contents, and optionally delete it [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shred : Overwrite a file to hide its contents, and optionally delete it [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shred : Overwrite a file to hide its contents, and optionally delete it [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shred : Overwrite a file to hide its contents, and optionally delete it [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shuf : Generate random permutations [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shuf : Generate random permutations [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shuf : Generate random permutations [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shuf : Generate random permutations [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shuf : Generate random permutations [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shuf : Generate random permutations [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ shuffledns : MassDNS wrapper to resolve and enumerate valid subdomains. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sic : Accessible image processing and conversion from the terminal using image-rs/image. To install it use the --soarpkg flag or the .soarpkg extension.
◆ siegfried : Signature-based file format identification. To install it use the --soarpkg flag or the .soarpkg extension.
◆ signify : Cryptographically sign and verify files [outils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ simplex-chat : A 100% private-by-design chat platform for the command-line. To install it use the --soarpkg flag or the .soarpkg extension.
◆ simplex-chat : A 100% private-by-design chat platform for the command-line. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sing-box : The universal proxy platform. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sish : HTTP(S)/WS(S)/TCP Tunnels to localhost using only SSH. To install it use the --soarpkg flag or the .soarpkg extension.
◆ size : Lists the section sizes of an object or archive file [bintools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ size : Lists the section sizes of an object or archive file [binutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sj : A tool for auditing endpoints defined in exposed (Swagger/OpenAPI) definition files. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sk : Command-line fuzzy finder written in Rust. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sk-tmux : Command-line fuzzy finder written in Rust. To install it use the --soarpkg flag or the .soarpkg extension.
◆ skadnsd : A daemon that asynchronously resolves queries from stdin & sends answers to the client [s6-dns]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ skim : Command-line fuzzy finder written in Rust. To install it use the --soarpkg flag or the .soarpkg extension.
◆ skim-tmux : Command-line fuzzy finder written in Rust. To install it use the --soarpkg flag or the .soarpkg extension.
◆ skopeo : Perform various operations on container images/repositories without dameon & root. To install it use the --soarpkg flag or the .soarpkg extension.
◆ skupper : Virtual Application Network, enabling rich hybrid cloud communication. To install it use the --soarpkg flag or the .soarpkg extension.
◆ skupper-controller : Virtual Application Network, enabling rich hybrid cloud communication. To install it use the --soarpkg flag or the .soarpkg extension.
◆ slabtop : Display kernel slab cache information in real time [procps]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ slattach : Attach a network interface to a serial line. To install it use the --soarpkg flag or the .soarpkg extension.
◆ slattach : Attach network interface(s) to serial line(s) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ slattach : Attach network interface(s) to serial line(s) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sleep : Delay for a specified amount of time [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sleep : Delay for a specified amount of time [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sleep : Delay for a specified amount of time [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sleep : Delay for a specified amount of time [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sleep : Pause for a time [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sleep : Pause for a time [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sleep : Pause for a time [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sling : Extract data from a source storage/database and Load it in a target storage/database. To install it use the --soarpkg flag or the .soarpkg extension.
◆ slirp4netns : User-mode networking for unprivileged network namespaces. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sliver-client : FOSS c2 Adversary Emulation Framework. To install it use the --soarpkg flag or the .soarpkg extension.
◆ smap : Drop-in replacement for Nmap powered by shodan.io. To install it use the --soarpkg flag or the .soarpkg extension.
◆ smemcap : Capture system memory usage details [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ smemcap : Capture system memory usage details [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ smemstat : Report memory usage with shared memory divided proportionally. To install it use the --soarpkg flag or the .soarpkg extension.
◆ snallygaster : Tool to scan for hidden/secret files on HTTP servers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ snooze : Run a command at a particular time. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sns : IIS shortname scanner written in Go. To install it use the --soarpkg flag or the .soarpkg extension.
◆ soar : The Modern, Bloat-Free Distro-Independent Package Manager (GLIBC). To install it use the --soarpkg flag or the .soarpkg extension.
◆ soar : The Modern, Bloat-Free Distro-Independent Package Manager (GLIBC-x86-64-v3). To install it use the --soarpkg flag or the .soarpkg extension.
◆ soar : The Modern, Bloat-Free Distro-Independent Package Manager (GLIBC-x86-64-v4). To install it use the --soarpkg flag or the .soarpkg extension.
◆ soar : The Modern, Bloat-Free Distro-Independent Package Manager (MUSL). To install it use the --soarpkg flag or the .soarpkg extension.
◆ soar : The Modern, Bloat-Free Distro-Independent Package Manager (MUSL-x86-64-v3). To install it use the --soarpkg flag or the .soarpkg extension.
◆ soar : The Modern, Bloat-Free Distro-Independent Package Manager (MUSL-x86-64-v4). To install it use the --soarpkg flag or the .soarpkg extension.
◆ soar-dl : A lightning-fast, feature-rich release download manager with support for GitHub, GitLab and OCI package downloads. To install it use the --soarpkg flag or the .soarpkg extension.
◆ socat : SOcket CAT, a utility for Bidirectional data relay between two data channels [soact]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ soft-serve : Self-hosted Git server for the command line. To install it use the --soarpkg flag or the .soarpkg extension.
◆ softlimit : Set soft resource limits, then run PROG [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ softlimit : Set soft resource limits, then run PROG [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sops : Editor of encrypted files that supports YAML, JSON, ENV, INI and BINARY formats. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sort : Sort lines of text [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sort : Sort lines of text [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sort : Sort lines of text [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sort : Sort lines of text files [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sort : Sort lines of text files [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sort : Sort lines of text files [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sort : Sort lines of text files [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sourcemapper : Extract JavaScript source trees from Sourcemap files. To install it use the --soarpkg flag or the .soarpkg extension.
◆ spc : Tool for building static, standalone PHP runtime with popular extensions. To install it use the --soarpkg flag or the .soarpkg extension.
◆ spectest-interp : Read a Spectest JSON file, and run its tests in the interpreter [binaryen+wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ spectest-interp : Read a Spectest JSON file, and run its tests in the interpreter [wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ speedtest-go : CLI to Test Internet Speed using multiple-servers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ spirv-as : Standalone assembler [spirv-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ spirv-cfg : Control flow graph dumper [spirv-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ spirv-dis : Standalone disassembler [spirv-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ spirv-link : Standalone linker [spirv-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ spirv-lint : Standalone linter [spirv-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ spirv-objdump : Dump information from a SPIR-V binary [spirv-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ spirv-opt : Standalone optimizer [spirv-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ spirv-reduce : Standalone reducer [spirv-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ spirv-val : Standalone validator [spirv-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ spk : A small OSINT/Recon tool to find CIDRs that belong to a specific organization. To install it use the --soarpkg flag or the .soarpkg extension.
◆ split : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ split : Split a file into pieces [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ split : Split a file into pieces [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ split : Split a file into pieces [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ split : Split a file into pieces [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ split : Split a file into pieces [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ splitlen : A dictionary optimizer for the now-deprecated oclHashcat [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sponge : Soak up standard input and write to a file [moreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ spoofdpi : Simple and fast anti-censorship tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sprayshark : Modular G-Suite password sprayer with threading. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sq : Tool to inspect, query, join, import, and export structured data. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sqfs2tar : Turn a SquashFS image into a tar archive [squashfs-tools-ng]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sqfscat : Cat files from a squashfs filesystem to stdout [squashfstools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sqfsdiff : Compare two squashfs images by contents and metadata [squashfs-tools-ng]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sqfstar : Create a squashfs filesystem from a tar archive [squashfstools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sqlc : Generate type-safe Golang code from SQL. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sqlite : A self-contained, serverless, zero-configuration, transactional SQL database engine. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sqlite3 : A self-contained, serverless, zero-configuration, transactional SQL database engine. To install it use the --soarpkg flag or the .soarpkg extension.
◆ squashfuse : Mount a SquashFS archive with FUSE. To install it use the --soarpkg flag or the .soarpkg extension.
◆ squashfuse_ll : Mount a SquashFS archive with low-level FUSE. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ss : Utility to dump/investigate sockets [iproute2]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ssh : OpenSSH remote login client [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ssh : OpenSSH remote login client [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ssh-add : Add private key identities to the OpenSSH authentication agent [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ssh-add : Add private key identities to the OpenSSH authentication agent [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ssh-agent : OpenSSH authentication agent [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ssh-agent : OpenSSH authentication agent [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ssh-copy-id : Use locally available keys to authorize logins on a remote machine [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ssh-key-sync : Sync GitHub SSH keys onto servers authorized_keys files. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ssh-keygen : OpenSSH authentication key utility [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ssh-keygen : OpenSSH authentication key utility [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ssh-keyscan : Gather SSH public keys from servers [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ssh-keyscan : Gather SSH public keys from servers [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ssh-keysign : OpenSSH helper for host-based authentication [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ssh-keysign : OpenSSH helper for host-based authentication [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ssh-pkcs11-helper : OpenSSH helper for PKCS#11 support [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ssh-pkcs11-helper : OpenSSH helper for PKCS#11 support [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ssh-sk-helper : OpenSSH helper for FIDO authenticator support [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ssh-sk-helper : OpenSSH helper for FIDO authenticator support [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sshamble : Unexpected Exposures in SSH. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sshd : OpenSSH daemon [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sshd : OpenSSH daemon [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sshd-session : OpenSSH Privilege Separation [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sshd-session : OpenSSH Privilege Separation [openssh]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sshesame : SSH honeypot, a fake SSH server that lets anyone in and logs their activity. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sshkeys : Get all ssh public keys of a ssh server. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sshpass : Non-interactive SSH authentication utility. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sshpass : Non-interactive SSH authentication utility. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sshpass : sshpass implementation in Go. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sshpiper : Reverse proxy for SSH. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sshs : Terminal user interface for SSH. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sshx : Fast, collaborative live terminal sharing over the web. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sshx-server : Fast, collaborative live terminal sharing over the web. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ssl-client : SSL/TLS client [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ssl-client : SSL/TLS client [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sslocal : Lightweight secured SOCKS5 proxy. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sslscan : Test SSL/TLS enabled services to discover supported cipher suites. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sslsearch : Hunt SSL Certificates for interesting keywords on major cloud service providers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ssmanager : Lightweight secured SOCKS5 proxy. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ssserver : Lightweight secured SOCKS5 proxy. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ssservice : Lightweight secured SOCKS5 proxy. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sstrip : Remove as much as possible from an ELF file. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sstrip.upx : Remove as much as possible from an ELF file (UPXed). To install it use the --soarpkg flag or the .soarpkg extension.
◆ sstrip.upx.ss : Remove as much as possible from an ELF file (UPXed + sstripped). To install it use the --soarpkg flag or the .soarpkg extension.
◆ ssurl : Lightweight secured SOCKS5 proxy. To install it use the --soarpkg flag or the .soarpkg extension.
◆ starship : Minimal, fast and customizable cross-shell prompt. To install it use the --soarpkg flag or the .soarpkg extension.
◆ start-stop-daemon : Search for matching processes, and stop all matching processes [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ start-stop-daemon : Search for matching processes, and stop all matching processes [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ start-stop-daemon : Start and stop system daemon programs [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ stat : Display file (default) or filesystem status [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ stat : Display file (default) or filesystem status [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ stat : Display file (default) or filesystem status [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ stat : Display file or file system status [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ stat : Display file or file system status [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ stat : Display file or file system status [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ stdbuf : Change standard streams initial buffering [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ stdbuf : Run COMMAND, with modified buffering operations for its standard streams [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ steampipe : Dynamically query any cloud, code, logs & more with SQL. To install it use the --soarpkg flag or the .soarpkg extension.
◆ step-cli : Zero trust swiss army knife for working with X509, OAuth, JWT, OATH OTP, etc. To install it use the --soarpkg flag or the .soarpkg extension.
◆ stew : An independent package manager for compiled binaries. To install it use the --soarpkg flag or the .soarpkg extension.
◆ stowaway-admin : Multi-hop proxy tool for security researchers and pentesters. To install it use the --soarpkg flag or the .soarpkg extension.
◆ stowaway-agent : Multi-hop proxy tool for security researchers and pentesters. To install it use the --soarpkg flag or the .soarpkg extension.
◆ strace : A Diagnostic, Debugging and Instructional Userspace utility for Linux. To install it use the --soarpkg flag or the .soarpkg extension.
◆ strace-log-merge : A Diagnostic, Debugging and Instructional Userspace utility for Linux. To install it use the --soarpkg flag or the .soarpkg extension.
◆ streamlink : Program to pipe streams from services into a video player. To install it use the --soarpkg flag or the .soarpkg extension.
◆ stree : List the directory trees of an AWS S3 Bucket. To install it use the --soarpkg flag or the .soarpkg extension.
◆ stress-ng : Stress test a computer system in various selectable ways. To install it use the --soarpkg flag or the .soarpkg extension.
◆ strings : Display printable strings in a binary file [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ strings : Display printable strings in a binary file [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ strings : Lists printable strings from files [bintools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ strings : Lists printable strings from files [binutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ strip : Discards symbols [bintools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ strip : Discards symbols [binutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ strip-ansi-escapes : Filter ANSI [wezterm]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ strip-bsn : Strip all \\0 bytes from stdin [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ strip-bsr : Strips all \\r bytes from stdin [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ strptime : Parse input from stdin according to one of the given formats FORMATs [dateutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sttr : Cli app to perform various operations on string. To install it use the --soarpkg flag or the .soarpkg extension.
◆ stty : Change and print terminal line settings [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ stty : Change and print terminal line settings [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ stty : Change and print terminal line settings [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ stty : Without arguments, prints baud rate, line discipline, and deviations from stty sane [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ stty : Without arguments, prints baud rate, line discipline, and deviations from stty sane [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ stty : Without arguments, prints baud rate, line discipline, and deviations from stty sane [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ stunner : Test and Exploit STUN, TURN and TURN over TCP servers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ su : Change user id or become root [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ su : Change user id or become root [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ su : Change user id or become root [sudo-rs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ subdog : Collect subdomains from number of different sources to create a list of root subdomains. To install it use the --soarpkg flag or the .soarpkg extension.
◆ subfinder : Fast Modular Subdomain Enumeration tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ subjs : Fetches javascript file from a list of URLS or subdomains. To install it use the --soarpkg flag or the .soarpkg extension.
◆ subs : Grab Domains from files, split them if they're fused, resolve them and more. To install it use the --soarpkg flag or the .soarpkg extension.
◆ subst : Quickie program to do @ substitutions [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ subtrace : Wireshark for Docker containers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ subxtract : Extract TLD (Top-Level-Domains), Root Domains & More using public-suffix List. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sudachi : Nintendo Switch emulator forked from yuzu. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sudo : Execute a command as another user [sudo-rs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sudo : Execute a command as another user [sudo]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sudo_logsrvd : sudo event and I/O log server [sudo]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sudo_sendlog : Send sudo I/O log to log server [sudo]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sudoedit : An extension of the sudo -e command to edit a file [sudo]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sudoreplay : Replay sudo session logs [sudo]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sugarfree : Analyze and Reduce the entropy of a provided PE file. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sulogin : Single user login [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sulogin : Single user login [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sulogin : Single-user login [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sum : Checksum and count the blocks in a file [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sum : Checksum and count the blocks in a file [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sum : Checksum and count the blocks in a file [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sum : Checksum and count the blocks in a file [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sum : Checksum and count the blocks in a file [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sum : Checksum and count the blocks in a file [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sunbeam : CLI Launcher to Generate powerful UIs from simple scripts. To install it use the --soarpkg flag or the .soarpkg extension.
◆ supercronic : A crontab-compatible job runner, designed specifically to run in containers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ supernova : Securely Encrypt and/or Obfuscate raw Shellcode. To install it use the --soarpkg flag or the .soarpkg extension.
◆ supervisord : Supervisor Daemon in Go, A process control system for Unix. To install it use the --soarpkg flag or the .soarpkg extension.
◆ surf : Tool to filter a list of hosts for viable SSRF candidates. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sv : Control services monitored by runsv supervisor [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sv : Control services monitored by runsv supervisor [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ svc : Controls services: start, stop, or send signals to daemons [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ svc : Controls services: start, stop, or send signals to daemons [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ svg-hush : Sanitizes SVGs of arbitrary code. To install it use the --soarpkg flag or the .soarpkg extension.
◆ svlogd : Continuously read log data from standard input [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ svlogd : Continuously read log data from standard input [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ svn : Subversion command line client tool [subversion]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ svn-lite : Minimalist svn client to checkout/update Subversion repositories. To install it use the --soarpkg flag or the .soarpkg extension.
◆ svn2git : Minimalist svn client to checkout/update Subversion repositories. To install it use the --soarpkg flag or the .soarpkg extension.
◆ svnadmin : Subversion repository administration tool [subversion]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ svnbench : Subversion benchmarking tool [subversion]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ svndumpfilter : Filter a subversion repository 'dumpfile' [subversion]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ svnfsfs : Subversion FSFS repository manipulation tool [subversion]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ svnlook : Subversion repository examination tool [subversion]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ svnmucc : Multiple URL Command Client for Subversion [subversion]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ svnrdump : Subversion remote repository dumper and loader [subversion]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ svnserve : Server for the 'svn' repository access method [subversion]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ svnsync : Subversion repository synchronization tool [subversion]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ svnversion : Produce a compact version identifier for a working copy [subversion]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ svok : Checks if a service is running under 'svscan' supervision [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ svok : Checks if a service is running under 'svscan' supervision [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ swaplabel : Display or change the label or UUID of a swap area [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ swapoff : Disable devices and files for paging and swapping [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ swapoff : Stop swapping on DEVICE [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ swapoff : Stop swapping on DEVICE [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ swapon : Enable devices and files for paging and swapping [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ swapon : Start swapping on DEVICE [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ swapon : Start swapping on DEVICE [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ switch-root : Free initramfs and switch to another root fs [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ switch-root : Free initramfs and switch to another root fs [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ switch_root : Free initramfs and switch to another root fs [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ switch_root : Switch to another filesystem as the root of the mount tree [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ symlinks : Create/Manage Symlinks [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ symtree : Display libraries that satisfy undefined symbols, as a tree [pax-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ symtree : Display libraries that satisfy undefined symbols, as a tree [pax-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sync : Synchronize cached writes to persistent storage [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sync : Synchronize cached writes to persistent storage [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sync : Synchronize cached writes to persistent storage [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sync : Synchronize cached writes to persistent storage [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sync : Write all buffered blocks to disk [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sync : Write all buffered blocks to disk [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sync : Write all buffered blocks to disk [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sync-color-schemes : Sync color schemes between wezterm sessions [wezterm]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ syncat : Syntax aware cat utility. To install it use the --soarpkg flag or the .soarpkg extension.
◆ syncthing : FOSS Continuous File Synchronisation. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sysctl : Configure kernel parameters at runtime [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sysctl : Configure kernel parameters at runtime [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sysctl : Configure kernel parameters at runtime [procps]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ sysinfo-collector : collect ram/cpu usage from OS and show it in pretty graphs. To install it use the --soarpkg flag or the .soarpkg extension.
◆ syslogd : System logging utility [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ syslogd : System logging utility [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ system-info-collector : collect ram/cpu usage from OS and show it in pretty graphs. To install it use the --soarpkg flag or the .soarpkg extension.
◆ system_info_collector : collect ram/cpu usage from OS and show it in pretty graphs. To install it use the --soarpkg flag or the .soarpkg extension.
◆ systemctl-tui : Simple TUI for interacting with systemd services and their logs. To install it use the --soarpkg flag or the .soarpkg extension.
◆ t1dotlessj : Create a dotless-j PostScript Type 1 font [lcdf-typetools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ t1lint : Check a PostScript Type 1 font for correctness [lcdf-typetools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ t1rawafm : Produce raw AFM metrics from a PostScript Type 1 font [lcdf-typetools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ t1reencode : Re-encode a PostScript Type 1 font [lcdf-typetools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ t1testpage : Create a PostScript proof for a Type 1 font [lcdf-typetools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tac : Concatenate FILE(s) and print them in reverse [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tac : Concatenate FILE(s) and print them in reverse [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tac : Concatenate and print files in reverse [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tac : Concatenate and print files in reverse [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tac : Concatenate and print files in reverse [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tac : Concatenate and print files in reverse [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tahm : Test HTTP methods and collect responses [lit-bb-hack-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ taierspeed-cli : Speedtest cli for Tel Speed Test. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tail : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tail : Output the last part of files [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tail : Output the last part of files [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tail : Output the last part of files [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tail : Output the last part of files [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tail : Print last N lines of each FILE to standard output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tail : Print last N lines of each FILE to standard output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tailscale : CLI Client for Tailscale, a mesh VPN built on WireGuard. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tailscale-multi : Combined Client & Daemon in a Single Binary (Limited Features). To install it use the --soarpkg flag or the .soarpkg extension.
◆ tailscaled : Daemon for the Tailscale CLI. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tailspin : A log file highlighter and a drop-in replacement for tail -f. To install it use the --soarpkg flag or the .soarpkg extension.
◆ taplo : A TOML toolkit written in Rust. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tar : Create, extract, or list files from a tar file [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tar : Create, extract, or list files from a tar file [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tar : Utility for archiving and extracting files [tar]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tar-split : tar Assembly and Disassembly utility. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tar2sqfs : Create a SquashFS image from a tar archive [squashfs-tools-ng]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tasker : Cron Expression parser with task runner and daemon that supports crontab like task list file. To install it use the --soarpkg flag or the .soarpkg extension.
◆ taskset : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ taskset : Set or get CPU affinity [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ taskset : Set or get CPU affinity [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ taskset : Show or change the CPU affinity of a process [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tc : Show/Manipulate traffic control settings [iproute2]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tcc : Fast Tiny C Compiler. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tcc : Fast Tiny C Compiler. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tcp6 : Exploit potential flaws in the processing of TCP/IPv6 packets [ipv6toolkit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tcpdump : Command-line network traffic analysis tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tcpsvd : Create TCP socket, bind to IP:PORT and listen for incoming connection [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tcpsvd : Create TCP socket, bind to IP:PORT and listen for incoming connection [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tdl : A Telegram downloader/tools written in Golang. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tee : Copy standard input to each FILE, and also to standard output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tee : Copy standard input to each FILE, and also to standard output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tee : Copy standard input to each FILE, and also to standard output [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tee : Read from standard input and write to standard output and files [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tee : Read from standard input and write to standard output and files [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tee : Read from standard input and write to standard output and files [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ teip : Tool to bypass a partial range of standard input to any command. To install it use the --soarpkg flag or the .soarpkg extension.
◆ telnet : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ telnet : Connect to telnet server [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ telnet : Connect to telnet server [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ telnetd : Handle incoming telnet connections [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ telnetd : Handle incoming telnet connections [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ terashuf : Shuffle multi-terabyte text files using limited memory. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tere : Terminal file explorer, A faster alternative to cd + ls. To install it use the --soarpkg flag or the .soarpkg extension.
◆ termshark : A terminal user-interface for tshark, inspired by Wireshark. To install it use the --soarpkg flag or the .soarpkg extension.
◆ test : Check file types and compare values [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ test : Check file types and compare values [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ test : Check file types and compare values [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ test : Check file types and compare values [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ test : Check file types, compare values etc [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ test : Check file types, compare values etc [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ test : the POSIX test command with some extensions for compatibility with extended shells and their scripts. To install it use the --soarpkg flag or the .soarpkg extension.
◆ test1 : Check file types, compare values etc '[' [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ test1 : Check file types, compare values etc '[' [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ test2 : Check file types, compare values etc '[[' [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ test2 : Check file types, compare values etc '[[' [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ test_extent : Tester for the extent abstraction [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ test_icount : Tester for the icount structure [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tftp : Transfer a file from/to tftp server [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tftp : Transfer a file from/to tftp server [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tftpd : Transfer a file on tftp client's request [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tftpd : Transfer a file on tftp client's request [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tget : Like wget but for torrents. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tgpt : AI Chatbots in terminal without needing API keys. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tgz : Make a gzip'd tar archive [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tidy-viewer : CLI csv pretty printer. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tiffcp : Copy (and possibly convert) a TIFF file [tiff]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tiffdump : Print verbatim information about TIFF files [tiff]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tiffinfo : Print information about TIFF files [tiff]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tiffset : Set or unset a field in a TIFF header [tiff]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tiffsplit : Split a multi-image TIFF into single-image TIFF files [tiff]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ time : Run PROG. When it finishes, its resource usage is displayed [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ time : Run PROG. When it finishes, its resource usage is displayed [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ time : Run PROG. When it finishes, its resource usage is displayed [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ timelimitx : Alternative/Advanced version of timeout command. To install it use the --soarpkg flag or the .soarpkg extension.
◆ timeout : Run a command with a time limit [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ timeout : Run a command with a time limit [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ timeout : Run a command with a time limit [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ timeout : Runs PROG. Sends SIG to it if it is not gone in SECS seconds [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ timeout : Runs PROG. Sends SIG to it if it is not gone in SECS seconds [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ timeout : Runs PROG. Sends SIG to it if it is not gone in SECS seconds [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tinja : CLI tool for testing web pages for template injection vulnerabilities. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tinywasm-cli : A tiny, interpreted WebAssembly Runtime written in Rust. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tinyxxd : Drop-in replacement and standalone version of xxd utility. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tio : A serial device I/O tool for connecting to TTY devices. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tip : Connect to a remote system [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tipc : A TIPC configuration and management tool [iproute2]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tlder : Check domain name availability across all valid top-level domains. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tldextract : Separates a URL's subdomain, domain, and public suffix, using the Public Suffix List (PSL). To install it use the --soarpkg flag or the .soarpkg extension.
◆ tldfinder : A streamlined tool for discovering private TLDs for security research. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tldr : Fetch and show tldr help pages for many CLI commands. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tldr : Official tldr/tldr-pages client written in Rust. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tldscan : Scan all possible TLD's for a given domain name. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tload : Graphic representation of system load average [procps]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tlrc : Official tldr/tldr-pages client written in Rust. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tlsx : Fast and configurable TLS grabber focused on TLS based data collection. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tmesis : Generate insertion rules to insert a word from a wordlist at various positions within another string [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tmesis-dynamic : Replace a substring in words from one list with words from another list [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tmux : Tool to control multiple terminals from a single terminal. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tmuxp : Manage tmux workspaces from JSON and YAML. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tok : Extract/Filter substrings from input based on length, alphanumeric rules, and delimiters. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tokei : A blazingly fast CLOC(Count Lines Of Code) program. To install it use the --soarpkg flag or the .soarpkg extension.
◆ toml2json : Convert TOML to JSON. To install it use the --soarpkg flag or the .soarpkg extension.
◆ top : Display Linux processes [procps]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ top : Provide a view of process activity in real time [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ top : Provide a view of process activity in real time [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ topmorph : Generate mutation rules by extracting frequent substrings from a dictionary within a specified range and width [hashcat-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ toru : Bittorrent streaming CLI for anime torrents, real-time with no waiting for downloads. To install it use the --soarpkg flag or the .soarpkg extension.
◆ touch : Change file timestamps [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ touch : Change file timestamps [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ touch : Change file timestamps [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ touch : Change file timestamps [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ touch : Update the last-modified date on the given FILE[s] [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ touch : Update the last-modified date on the given FILE[s] [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ touch : Update the last-modified date on the given FILE[s] [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tpws : Transparent proxy [zapret]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tr : Translate or Delete characters [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tr : Translate or Delete characters [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tr : Translate or Delete characters [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tr : Translate, squeeze, and/or delete characters from standard input, writing to standard output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tr : Translate, squeeze, and/or delete characters from standard input, writing to standard output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tr : Translate, squeeze, and/or delete characters from standard input, writing to standard output [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tracee : Linux Runtime Security and Forensics using eBPF. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tracee-ebpf : Linux Runtime Security and Forensics using eBPF. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tracepath : Trace path to a network host discovering MTU along this path [iputils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ traceroute : Trace the route to IPv4 HOST [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ traceroute : Trace the route to IPv4 HOST [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ traceroute6 : Trace the route to IPv6 HOST [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ traceroute6 : Trace the route to IPv6 HOST [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ traefik : High availability reverse proxy and load balancer. To install it use the --soarpkg flag or the .soarpkg extension.
◆ trafilatura : Command line journal application that stores your journal in a plain text file. To install it use the --soarpkg flag or the .soarpkg extension.
◆ transfersh : Easy and fast file sharing and pastebin server with access from the command-line. To install it use the --soarpkg flag or the .soarpkg extension.
◆ trap : Trap signals and run a variety of commands according to the signals it catches [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ trap : Trap signals and run a variety of commands according to the signals it catches [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tree : Display directories as trees (with optional color/HTML output). To install it use the --soarpkg flag or the .soarpkg extension.
◆ tree : List contents of directories in a tree-like format [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tree : List contents of directories in a tree-like format [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ trep : Selectively stream stdin to stdout/stderr based on regular expressions. To install it use the --soarpkg flag or the .soarpkg extension.
◆ trip : A network diagnostic tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ trippy : A network diagnostic tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ true : Do nothing, successfully [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ true : Do nothing, successfully [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ true : Do nothing, successfully [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ true : Do nothing, successfully [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ true : Return an exit code of TRUE (0) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ true : Return an exit code of TRUE (0) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ true : Return an exit code of TRUE (0) [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ trufflehog : Find, verify, and analyze leaked credentials. To install it use the --soarpkg flag or the .soarpkg extension.
◆ truncate : Shrink or extend the size of a file [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ truncate : Shrink or extend the size of a file [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ truncate : Shrink or extend the size of a file [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ truncate : Shrink or extend the size of a file to the specified size [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ truncate : Shrink or extend the size of a file to the specified size [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ truncate : Shrink or extend the size of a file to the specified size [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ truncate : Shrink or extend the size of a file to the specified size [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ trurl : A command line tool for URL parsing and Manipulation. To install it use the --soarpkg flag or the .soarpkg extension.
◆ try : Inspect a commands effects before modifying your live system. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tryexec : Execute into a command line, with a fallback [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tryexec : Execute into a command line, with a fallback [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ts : Add timestamp to input lines [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ts : Add timestamp to input lines [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ts : Timestamp input [moreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ts : Timestamp input [outils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tscat : Timestamp stdin to stdout/stderr. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tsort : Perform a topological sort [bsdutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tsort : Perform a topological sort [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tsort : Perform a topological sort [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tsort : Perform a topological sort [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tsort : Perform topological sort [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tsort : Perform topological sort [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tsort : Perform topological sort [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tspin : A log file highlighter and a drop-in replacement for tail -f. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tssh : Simple ssh client with trzsz (trz/tsz) support. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ttfb : Measure the TTFB (time to first byte) of HTTP/DNS/TCP/TLS Connections. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ttftotype42 : Create PostScript Type 42 wrapper of TrueType font [lcdf-typetools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tty : Print file name of standard input's terminal [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tty : Print file name of standard input's terminal [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tty : Print file name of standard input's terminal [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tty : Print the file name of the terminal connected to standard input [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tty : Print the file name of the terminal connected to standard input [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tty : Print the file name of the terminal connected to standard input [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tty : Print the file name of the terminal connected to standard input [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tty2web : Share your terminal as a web application in bind/reverse mode. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ttyd : Command-line tool for sharing terminal over the web. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ttyimg : Terminal Image Viewer. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ttyrec2ansi : TTYREC to ANSI converter [colorized-logs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ttysize : Print dimension(s) of standard input's terminal, on error return 80x25 [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ttysize : Print dimension(s) of standard input's terminal, on error return 80x25 [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tuiarchiver : TUI archive manager. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tun2socks : Create a network interface which passes all traffic through proxy. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tunctl : Create and manage TUN/TAP network devices [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tunctl : Create and manage TUN/TAP network devices [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tune.exfat : Userspace utilities for creating and fixing and debugging exFAT filesystem. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tune2fs : Adjust tunable file system parameters on ext2/ext3/ext4 file systems [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tune2fs : Adjust tunable file system parameters on ext2/ext3/ext4 file systems [e2fsprogs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ turbo-attack : Generate random traffic with spoofed MAC, IP, and sequence numbers to a target IP and port. To install it use the --soarpkg flag or the .soarpkg extension.
◆ turbo-scanner : A high-speed port scanner with service detection using 1000 goroutines. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tusd : TUS Resumable Upload Protocol Server. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tut : TUI for Mastodon with vim inspired keys. To install it use the --soarpkg flag or the .soarpkg extension.
◆ twine : Utility for publishing Python packages on PyPI. To install it use the --soarpkg flag or the .soarpkg extension.
◆ twingate : Zero trust remote access client for resource and networks [twingate]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ twingate-notifier : Monitor & Notify for Twingate Client [twingate]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ twingated : Daemon for Twingate [twingate]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ twitch-dl : CLI tool for downloading videos from Twitch. To install it use the --soarpkg flag or the .soarpkg extension.
◆ twolame : MPEG Audio Layer 2 (MP2) encoder. To install it use the --soarpkg flag or the .soarpkg extension.
◆ txeh : CLI utility for /etc/hosts management. To install it use the --soarpkg flag or the .soarpkg extension.
◆ typst : Powerful & New markup-based typesetting system. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tz : 🌐 A time zone helper. To install it use the --soarpkg flag or the .soarpkg extension.
◆ tzupdate : Update timezone information based on geoip. To install it use the --soarpkg flag or the .soarpkg extension.
◆ u-root : Tool to create a one-binary root file system (initramfs) containing a busybox-like set of tools & More. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ubiattach : Attach an UBI device to a MTD device [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ubiattach : Attach an UBI device to a MTD device [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ubidetach : Detach an UBI device from a MTD device [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ubidetach : Detach an UBI device from a MTD device [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ubimkvol : Create an UBI volume [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ubimkvol : Create an UBI volume [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ubirename : Rename UBI volumes [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ubirename : Rename UBI volumes [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ubirmvol : Remove an UBI volume [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ubirmvol : Remove an UBI volume [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ubirsvol : Resize an UBI volume [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ubirsvol : Resize an UBI volume [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ubiupdatevol : Update an UBI volume [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ubiupdatevol : Update an UBI volume [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uclampset : Show or change the utilization clamping attributes [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ucspilogd : Convert syslog facility numbers and alert levels into names [s6]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ udevmon : Monitor input devices for launching tasks [interception-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ udhcpc : Lightweight DHCP client to obtain IPv4 configuration [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ udhcpc : Lightweight DHCP client to obtain IPv4 configuration [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ udhcpc6 : Lightweight DHCP client to obtain IPv6 configuration [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ udhcpc6 : Lightweight DHCP client to obtain IPv6 configuration [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ udhcpd : DHCP server [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ udhcpd : DHCP server [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ udocker : Execute simple Docker Containers in User Space without root privileges. To install it use the --soarpkg flag or the .soarpkg extension.
◆ udon : Find assets/domains based on the Google Analytics ID. To install it use the --soarpkg flag or the .soarpkg extension.
◆ udp6 : Exploit potential flaws in the processing of TCP/IPv6 packets [ipv6toolkit]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ udpsvd : Create UDP socket, bind to IP:PORT and wait for incoming packets [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ udpsvd : Create UDP socket, bind to IP:PORT and wait for incoming packets [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ udpx : Single-packet UDP scanner. To install it use the --soarpkg flag or the .soarpkg extension.
◆ udpz : Speedy probe-based UDP service scanner. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uevent : Handle kernel uevents [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uevent : Handle kernel uevents [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uforall : Passive URL crawler using alienvault, WayBackMachine, urlscan, commoncrawl & more. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uinput : Redirect device input events from stdin to virtual device [interception-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ul : Convert underscores in text to terminal-specific underline sequences [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ul : Do underlining [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ulexec : A tool for loading and executing ELF on Linux from memory. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ulockmgr_server : Lock Manager Server for FUSE filesystems [fuse]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ultimmc : Offline Minecraft launcher. To install it use the --soarpkg flag or the .soarpkg extension.
◆ umask : Unsets the umask (file creation mask) & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ umask : Unsets the umask (file creation mask) & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ umoci : Create, Manipulate, and Interact with container images. To install it use the --soarpkg flag or the .soarpkg extension.
◆ umount : Unmount file systems [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ umount : Unmount file systems [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ umount : Unmount filesystems [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uname : Print system information [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uname : Print system information [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uname : Print system information [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uname : Print system information [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uname : Print system information [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uname : Print system information [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uname : Print system information [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uname26 : Alter the reported kernel version to appear as 2.6.x [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unch : Hide message with invisible Unicode characters. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uncomment : Remove comments from code. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uncompress : Decompress .Z (compress) file [gzip]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uncover : Discover exposed hosts on the internet using multiple search engines. To install it use the --soarpkg flag or the .soarpkg extension.
◆ undock : Extract contents of a container image in a local folder. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unew : A tool for adding new lines to files, skipping duplicates. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unexpand : Convert spaces to tabs [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unexpand : Convert spaces to tabs [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unexpand : Convert spaces to tabs [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unexpand : Convert spaces to tabs [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unexpand : Convert spaces to tabs, writing to standard output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unexpand : Convert spaces to tabs, writing to standard output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unexpand : Convert spaces to tabs, writing to standard output [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unexport : Remove a variable from the environment & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unexport : Remove a variable from the environment & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unf : UNixize Filename, replace annoying anti-unix characters in filenames. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unfurl : Pull out bits of URLs provided on stdin. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unifetch : An unofficial inheritor of Neofetch. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unionfs : Overlay several directories into one single mount point [unionfs-fuse3]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unionfs : Overlay several directories into one single mount point [unionfs-fuse]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unionfsctl : Utility to control unionfs-fuse over ioctl [unionfs-fuse3]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unionfsctl : Utility to control unionfs-fuse over ioctl [unionfs-fuse]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uniq : Discard duplicate lines [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uniq : Discard duplicate lines [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uniq : Discard duplicate lines [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uniq : Report or omit repeated lines [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uniq : Report or omit repeated lines [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uniq : Report or omit repeated lines [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uniq : Report or omit repeated lines [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uniqs : uniq(1) alternative with streaming support. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uniqtoo : A real-time, single-binary alternative to 'sort. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unix2dos : Convert FILE in-place from Unix to DOS format [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unix2dos : Convert FILE in-place from Unix to DOS format [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unix2dos : Unix to DOS and vice versa text file format (line endings) converter [dos2unix]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unix2mac : Unix to MAC and vice versa text file format (line endings) converter [dos2unix]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unixexec : Attach stdin/stdout of a command to a Unix socket . To install it use the --soarpkg flag or the .soarpkg extension.
◆ unlink : Call the unlink function to remove the specified file [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unlink : Call the unlink function to remove the specified file [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unlink : Call the unlink function to remove the specified file [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unlink : Call the unlink function to remove the specified file [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unlink : Remove a file [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unlink : Remove a file [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unlzma : Decompress files in LZMA format [alias=xz --format=lzma --decompress] [xz]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unlzma : Uncompress FILE [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unlzma : Uncompress FILE [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unpigz : A parallel implementation of gzip for multi-core machines. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unshar : Unpack a shar archive [sharutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unshare : Run a program in a new namespace [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unshare : Run a program in a new namespace [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unshare : Run a program with some namespaces unshared from the parent [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unsquashfs : Uncompress, extract and list squashfs filesystems [squashfstools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unvis : Decode characters previously encoded by vis [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unvis : Revert a visual representation of data back to original form [outils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unxz : Decompress files compressed with xz [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unxz : Decompress files compressed with xz [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unxz : Decompress files in XZ format [alias=xz --decompress] [xz]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unzip : Extract files from ZIP archives [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unzip : Extract files from ZIP archives [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ unzstd : Decompress .zst files [alias=zstd -d] [zstd]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ update-alternatives : Maintain symbolic links determining default commands [dpkg]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ update-desktop-database : Update the MIME-types application database [desktop-file-utils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ updatedb : Update a file name database. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uptime : Display the time since the last boot [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uptime : Display the time since the last boot [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uptime : Display the time since the last boot [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uptime : Display the time since the last boot [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uptime : Display the time since the last boot [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uptime : Tell how long the system has been running [procps]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ upx : Extendable, high-performance executable packer for several executable formats. To install it use the --soarpkg flag or the .soarpkg extension.
◆ url-parser : Simple command-line URL parser. To install it use the --soarpkg flag or the .soarpkg extension.
◆ urless : De-clutter a list of URLs. To install it use the --soarpkg flag or the .soarpkg extension.
◆ urlfinder : Passively gather URLs without active scanning. To install it use the --soarpkg flag or the .soarpkg extension.
◆ urlhunter : Recon on URLs that are exposed via shortener services such as bit.ly, goo.gl etc. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uro : Declutter url lists for crawling/pentesting. To install it use the --soarpkg flag or the .soarpkg extension.
◆ urx : Extract URLs from OSINT Archives for Security Insights. To install it use the --soarpkg flag or the .soarpkg extension.
◆ usbhid-dump : Dump USB HID device report descriptors and streams [usbutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ useradd : Create a new user or Update default new user information [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ userdel : Delete a user account and related files [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ usermod : Modify a user account [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ users : List the usernames of currently logged-in users [bsdutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ users : List the usernames of currently logged-in users [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ users : List the usernames of currently logged-in users [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ users : List the usernames of currently logged-in users [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ users : Print the user names of users currently logged in to the current host [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ users : Print the user names of users currently logged in to the current host [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ users : Print the user names of users currently logged in to the current host [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ users : Print the user names of users currently logged in to the current host [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ usleep : Pause for N microseconds [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ usleep : Pause for N microseconds [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ usql : Universal command-line interface for SQL databases. To install it use the --soarpkg flag or the .soarpkg extension.
◆ usql.upx : Upxed (--best) version of the main usql binary. To install it use the --soarpkg flag or the .soarpkg extension.
◆ usque : FOSS reimplementation of the Cloudflare WARP client's MASQUE protocol. To install it use the --soarpkg flag or the .soarpkg extension.
◆ utmpdump : Dump UTMP and WTMP files in raw format [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uudecode : Decode an uuencoded file [sharutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uudecode : Uudecode a file Finds outfile name in uuencoded source unless -o is given [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uudecode : Uudecode a file Finds outfile name in uuencoded source unless -o is given [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uuencode : Encode a binary file to ASCII data [sharutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uuencode : Uuencode a file to stdout [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uuencode : Uuencode a file to stdout [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uuidd : A daemon for generating UUIDs [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uuidgen : Create a new UUID value [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uuidparse : Parse unique identifiers [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ uuinfo : Debug Unique Identifiers (UUID, ULID, Snowflake, etc). To install it use the --soarpkg flag or the .soarpkg extension.
◆ uz : Gunzip and Extract a gzip'd tar'd archive [mtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ v2ray : A proxy server for bypassing network restrictions. To install it use the --soarpkg flag or the .soarpkg extension.
◆ v2raya : A web GUI client of Project V which supports VMess, VLESS, SS, SSR, Trojan, Tuic and Juicity protocols. To install it use the --soarpkg flag or the .soarpkg extension.
◆ v6disk : Convert a v6unix disk image to txtar disk format. To install it use the --soarpkg flag or the .soarpkg extension.
◆ v6run : CLI for v6unix. To install it use the --soarpkg flag or the .soarpkg extension.
◆ validtoml : CLI to validate TOML syntax of input file. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vconfig : Create and remove virtual ethernet devices [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vconfig : Create and remove virtual ethernet devices [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vcut : Cut Ogg Vorbis files [vorbis-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vdir : List directory contents [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vdir : List directory contents [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vdir : List directory contents [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vdpa : vDPA management tool [iproute2]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vegeta : Versatile HTTP load testing tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ veritysetup : Tool to setup and verify dm-verity protected disk volumes [cryptsetup]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vermilion : Linux post exploitation tool for Info Gathering and Exfiltration. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vetu : Create, publish and virtualize ephemeral Linux VMs with ease. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vfox : Version manager with support for Java, Node.js, Flutter, .NET & more. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vhost-fuzzer : Tool to fuzz for interesting vhost. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vhs : Tool for generating terminal GIFs with code. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vi : Edit FILE [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vi : Edit FILE [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vi : VIsual editor iMproved [alias=vim] [vim]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vidir : Edit directories and filenames [moreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ view : Vim in Read-Only Mode [vim]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vigr : Edit the group or shadow-group file [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vim : VIsual editor iMproved [alias=vi] [vim]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vimdiff : Edit between 2 & 8 versions of a file with Vim and show differences [vim]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vimtutor : The VIM Tutor for learning VIM [vim]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vipe : Edit pipe [moreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vipw : Edit the password or shadow-password file [shadow]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vis : Display non-printable characters in a visual format [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vis : Display non-printable characters in a visual format [outils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ visudo : Edit the sudoers file [sudo-rs]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ visudo : Edit the sudoers file [sudo]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vlock : Lock a virtual terminal [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vlock : Lock a virtual terminal [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vmexec : Run a single command in a speedy virtual machine with zero-setup. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vmstat : Report virtual memory statistics [procps]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vnstat : Terminal based network traffic monitor (Client). To install it use the --soarpkg flag or the .soarpkg extension.
◆ vnstat : Terminal based network traffic monitor (Client). To install it use the --soarpkg flag or the .soarpkg extension.
◆ vnstatd : Server daemon for VnStat. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vnstatd : Server daemon for VnStat. To install it use the --soarpkg flag or the .soarpkg extension.
◆ volname : Show CD volume name of the DEVICE (default /dev/cdrom) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ volname : Show CD volume name of the DEVICE (default /dev/cdrom) [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ volta : JS Toolchains as Code. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vopono : Run applications through VPN connections in network namespaces. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vorbiscomment : List or edit comments in Ogg Vorbis files [vorbis-tools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ voyage : Resumable & Stateful Subdomain Enumeration tool combining Active & Passive Discovery Methods. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vtm : Terminal multiplexer with TUI window manager and multi-party session sharing. To install it use the --soarpkg flag or the .soarpkg extension.
◆ vultr-cli : Official command line tool for Vultr services. To install it use the --soarpkg flag or the .soarpkg extension.
◆ w : Shows who is logged in and their activity [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ w : Shows who is logged in and their activity [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ w2c2 : Translate WebAssembly modules to portable C. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wacker : Like Docker, but for WASM. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wackerd : Like Docker, but for WASM. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wadl-dumper : Dump all available paths and/or endpoints on WADL file. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wafw00f : Tool to identify and fingerprint Web Application Firewalls (WAF). To install it use the --soarpkg flag or the .soarpkg extension.
◆ wait : Wait for a set of children & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wait : Wait for a set of children & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wait4x : Wait for a port or a service to enter the requested state. To install it use the --soarpkg flag or the .soarpkg extension.
◆ waitpid : Wait for arbitrary processes [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ walk : Terminal file manager. To install it use the --soarpkg flag or the .soarpkg extension.
◆ walk : walk command, from Torgo, this command walks a directory based on various rules and it print each file's full path to STDOUT. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wall : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wall : Send a message to all logged-in users [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wall : Send a message to all logged-in users [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wall : Write a message to all users [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wappalyzer : Tool to Detect web Technologies. To install it use the --soarpkg flag or the .soarpkg extension.
◆ warp : S3 benchmarking tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ warp-cli : Cloudflare WARP client [cloudflare-warp]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ warp-dex : CLI to perform DEX (Digital Experience Monitoring) tests [cloudflare-warp]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ warp-diag : CLI to diagnose & troubleshoot Warp Connections [cloudflare-warp]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ warp-plus : Anti Censorship Utility using Cloudflare Warp+Psiphon. To install it use the --soarpkg flag or the .soarpkg extension.
◆ warp-scan : Anti Censorship Utility using Cloudflare Warp+Psiphon. To install it use the --soarpkg flag or the .soarpkg extension.
◆ warp-svc : Cloudflare WARP Service Manager/Daemon [cloudflare-warp]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ warp-taskbar : Cloudflare WARP Taskbar Applet [cloudflare-warp]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm-as : Assemble WebAssembly in text format [binaryen+wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm-ctor-eval : A tool that can execute functions (or parts of functions) at compile time [binaryen+wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm-decompile : Decompile a wasm binary into readable C-like syntax [binaryen+wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm-decompile : Decompile a wasm binary into readable C-like syntax [wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm-dis : Un-assemble WebAssembly in binary format into text format [binaryen+wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm-emscripten-finalize : Take a wasm binary produced by llvm+lld and performs emscripten-specific passes over it [binaryen+wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm-fuzz-lattices : The WebAssembly Binary Toolkit. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm-fuzz-types : The WebAssembly Binary Toolkit. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm-interp : Decode and run a WebAssembly binary file using a stack-based interpreter [binaryen+wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm-interp : Decode and run a WebAssembly binary file using a stack-based interpreter [wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm-merge : Merge multiple wasm files into a single file [binaryen+wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm-metadce : A tool to remove parts of Wasm files in a flexible way [binaryen+wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm-objdump : Print information about a wasm binary [binaryen+wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm-objdump : Print information about a wasm binary [wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm-opt : Load WebAssembly and runs Binaryen IR passes on it [binaryen+wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm-reduce : A testcase reducer for WebAssembly files [binaryen+wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm-shell : A shell that can load and interpret WebAssembly code [binaryen+wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm-stats : Output stats for a WASM module [binaryen+wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm-stats : Output stats for a WASM module [wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm-strip : Remove sections of a WebAssembly binary file [binaryen+wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm-strip : Remove sections of a WebAssembly binary file [wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm-tools : CLI for low-level manipulation of WebAssembly modules. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm-validate : Validate a file in the WebAssembly binary format [binaryen+wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm-validate : Validate a file in the WebAssembly binary format [wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm2c : Convert a WebAssembly binary file to a C source and header [binaryen+wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm2c : Convert a WebAssembly binary file to a C source and header [wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm2wat : Translate from the binary format back to the text format [binaryen+wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm2wat : Translate from the binary format back to the text format [wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasm2wat-fuzz : The WebAssembly Binary Toolkit. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasmi-cli : Efficient and Lightweight WebAssembly interpreter with a focus on constrained and embedded systems. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wasmtime : Lightweight WebAssembly runtime that is fast, secure, and standards-compliant. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wast2json : Convert a file in the wasm spec test format to a JSON file and associated wasm binary files [binaryen+wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wast2json : Convert a file in the wasm spec test format to a JSON file and associated wasm binary files [wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wat-desugar : Parse .wat text form and print 'canonical' flat format [binaryen+wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wat-desugar : Parse .wat text form and print 'canonical' flat format [wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wat2wasm : Translate from the text format to the Binary Format [binaryen+wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wat2wasm : Translate from the text format to the Binary Format [wabt]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ watch : Execute a program periodically, showing output fullscreen [procps]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ watch : Run a command periodically and displays its output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ watch : Run a command periodically and displays its output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ watchdog : Monitor the system for hangs or failures, restarting it if necessary [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ watchdog : Monitor the system for hangs or failures, restarting it if necessary [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wavpack : Encode audio files to wavpack [wavpack]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ waybackfetch : Tool for fetching all the available waybackmachine snapshot urls. To install it use the --soarpkg flag or the .soarpkg extension.
◆ waybackrobots : Enumerate old versions of robots.txt paths using Wayback Machine for content discovery. To install it use the --soarpkg flag or the .soarpkg extension.
◆ waybackurls : Fetch all the URLs that the Wayback Machine knows about for a domain. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wc : Print line, word, and byte counts for each FILE [bsdutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wc : Print line, word, and byte counts for each FILE [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wc : Print line, word, and byte counts for each FILE [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wc : Print line, word, and byte counts for each FILE [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wc : Print newline, word, and byte counts for each file [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wc : Print newline, word, and byte counts for each file [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wc : Print newline, word, and byte counts for each file [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wc : Print newline, word, and byte counts for each file [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wcurl : A simple wrapper around curl to easily download files. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wdctl : Show the status of the hardware watchdog [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ web-cache-vulnerability-scanner : CLI tool for testing for web cache poisoning. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wego : A Weather client for the terminal. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wezterm : CLI and frontend to launch the Wezterm GUI [wezterm]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wezterm-mux-server : Multiplex Server for Wezterm [wezterm]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wgcf : Unofficial CLI for Cloudflare Warp. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wgcf-cli : Command-line tool for Cloudflare-WARP API. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wgcfcat : Like netcat, but over Cloudflare WARP. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wget : Retrieve files via HTTP or FTP [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wget : Retrieve files via HTTP or FTP [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wget : Tool for retrieving files using HTTP, HTTPS, and FTP. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wget : Tool for retrieving files using HTTP, HTTPS, and FTP. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wget2 : The successor of GNU Wget, a file and recursive website downloader. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wget2 : The successor of GNU Wget, a file and recursive website downloader. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wharfie : Pull/Unpack a container image to the local filesystem. To install it use the --soarpkg flag or the .soarpkg extension.
◆ what : Show what versions of object modules were used to construct a file [outils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ whereis : Locate programs [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ whereis : Locate the binary, source, and manual-page files for a command [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ which : Locate a COMMAND [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ which : Locate a COMMAND [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ which : Locate a COMMAND [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ which : Shows the full path of (shell) commands. To install it use the --soarpkg flag or the .soarpkg extension.
◆ who : Show who is logged on [bsdutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ who : Show who is logged on [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ who : Show who is logged on [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ who : Show who is logged on [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ who : Show who is logged on [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ who : Show who is logged on [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ who : Show who is logged on [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ whoami : Print effective user name [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ whoami : Print effective user name [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ whoami : Print effective user name [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ whoami : Print effective user name [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ whoami : Print the user name associated with the current effective user id [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ whoami : Print the user name associated with the current effective user id [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ whois : Look up IP or domain registration information [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ whois : Look up IP or domain registration information [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ whois : Lookup tool for domain names and other internet resources. To install it use the --soarpkg flag or the .soarpkg extension.
◆ whoismac : Show vendor information and/or download oui reference list [hcxtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ whoiswatcher : Do bulk whois lookups and get alerted on domains of interest. To install it use the --soarpkg flag or the .soarpkg extension.
◆ whoxysubs : Scrape whoxy subdomains without api key. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wipefs : Wipe signatures from a device [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wireguard : Go Userspace implementation of WireGuard. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wireguard-go : Go Userspace implementation of WireGuard. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wireproxy : Wireguard client that exposes itself as a SOCKS5 proxy. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wiretap : VPN-like proxy server that tunnels traffic via WireGuard without requiring special privileges. To install it use the --soarpkg flag or the .soarpkg extension.
◆ witchcraft : Versatile toolkit for cybersecurity. To install it use the --soarpkg flag or the .soarpkg extension.
◆ withstdinas : Read the entirety of standard input in an environment variable & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ withstdinas : Read the entirety of standard input in an environment variable & execute a program [execline]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wlancap2wpasec : Upload multiple (gzip compressed) pcapng, pcap and cap files to https://wpa-sec.stanev.org [hcxtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wormhole-rs : Rust implementation of Magic Wormhole, with new features and enhancements. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wpjson-parser : Extract URLs from the WordPress wp-json route and identify authentication-less routes. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wrangler : Command-line interface for all things Cloudflare Workers. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wrappe : Packer for creating self-contained single-binary applications from executables and directories. To install it use the --soarpkg flag or the .soarpkg extension.
◆ write : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ write : Send a message to another user [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wrkflw : Validate and execute GitHub Actions workflows locally. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wsl : Whitespace Linter - Forces you to use empty lines. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wstunnel : Traffic tunnel over Websocket or HTTP2 to bypass firewalls/DPI. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wtf : Personal information Dashboard for the terminal. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wth : What The Heck  The better personal information dashboard for your terminal. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wttr : gets weather info from wttr.in. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wush : Simplest & Fastest way to transfer files between computers via WireGuard. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wvgain : Add ReplayGain information to wavpack files [wavpack]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wvtag : Manipulate wavpack metadata [wavpack]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ wvunpack : Decode wavpack encoded files [wavpack]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ x8 : Hidden parameters discovery suite. To install it use the --soarpkg flag or the .soarpkg extension.
◆ x86_64 : Change reported architecture to x86_64 [alias=setarch x86_64] [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xan : The CSV magician. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xargs : A toolset for searching and processing files [Rust (uutils)]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xargs : Build and Execute command lines from standard input. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xargs : Chimera Linux userland Tools. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xargs : Run PROG on every item given by standard input [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xargs : Run PROG on every item given by standard input [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xashds : Xash3D FWGS Dedicated Server. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xcc : Entrypoint to xcc [xcc]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xclip : CLI to X selections (clipboard) [xclip]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xclip-copyfile : Copy files into the X clipboard, preserving tree structure [xclip]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xclip-cutfile : Copy files into the X clipboard & delete them afterwards [xclip]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xclip-pastefile : Paste files from the X clipboard [xclip]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xdo : Small X utility to perform elementary actions on windows. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xdotool : Simulate keyboard input and mouse activity, move and resize windows. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xgettext : Extract gettext strings from source [gettext]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xhost : Utility to control X server access. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xinstall : Install files [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xit : git alternative written in zig. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xmap : Fast Internet-wide IPv6 & IPv4 network scanner. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xmrig : High performance Monero (XMR) CPU miner. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xnlinkfinder : Tool to discover endpoints, potential parameters & more for a given Target. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xo : Core Utilities of https://github.com/xplshn/Andes. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xonsh : Python-ish, BASHwards-compatible shell. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xplr : Hackable, minimal, fast TUI file explorer. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xq : Command-line XML and HTML beautifier and content extractor. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xray : The best v2ray-core, with XTLS support. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xray-knife : Swiss army knife tool for xray-core and sing-box. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xsel : Command-line program for manipulating the X selection (Clipboard). To install it use the --soarpkg flag or the .soarpkg extension.
◆ xsubfind3r : Discover Subdomains for a given Domain from passive sources. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xtee : Split single stream of input into two outputs: standard output and file. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xtest : Condition evaluation utility [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xurlfind3r : Discover URLs for a given domain from passive sources. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xurls : Extract urls from text or files. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xvfb-run : Run a command in a virtual X server environment. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xxd : Create a hex dump of a file or convert a hex dump back to binary [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xxd : Create a hex dump of a file or convert a hex dump back to binary [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xz : Compress files using the xz format [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xz : Compress files using the xz format [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xz : Compress/Decompress files using XZ format [xz]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xzcat : Decompress XZ files to stdout [alias=xz --decompress --stdout] [xz]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xzcat : Decompress and display xz-compressed files to standard output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xzcat : Decompress and display xz-compressed files to standard output [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xzcmp : Compare XZ compressed files [xz]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xzdec : Small and fast decoder for XZ compressed files [xz]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xzdiff : Diff XZ compressed files [xz]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xzegrep : Search XZ compressed files using extended regular expressions [xz]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xzfgrep : Search XZ compressed files using fixed strings [xz]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xzgrep : Search XZ compressed files using regular expressions [xz]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xzless : View XZ compressed files with less [xz]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ xzmore : View XZ compressed files with more [xz]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ y4mblack : Generate black frames in YUV4MPEG2 format [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ y4mcolorbars : Create a YUV4MPEG2 stream containing a colorbar test pattern [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ y4mdenoise : Motion-compensating YUV4MPEG-frame denoiser [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ y4mhist : Generate histogram information from YUV4MPEG2 streams [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ y4minterlace : Interlace/deinterlace YUV4MPEG2 streams [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ y4mivtc : Inverse telecine filter for YUV4MPEG2 streams [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ y4mscaler : Scale/crop/translate a YUV4MPEG2 stream [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ y4mshift : Shift/pad YUV4MPEG2 stream frames [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ y4mspatialfilter : Spatial filtering for YUV4MPEG2 streams [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ y4mstabilizer : Video stabilizer for YUV4MPEG2 streams [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ y4mtopnm : Convert a YUV4MPEG2 stream to PNM images [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ y4mtoppm : Convert YUV4MPEG2 stream to PPM images [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ y4mtoyuv : Convert YUV4MPEG2 stream to raw YUV [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ y4munsharp : Unsharp filter for YUV4MPEG2 streams [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ya : CLI interface for yazi. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yacc : Berkeley YACC. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yalis : Yet Another LinkedIn Scraper. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yamllint : Linter for YAML files. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yataf : yataf extracts secrets and paths from files or urls - its best used against javascript files. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yaui : Yet Another Unix Injector using ptrace. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yazi : Blazing fast terminal file manager. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yes : Output a string repeatedly until killed [coreutils-uutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yes : Output a string repeatedly until killed [coreutils-vlang]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yes : Output a string repeatedly until killed [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yes : Output a string repeatedly until killed [coreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yes : Repeatedly output a line with STRING, or 'y' [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yes : Repeatedly output a line with STRING, or 'y' [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yes : Repeatedly output a line with STRING, or 'y' [chimerautils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yggdrasil : Experimental end-to-end encrypted self-arranging IPv6 network. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yggdrasilctl : Experimental end-to-end encrypted self-arranging IPv6 network. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yip : Tool to apply a cloud-init style yaml file to the system. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yj : Convert between YAML, TOML, JSON, and HCL. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ypdomainname : Show/Set the system's YP domain name. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ypipe : Pipe YUV data between mjpegtools programs [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yq : Portable command-line YAML, JSON, XML, CSV, TOML and properties processor. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ysh : Unix shell with JSON-compatible structured data. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ysh : Unix shell with JSON-compatible structured data. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yt-dlp : CLI program to download videos from YouTube and other sites. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yt-dlp : CLI program to download videos from YouTube and other sites. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yuv2lav : Encode lavtool's raw YUV frame streams into MJPEG files [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yuv4mpeg : YUV4MPEG stream format handler [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yuvcorrect : Color correction for YUV streams [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yuvcorrect_tune : Interactive color correction tuning for YUV streams [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yuvdeinterlace : Deinterlace YUV streams [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yuvdenoise : Motion-Compensating-YUV4MPEG2-frame-denoiser [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yuvfps : Convert to a different frame rate [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yuvinactive : Set a part of the video to a defined state [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yuvkineco : Revert 2-3 pulldown movie [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yuvmedianfilter : A filter for yuv images produced by the mjpeg tools [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yuvscaler : UPscale/DOWNscale a YUV4MPEG2 stream to a specified format [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yuvycsnoise : Noise filter specialized for NTSC Y/C separation noise [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ yuyvtoy4m : Convert YUYV format to YUV4MPEG2 stream [mjpegtools]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ z80asm : An assembler for Z80 assembly [z88dk]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ z88dk-appmake : Process raw binaries generated into specific target machine executables [z88dk]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ z88dk-basck : ROM Basic analyzer [z88dk]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ z88dk-copt : Regex engine used by z88dk-sccz80 and z88dk-zsdcc [z88dk]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ z88dk-dis : Disassembler for 8080, 8085, GBZ80, Z80, Z180, Z80N, EZ80, R800 and Rabbit 2000/3000 [z88dk]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ z88dk-dzx0 : PC-side decompressor counterparties to the z88dk-zx0 [z88dk]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ z88dk-dzx7 : PC-side decompressor counterparties to the z88dk-zx7 [z88dk]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ z88dk-font2pv1000 : Convert a standard z88dk/ZX font into a format suitable for the PV-1000 [z88dk]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ z88dk-gdb : Debugger interface for Z80/Z180 [z88dk]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ z88dk-lib : Installer for third party libraries [z88dk]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ z88dk-sccz80 : z88dk's Native C Compiler [z88dk]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ z88dk-ticks : Emulator used to time execution speed of code fragments [z88dk]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ z88dk-ucpp : C preprocessor invoked for sccz80 and for zsdcc [z88dk]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ z88dk-z80asm : Fully featured Assembler, Linker & Librarian [z88dk]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ z88dk-z80nm : z80asm's companion archiver [z88dk]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ z88dk-z80svg : Translate an SVG vector file in a C source data [z88dk]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ z88dk-zobjcopy : Manipulate z80asm object files [z88dk]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ z88dk-zpragma : Process pragmas embedded in c source [z88dk]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ z88dk-zx0 : Optimal LZ77/LZSS compression [z88dk]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ z88dk-zx7 : Optimal LZ77/LZSS compression (Superseded by zx0) [z88dk]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zapper : Zaps arguments and environment from the process list [zapper]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zapper-stealth : Zaps arguments and environment from the process list (Stealthy) [zapper]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zapret : Autonomous Deep Packet Inspection (DPI) circumvention Toolkit. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zblocklist : Tool for limiting and deduplicating a list of IP addresses using a blocklist or allowlist [zmap]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zcat : Cat compressed file contents without decompressing them to disk [gzip]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zcat : Uncompress and concatenate files [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zcat : Uncompress and concatenate files [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zcc : Generate an output binary out of any set of input source files [z88dk]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zcip : Manage a ZeroConf IPv4 link-local address [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zcip : Manage a ZeroConf IPv4 link-local address [busybox]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zcmp : Compare two compressed files [gzip]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zdiff : Display differences between compressed files [gzip]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zdns : Fast CLI DNS Lookup Tool. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zed : High-performance, multiplayer code editor. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zellij : Versatile, extensible terminal multiplexer. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zerotier-one : Connect a Unix system to ZeroTier networks as virtual ports [zerotier]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zfind : Search for files (even inside tar/zip/7z/rar) using a SQL-WHERE filter. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zforce : Force a '.gz' extension on all gzip files [gzip]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zfxtop : fetch top for gen Z with X written by bubbletea enjoyer. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zgrab2 : Fast Go Application Scanner. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zgrep : Searches compressed files using regular expressions [gzip]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ziterate : IP permutation generation file [zmap]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zless : View compressed file contents one screen at a time [gzip]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zmap : The Fast Internet Scanner [zmap]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zmore : View compressed file contents incrementally [gzip]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ znew : Recompresses .Z (compress) files into .gz format [gzip]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zombie : Powerful Password Blaster/Buster/Cracker/Fuzzer/Sprayer. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zoxide : Fuzzy cd command for interactive shells. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zp : Copy Text/Data from Files/STDOUT buffers directly to the Clipboard. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zramctl : Set up and control zram devices [util-linux]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zrun : Automatically uncompress arguments to command [moreutils]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zsh : The Z shell, A Very advanced and programmable Command Interpreter. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zsplit : Split text into multiple files by line. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zstd : Zstandard Compression/Decompression Utility [zstd]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zstdcat : Output Zstandard compressed files to stdout [alias=zstd -dcf] [zstd]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zstdgrep : Search Zstandard compressed files using regular expressions [zstd]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zstdless : View Zstandard compressed files with less [zstd]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zstdmt : Multi-threaded zstd [alias=zstd -T0] [zstd]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zsv : A command-line utility for parsing CSV. To install it use the --soarpkg flag or the .soarpkg extension.
◆ zsv : A command-line utility for parsing CSV. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ztee : Output buffer and splitter for use with ZMap output data [zmap]. To install it use the --soarpkg flag or the .soarpkg extension.
◆ ztests : A tests harness tool for zmap [zmap]. To install it use the --soarpkg flag or the .soarpkg extension.

